/**
 * EasyClick API 知识库 — 自动生成，安卓平台 (acEvent/无障碍模式)
 * 数据来源: http://127.0.0.1:10089/docs/zh-cn/funcs/
 * 平台: Android only (不再包含 iOS/Harmony)
 * 注意: 所有API以 Android 无障碍(acEvent)模式下的实测结果为准
 */

export interface ApiEntry {
  name: string;
  platform: "android" | "common";
  category: string;
  signature: string;
  description: string;
  params?: { name: string; type: string; desc: string }[];
  returnType?: string;
  example: string;
  cautions?: string[];
}

export const EC_API_DATABASE: ApiEntry[] = [

  // ===================== device =====================
  {
    name: "tcDeviceId",
    platform: "android",
    category: "device",
    signature: "device.tcDeviceId()",
    description: "device.tcDeviceId 三方统计唯一设备标识",
    example: "function main() {\n    var d = device.tcDeviceId();\n    //如果想传给对象,需强转一下,否则会报错\n    d += \"\"\n    toast(d);\n}\n\nmain();",
  },
  {
    name: "getIMEI",
    platform: "android",
    category: "device",
    signature: "device.getIMEI()",
    description: "device.getIMEI 获取IMEI",
    example: "function main() {\n    var imei = device.getIMEI();\n    toast(imei);\n}\n\nmain();",
  },
  {
    name: "getScreenWidth",
    platform: "android",
    category: "device",
    signature: "device.getScreenWidth()",
    description: "device.getScreenWidth 屏幕宽度",
    example: "function main() {\n    var width = device.getScreenWidth();\n    toast(width);\n}\n\nmain();",
  },
  {
    name: "getScreenHeight",
    platform: "android",
    category: "device",
    signature: "device.getScreenHeight()",
    description: "device.getScreenHeight 屏幕高度",
    example: "function main() {\n    var height = device.getScreenHeight();\n    toast(height);\n}\n\nmain();",
  },
  {
    name: "getAndroidId",
    platform: "android",
    category: "device",
    signature: "device.getAndroidId()",
    description: "device.getAndroidId 取得Android ID",
    example: "function main() {\n    var androidId = device.getAndroidId();\n    toast(androidId);\n}\n\nmain();",
  },
  {
    name: "getBrand",
    platform: "android",
    category: "device",
    signature: "device.getBrand()",
    description: "device.getBrand 取得品牌",
    example: "function main() {\n    var brand = device.getBrand();\n    toast(brand);\n}\n\nmain();",
  },
  {
    name: "getModel",
    platform: "android",
    category: "device",
    signature: "device.getModel()",
    description: "device.getModel 取得机型",
    example: "function main() {\n    var model = device.getModel();\n    toast(model);\n}\n\nmain();",
  },
  {
    name: "getImsi",
    platform: "android",
    category: "device",
    signature: "device.getImsi()",
    description: "device.getImsi 取得卡号",
    example: "function main() {\n    var imsi = device.getImsi();\n    toast(imsi);\n}\n\nmain();",
  },
  {
    name: "getSerial",
    platform: "android",
    category: "device",
    signature: "device.getSerial()",
    description: "device.getSerial 取得手机串号",
    example: "function main() {\n    var serial = device.getSerial();\n    toast(serial);\n}\n\nmain();",
  },
  {
    name: "getSdkInt",
    platform: "android",
    category: "device",
    signature: "device.getSdkInt()",
    description: "device.getSdkInt 取得SDK 版本号",
    example: "function main() {\n    var sdkInt = device.getSdkInt();\n    toast(sdkInt);\n}\n\nmain();",
  },
  {
    name: "getOSVersion",
    platform: "android",
    category: "device",
    signature: "device.getOSVersion()",
    description: "device.getOSVersion 取得手机版本号",
    example: "function main() {\n    var osVersion = device.getOSVersion();\n    toast(osVersion);\n}\n\nmain();",
  },
  {
    name: "getMacAddress",
    platform: "android",
    category: "device",
    signature: "device.getMacAddress()",
    description: "device.getMacAddress 获取mac地址",
    example: "function main() {\n    var res = device.getMacAddress();\n    toast(res);\n}\n\nmain();",
  },
  {
    name: "getBattery",
    platform: "android",
    category: "device",
    signature: "device.getBattery()",
    description: "device.getBattery 取得电量",
    example: "function main() {\n    var res = device.getBattery();\n    toast(res);\n}\n\nmain();",
  },
  {
    name: "getTotalMem",
    platform: "android",
    category: "device",
    signature: "device.getTotalMem()",
    description: "device.getTotalMem 取得总内存",
    example: "function main() {\n    var res = device.getTotalMem();\n    toast(res);\n}\n\nmain();",
  },
  {
    name: "getAvailMem",
    platform: "android",
    category: "device",
    signature: "device.getAvailMem()",
    description: "device.getAvailMem 取得可用内存",
    example: "function main() {\n    var res = device.getAvailMem();\n    toast(res);\n}\n\nmain();",
  },
  {
    name: "isCharging",
    platform: "android",
    category: "device",
    signature: "device.isCharging()",
    description: "device.isCharging 是否正在充电",
    example: "function main() {\n    var res = device.isCharging();\n    toast(res);\n}\n\nmain();",
  },
  {
    name: "vibrate",
    platform: "android",
    category: "device",
    signature: "device.vibrate()",
    description: "device.vibrate 发出震动",
    example: "function main() {\n    device.vibrate(1 * 1000);\n}\n\nmain();",
  },
  {
    name: "cancelVibration",
    platform: "android",
    category: "device",
    signature: "device.cancelVibration()",
    description: "device.cancelVibration 取消震动",
    example: "function main() {\n    device.cancelVibration();\n}\n\nmain();",
  },
  {
    name: "keepAwake",
    platform: "android",
    category: "device",
    signature: "device.keepAwake()",
    description: "device.keepAwake 保持设备唤醒",
    example: "function main() {\n    importClass(android.os.PowerManager)\n    device.keepAwake(PowerManager.SCREEN_DIM_WAKE_LOCK | PowerManager.ACQUIRE_CAUSES_WAKEUP);\n}\n\nmain();",
  },
  {
    name: "keepScreenOn",
    platform: "android",
    category: "device",
    signature: "device.keepScreenOn()",
    description: "device.keepScreenOn 保持屏幕唤醒状态",
    example: "function main() {\n    device.keepScreenOn();\n}\n\nmain();",
  },
  {
    name: "keepScreenDim",
    platform: "android",
    category: "device",
    signature: "device.keepScreenDim()",
    description: "device.keepScreenDim 保持设备昏暗状态",
    example: "function main() {\n    device.keepScreenDim();\n}\n\nmain();",
  },
  {
    name: "cancelKeepingAwake",
    platform: "android",
    category: "device",
    signature: "device.cancelKeepingAwake()",
    description: "function main() {    device.cancelKeepingAwake();}main();上一页OCR识别下一页悬浮窗函数说明device.tcDeviceId 三方统计唯一设备标识device.getIMEI 获取IMEIdevice.getScreenWidth 屏幕宽度device.getScreenHeight 屏幕高度device.getAndroidId 取得Android IDdevice.getBrand 取得品牌device.getModel 取得机型device.getImsi 取得卡号device.getSerial 取得手机串号device...",
    example: "function main() {\n    device.cancelKeepingAwake();\n}\n\nmain();",
  },
  {
    name: "setAgentSupportNode",
    platform: "android",
    category: "device",
    signature: "setAgentSupportNode()",
    description: "setAgentSupportNode 设置代理模式下获取节点方式",
    example: "function main() {\n  setAgentSupportNode(\"2\");\n}\n\nmain();",
  },
  {
    name: "setShellDumpCompressed",
    platform: "android",
    category: "device",
    signature: "setShellDumpCompressed()",
    description: "setShellDumpCompressed 代理模式dump压缩模式",
    example: "function main() {\n  setAgentSupportNode(\"2\")\n  startEnv()\n  let a = setShellDumpCompressed(1);\n  logd(\"setShellDumpCompressed \" + a)\n}\n\nmain();",
  },
  {
    name: "setGestureActionMode",
    platform: "android",
    category: "device",
    signature: "setGestureActionMode()",
    description: "setGestureActionMode 设置手势模式事件",
    example: "function main() {\n  setGestureActionMode(1);\n  // setGestureActionMode(2);\n}\n\nmain();",
  },
  {
    name: "clickPoint",
    platform: "android",
    category: "device",
    signature: "clickPoint()",
    description: "clickPoint 坐标点击",
    example: "function main() {\n  var result = clickPoint(100, 100);\n  if (result) {\n    toast(\"点击成功\");\n  } else {\n    toast(\"点击失败\");\n  }\n}\n\nmain();",
  },
  {
    name: "click",
    platform: "android",
    category: "device",
    signature: "click()",
    description: "click 选择器点击",
    example: "function main() {\n  var selector = text(\"我是文本\");\n  var result = click(selector);\n  if (result) {\n    toast(\"点击成功\");\n  } else {\n    toast(\"点击失败\");\n  }\n}\n\nmain();",
  },
  {
    name: "clickEx",
    platform: "android",
    category: "device",
    signature: "clickEx()",
    description: "clickEx 无指针点击",
    example: "function main() {\n  var selector = text(\"我是文本\");\n  var result = clickEx(selector);\n  if (result) {\n    toast(\"点击成功\");\n  } else {\n    toast(\"点击失败\");\n  }\n}\n\nmain();",
  },
  {
    name: "longClickEx",
    platform: "android",
    category: "device",
    signature: "longClickEx()",
    description: "longClickEx 无指针长点击",
    example: "function main() {\n  var selector = text(\"我是文本\");\n  var result = longClickEx(selector);\n  if (result) {\n    toast(\"点击成功\");\n  } else {\n    toast(\"点击失败\");\n  }\n}\n\nmain();",
  },
  {
    name: "clickRandom",
    platform: "android",
    category: "device",
    signature: "clickRandom()",
    description: "clickRandom 选择器随机点击",
    example: "function main() {\n  var selector = text(\"我是文本\");\n  var result = clickRandom(selector);\n  if (result) {\n    toast(\"点击成功\");\n  } else {\n    toast(\"点击失败\");\n  }\n}\n\nmain();",
  },
  {
    name: "clickRandomEx",
    platform: "android",
    category: "device",
    signature: "clickRandomEx()",
    description: "clickRandomEx 无指针随机点击",
    example: "function main() {\n  var selector = text(\"我是文本\");\n  var result = clickRandomEx(selector);\n  if (result) {\n    toast(\"点击成功\");\n  } else {\n    toast(\"点击失败\");\n  }\n}\n\nmain();",
  },
  {
    name: "clickRandomRect",
    platform: "android",
    category: "device",
    signature: "clickRandomRect()",
    description: "clickRandomRect 区域随机点击",
    example: "function main() {\n  var rect = new Rect();\n  rect.left = 10;\n  rect.right = 200;\n  rect.top = 10;\n  rect.bottom = 400;\n  var result = clickRandomRect(rect);\n  if (result) {\n    toast(\"点击成功\");\n  } else {\n    toast(\"点击失败\");\n  }\n}\n\nmain();",
  },
  {
    name: "clickCenter",
    platform: "android",
    category: "device",
    signature: "clickCenter()",
    description: "clickCenter 点击中心点",
    example: "function main() {\n  var rect = new Rect();\n  rect.left = 10;\n  rect.right = 200;\n  rect.top = 10;\n  rect.bottom = 400;\n  var result = clickCenter(rect);\n  if (result) {\n    toast(\"点击成功\");\n  } else {\n    toast(\"点击失败\");\n  }\n}\n\nmain();",
  },
  {
    name: "clickText",
    platform: "android",
    category: "device",
    signature: "clickText()",
    description: "clickText 点击文本",
    example: "function main() {\n  var result = clickText(\"设置\");\n  if (result) {\n    toast(\"点击成功\");\n  } else {\n    toast(\"点击失败\");\n  }\n}\n\nmain();",
  },
  {
    name: "longClick",
    platform: "android",
    category: "device",
    signature: "longClick()",
    description: "longClick 选择器长点击",
    example: "function main() {\n  var selector = text(\"我是文本\");\n  var result = longClick(selector);\n  if (result) {\n    toast(\"点击成功\");\n  } else {\n    toast(\"点击失败\");\n  }\n}\n\nmain();",
  },
  {
    name: "longClickPoint",
    platform: "android",
    category: "device",
    signature: "longClickPoint()",
    description: "longClickPoint 坐标长点击",
    example: "function main() {\n  var result = longClickPoint(100, 100);\n  if (result) {\n    toast(\"点击成功\");\n  } else {\n    toast(\"点击失败\");\n  }\n}\n\nmain();",
  },
  {
    name: "press",
    platform: "android",
    category: "device",
    signature: "press()",
    description: "press 坐标长按",
    example: "function main() {\n  var result = press(100, 100, 5000);\n  if (result) {\n    toast(\"长\u0000\u0000按成功\");\n  } else {\n    toast(\"长按失败\");\n  }\n}\n\nmain();",
  },
  {
    name: "multiTouch",
    platform: "android",
    category: "device",
    signature: "multiTouch()",
    description: "multiTouch 多点触摸",
    example: "function main() {\n  utils.openAppByName(\"视频\");\n  sleep(3000);\n  //第一种数组式的写法\n  var touch1 = [\n    {\"action\": 0, \"x\": 500, \"y\": 1200, \"pointer\": 1, \"delay\": 1},\n    {\"action\": 2, \"x\": 500, \"y\": 1100, \"pointer\": 1, \"delay\": 20},\n    {\"action\": 2, \"x\": 500, \"y\": 1000, \"pointer\": 1, \"delay\": 20},\n    {\"action\": 1, \"x\": 1, \"y\": 1, \"pointer\": 1, \"delay\": 20}\n  ]\n  //第二种链式调用方法\n  var touch1 = MultiPoint\n    .get()\n    .action(0).x(500).y(1200).pointer(1).delay(1)\n    .next()\n    .action(2).x(500).y(1100).pointer(1).delay(1)\n    .next()\n    .action(2).x(500).y(1000).pointer(1).delay(1)\n    .next()\n    .action(2).x(500).y(900).pointer(1).delay(1)\n    .next()\n    .action(1).x(500).y(800).pointer(1).delay(1);\n  var touch2 = MultiPoint\n    .get()\n    .action(0).x(300).y(1200).pointer(2).delay(1)\n    .next()\n    .action(2).x(300).y(1100).pointer(2).delay(1)\n    .next()\n    .action(2).x(300).y(1000).pointer(2).delay(1)\n    .next()\n    .action(2).x(300).y(900).pointer(2).delay(1)\n    .next()\n    .action(1).x(300).y(800).pointer(2).delay(1);\n  var x = multiTouch(touch1, touch2, null, 30000);\n  logd(\"xxs \" + x);\n}\n\nmain();",
  },
  {
    name: "touchDown",
    platform: "android",
    category: "device",
    signature: "touchDown()",
    description: "touchDown 执行按下",
    example: "function main() {\n  //整个动作需要配合使用,单独无效\n  //按下\n  touchDown(100, 100)\n  sleep(50)\n  //移动\n  touchMove(100, 150)\n  sleep(50)\n  //移动\n  touchMove(100, 200)\n  sleep(50)\n  //抬起\n  touchUp(100, 200)\n  sleep(200)\n}\n\nmain();",
  },
  {
    name: "touchMove",
    platform: "android",
    category: "device",
    signature: "touchMove()",
    description: "touchMove 执行移动",
    example: "function main() {\n  //整个动作需要配合使用,单独无效\n  //按下\n  touchDown(100, 100)\n  sleep(50)\n  //移动\n  touchMove(100, 150)\n  sleep(50)\n  //移动\n  touchMove(100, 200)\n  sleep(50)\n  //抬起\n  touchUp(100, 200)\n  sleep(200)\n}\n\nmain();",
  },
  {
    name: "touchUp",
    platform: "android",
    category: "device",
    signature: "touchUp()",
    description: "touchUp 执行弹起",
    example: "function main() {\n  //整个动作需要配合使用,单独无效\n  //按下\n  touchDown(100, 100)\n  sleep(50)\n  //移动\n  touchMove(100, 150)\n  sleep(50)\n  //移动\n  touchMove(100, 200)\n  sleep(50)\n  //抬起\n  touchUp(100, 200)\n  sleep(200)\n}\n\nmain();",
  },
  {
    name: "resetAllTouchState",
    platform: "android",
    category: "device",
    signature: "resetAllTouchState()",
    description: "resetAllTouchState 重置touch状态",
    example: "function main() {\n  resetAllTouchState();\n  logd(touchDownEx(300,500,1));\n  logd(touchMoveEx(300,500,1));\n  sleep(2000)\n  while(true){\n    sleep(10)\n    logd(touchDownEx(610,510,2));\n    sleep(1000)\n    logd(touchMoveEx(660,660,2));\n    sleep(2000)\n    logd(touchUpEx(710,710,2));\n    sleep(2000)\n  }\n}\n\nmain();",
  },
  {
    name: "touchDownEx",
    platform: "android",
    category: "device",
    signature: "touchDownEx()",
    description: "touchDownEx 执行弹起",
    example: "function main() {\n  resetAllTouchState();\n  logd(touchDownEx(300,500,1));\n  logd(touchMoveEx(300,500,1));\n  sleep(2000)\n  while(true){\n    sleep(10)\n    logd(touchDownEx(610,510,2));\n    sleep(1000)\n    logd(touchMoveEx(660,660,2));\n    sleep(2000)\n    logd(touchUpEx(710,710,2));\n    sleep(2000)\n  }\n}\n\nmain();",
  },
  {
    name: "touchMoveEx",
    platform: "android",
    category: "device",
    signature: "touchMoveEx()",
    description: "touchMoveEx 执行弹\u0000起",
    example: "function main() {\n  resetAllTouchState();\n  logd(touchDownEx(300,500,1));\n  logd(touchMoveEx(300,500,1));\n  sleep(2000)\n  while(true){\n    sleep(10)\n    logd(touchDownEx(610,510,2));\n    sleep(1000)\n    logd(touchMoveEx(660,660,2));\n    sleep(2000)\n    logd(touchUpEx(710,710,2));\n    sleep(2000)\n  }\n}\n\nmain();",
  },
  {
    name: "touchUpEx",
    platform: "android",
    category: "device",
    signature: "touchUpEx()",
    description: "touchUpEx 执行弹起",
    example: "function main() {\n  resetAllTouchState();\n  logd(touchDownEx(300,500,1));\n  logd(touchMoveEx(300,500,1));\n  sleep(2000)\n  while(true){\n    sleep(10)\n    logd(touchDownEx(610,510,2));\n    sleep(1000)\n    logd(touchMoveEx(660,660,2));\n    sleep(2000)\n    logd(touchUpEx(710,710,2));\n    sleep(2000)\n  }\n}\n\nmain();",
  },
  {
    name: "scrollForward",
    platform: "android",
    category: "device",
    signature: "scrollForward()",
    description: "scrollForward 无指针向前滚动",
    example: "function main() {\n  var selector = scrollable(true);\n  var result = scrollForward(selector);\n  if (result) {\n    toast(\"滚动成功\");\n  } else {\n    toast(\"滚动失败\");\n  }\n}\n\nmain();",
  },
  {
    name: "scrollBackward",
    platform: "android",
    category: "device",
    signature: "scrollBackward()",
    description: "scrollBackward 无指针向后滚动",
    example: "function main() {\n  var selector = scrollable(true);\n  var result = scrollBackward(selector);\n  if (result) {\n    toast(\"滚动成功\");\n  } else {\n    toast(\"滚动失败\");\n  }\n}\n\nmain();",
  },
  {
    name: "swipe",
    platform: "android",
    category: "device",
    signature: "swipe()",
    description: "swipe 滑动",
    example: "function main() {\n  var selectors = text(\"我是文本\");\n  var result = swipe(selectors, 100, 100, 200);\n  if (result) {\n    toast(\"滑动成功\");\n  } else {\n    toast(\"滑动失败\");\n  }\n}\n\nmain();",
  },
  {
    name: "swipeToPoint",
    platform: "android",
    category: "device",
    signature: "swipeToPoint()",
    description: "swipeToPoint 坐标点滑动",
    example: "function main() {\n  var result = swipeToPoint(10, 10, 100, 100, 200);\n  if (result) {\n    toast(\"滑动成功\");\n  } else {\n    toast(\"滑动失败\");\n  }\n}\n\nmain();",
  },
  {
    name: "isScrollEnd",
    platform: "android",
    category: "device",
    signature: "isScrollEnd()",
    description: "isScrollEnd 滚到底部判断",
    example: "function main() {\n  var selectors = clz(\"android.widget.ListView\");\n  var result = isScrollEnd(\"UP\", selectors);\n  if (result) {\n    toast(\"滚动完成\");\n  } else {\n    toast(\"滚动未完成\");\n  }\n}\n\nmain();",
  },
  {
    name: "drag",
    platform: "android",
    category: "device",
    signature: "drag()",
    description: "drag 拖动坐标",
    example: "function main() {\n  var result = drag(10, 10, 100, 100, 200);\n  if (result) {\n    toast(\"拖动成功\");\n  } else {\n    toast(\"拖动失败\");\n  }\n}\n\nmain();",
  },
  {
    name: "dragTo",
    platform: "android",
    category: "device",
    signature: "dragTo()",
    description: "dragTo 拖动选择器",
    example: "function main() {\n  var selectors = text(\"设置\");\n  var destObj = text(\"日历\");\n  var result = dragTo(selectors, destObj, 200);\n  if (result) {\n    toast(\"拖动成功\");\n  } else {\n    toast(\"拖动失败\");\n  }\n}\n\nmain();",
  },
  {
    name: "dragToPoint",
    platform: "android",
    category: "device",
    signature: "dragToPoint()",
    description: "dragToPoint 拖动到目标选择器",
    example: "function main() {\n  var selectors = text(\"设置\");\n  var result = dragToPoint(selectors, 100, 100, 200);\n  if (result) {\n    toast(\"拖动成功\");\n  } else {\n    toast(\"拖动失败\");\n  }\n}\n\nmain();",
  },
  {
    name: "imeInputViewShown",
    platform: "android",
    category: "device",
    signature: "imeInputViewShown()",
    description: "imeInputViewShown 输入法键盘是否展示",
    example: "function main() {\n  var result = imeInputViewShown();\n  if (result) {\n    toast(\"是\");\n  } else {\n    toast(\"否\");\n  }\n}\n\nmain();",
  },
  {
    name: "currentIsOurIme",
    platform: "android",
    category: "device",
    signature: "currentIsOurIme()",
    description: "currentIsOurIme 是否是自带输入法",
    example: "function main() {\n  var result = currentIsOurIme();\n  if (result) {\n    toast(\"是\");\n  } else {\n    toast(\"否\");\n  }\n}\n\nmain();",
  },
  {
    name: "inputText",
    platform: "android",
    category: "device",
    signature: "inputText()",
    description: "inputText 输入数据",
    example: "function main() {\n  var selectors = clz(\"android.widget.EditText\");\n  var result = inputText(selectors, \"我是内容\");\n  if (result) {\n    toast(\"是\");\n  } else {\n    toast(\"否\");\n  }\n}\n\nmain();",
  },
  {
    name: "imeInputText",
    platform: "android",
    category: "device",
    signature: "imeInputText()",
    description: "imeInputText 输入法输入数据",
    example: "function main() {\n\n  // 打开软键盘\n  //imeInputText(null, \"--ec_open_input--\");\n\n  var selectors = clz(\"android.widget.EditText\");\n  var result = imeInputText(selectors, \"我是内容\");\n\n  if (result) {\n    toast(\"是\");\n  } else {\n    toast(\"否\");\n  }\n}\n\nmain();",
  },
  {
    name: "imeInputKeyCode",
    platform: "android",
    category: "device",
    signature: "imeInputKeyCode()",
    description: "imeInputKeyCode 输入法输入code",
    example: "function main() {\n  // 模拟搜索按键\n  // imeInputKeyCode(null,1000);\n  var selectors = clz(\"android.widget.EditText\");\n  var result = imeInputKeyCode(selectors, 66);\n  if (result) {\n    toast(\"是\");\n  } else {\n    toast(\"否\");\n  }\n}\n\nmain();",
  },
  {
    name: "pasteText",
    platform: "android",
    category: "device",
    signature: "pasteText()",
    description: "pasteText 粘贴数据",
    example: "function main() {\n  var selectors = clz(\"android.widget.EditText\");\n  var result = pasteText(selectors, \"我是内容\");\n  if (result) {\n    toast(\"是\");\n  } else {\n    toast(\"否\");\n  }\n}\n\nmain();",
  },
  {
    name: "clearTextField",
    platform: "android",
    category: "device",
    signature: "clearTextField()",
    description: "clearTextField 清除数据",
    example: "function main() {\n  var selectors = clz(\"android.widget.EditText\");\n  var result = clearTextField(selectors);\n  if (result) {\n    toast(\"是\");\n  } else {\n    toast(\"否\");\n  }\n}\n\nmain();",
  },
  {
    name: "lastNodeEventTime",
    platform: "android",
    category: "device",
    signature: "lastNodeEventTime()",
    description: "lastNodeEventTime 最近节点事件发生时间",
    example: "function main() {\n  startEnv();\n  logd(\"开始监听\");\n\n  while (true) {\n    let d = lastNodeEventTime();\n    logd(\"time-\" + d);\n    sleep(1000)\n  }\n}\n\nmain();",
  },
  {
    name: "setFocus",
    platform: "android",
    category: "device",
    signature: "setFocus()",
    description: "setFocus 设置节点聚焦",
    example: "function main() {\n  var selectors = text(\"设置\");\n  var result = setFocus(selectors);\n  if (result) {\n    toast(\"ok\");\n  } else {\n    toast(\"false\");\n  }\n}\n\nmain();",
  },
  {
    name: "has",
    platform: "android",
    category: "device",
    signature: "has()",
    description: "has 节点存在判断",
    example: "function main() {\n  var selectors = text(\"设置\");\n  var result = has(selectors);\n  if (result) {\n    toast(\"存在节点\");\n  } else {\n    toast(\"不存在节点\");\n  }\n}\n\nmain();",
  },
  {
    name: "waitExistActivity",
    platform: "android",
    category: "device",
    signature: "waitExistActivity()",
    description: "waitExistActivity 等界面出现",
    example: "function main() {\n  var ac = \"com.xxx.MainActivity\";\n  var result = waitExistActivity(ac, 10000);\n  if (result) {\n    toast(\"存在界面\");\n  } else {\n    toast(\"不存在界面\");\n  }\n}\n\nmain();",
  },
  {
    name: "waitExistNode",
    platform: "android",
    category: "device",
    signature: "waitExistNode()",
    description: "waitExistNode 等节点出现",
    example: "function main() {\n  var selectors = text(\"设置\");\n  var result = waitExistNode(selectors, 10000);\n  if (result) {\n    toast(\"存在节点\");\n  } else {\n    toast(\"不存在节点\");\n  }\n}\n\nmain();",
  },
  {
    name: "getText",
    platform: "android",
    category: "device",
    signature: "getText()",
    description: "getText 获取文本",
    example: "function main() {\n  var selectors = clz(\"android.widget.TextView\");\n  var result = getText(selectors);\n  toast(\"result:\" + result);\n}\n\nmain();",
  },
  {
    name: "getNodeInfo",
    platform: "android",
    category: "device",
    signature: "getNodeInfo()",
    description: "getNodeInfo 获取节点数组",
    example: "function main() {\n  var result = getNodeInfo(clz(\"android.widget.TextView\"), 10 * 1000);\n  toast(\"result:\" + JSON.stringify(result));\n}\n\nmain();",
  },
  {
    name: "getNodeAttrs",
    platform: "android",
    category: "device",
    signature: "getNodeAttrs()",
    description: "getNodeAttrs 节点属性信息",
    example: "function main() {\n  var selectors = clz(\"android.widget.TextView\");\n  //获取所有text属性\n  var result = getNodeAttrs(selectors, \"text\");\n  toast(\"result:\" + result);\n  //获取所有bounds属性\n  result = getNodeAttrs(selectors, \"bounds\");\n  toast(\"result:\" + result);\n}\n\nmain();",
  },
  {
    name: "getOneNodeInfo",
    platform: "android",
    category: "device",
    signature: "getOneNodeInfo()",
    description: "getOneNodeInfo 获取单个节点",
    example: "function main() {\n  var result = getOneNodeInfo(clz(\"android.widget.TextView\"), 10 * 1000);\n  toast(\"result:\" + JSON.stringify(result));\n  if (result) {\n    result.click();\n  }\n}\n\nmain();",
  },
  {
    name: "setFetchNodeMode",
    platform: "android",
    category: "device",
    signature: "setFetchNodeMode()",
    description: "setFetchNodeMode 获取节点的模式",
    example: "function main() {\n  // 脚本开头执行一次即可\n  var result = setFetchNodeMode(1, false, true, \"nsf\");\n  toast(\"result:\" + result);\n}\n\nmain();",
  },
  {
    name: "setNodeDumpParam",
    platform: "android",
    category: "device",
    signature: "setNodeDumpParam()",
    description: "setNodeDumpParam 设置nsf抓节点参数",
    example: "function main() {\n  // 脚本开头执行一次即可\n  var result = setNodeDumpParam(1, false, true, \"nsf\");\n  toast(\"result:\" + result);\n  logd(dumpXml())\n}\n\nmain();",
  },
  {
    name: "setBlockNodeAttr",
    platform: "android",
    category: "device",
    signature: "setBlockNodeAttr()",
    description: "setBlockNodeAttr 屏蔽节点属性",
    example: "function main() {\n  var result = setBlockNodeAttr(\"row,bounds,index\");\n  toast(\"result:\" + result);\n  let data = dumpXml()\n  logd(data)\n\n  // 恢复默认\n  setBlockNodeAttr(\"\")\n}\n\nmain();",
  },
  {
    name: "addNodeFlag",
    platform: "android",
    category: "device",
    signature: "addNodeFlag()",
    description: "addNodeFlag 加上节点获取的某个标志位",
    example: "function main() {\n  addNodeFlag(0);\n}\n\nmain();",
  },
  {
    name: "removeNodeFlag",
    platform: "android",
    category: "device",
    signature: "removeNodeFlag()",
    description: "removeNodeFlag 移除节点获取的某个标志位[强制刷新节点]",
    example: "function main() {\n  removeNodeFlag(0);\n}\n\nmain();",
  },
  {
    name: "dumpXml",
    platform: "android",
    category: "device",
    signature: "dumpXml()",
    description: "dumpXml 元素变XML",
    example: "function main() {\n  var result = dumpXml();\n  if (result) {\n    toast(\"ok\");\n  } else {\n    toast(\"no\");\n  }\n}\n\nmain();",
  },
  {
    name: "lockNode",
    platform: "android",
    category: "device",
    signature: "lockNode()",
    description: "lockNode 锁定当前节点",
    example: "function main() {\n  logd(\"锁住节点...\")\n  //锁住节点，界面刷新也不动\n  console.time(\"1\")\n  lockNode()\n  for (let i = 0; i < 10; i++) {\n    let n = text(\"设置\").getOneNodeInfo(1000)\n    logd(\"lock \" + n)\n  }\n  logd(\"释放节点锁...\")\n  //释放节点锁\n  releaseNode()\n  logd(console.timeEnd(\"1\"))\n\n  console.time(\"1\")\n  for (var i = 0; i < 10; i++) {\n    let n = text(\"设置\").getOneNodeInfo(1000)\n    logd(\"unlocked \" + n)\n  }\n  logd(console.timeEnd(\"1\"))\n  //锁住时间明显更短\n}\n\nmain();",
  },
  {
    name: "releaseNode",
    platform: "android",
    category: "device",
    signature: "releaseNode()",
    description: "releaseNode 释放节点的锁",
    example: "function main() {\n  logd(\"锁住节点...\")\n  //锁住节点，界面刷新也不动\n  console.time(\"1\")\n  lockNode()\n  for (let i = 0; i < 10; i++) {\n    let n = text(\"设置\").getOneNodeInfo(1000)\n    logd(\"lock \" + n)\n  }\n  logd(\"释放节点锁...\")\n  //释放节点锁\n  releaseNode()\n  logd(console.timeEnd(\"1\"))\n\n  console.time(\"1\")\n  for (var i = 0; i < 10; i++) {\n    let n = text(\"设置\").getOneNodeInfo(1000)\n    logd(\"unlocked \" + n)\n  }\n  logd(console.timeEnd(\"1\"))\n  //锁住时间明显更短\n}\n\nmain();",
  },
  {
    name: "home",
    platform: "android",
    category: "device",
    signature: "home()",
    description: "home 返回主页",
    example: "function main() {\n  var result = home();\n  if (result) {\n    toast(\"成功\");\n  } else {\n    toast(\"失败\");\n  }\n}\n\nmain();",
  },
  {
    name: "home2",
    platform: "android",
    category: "device",
    signature: "home2()",
    description: "home2 返回主页2",
    example: "function main() {\n  var result = home2();\n  if (result) {\n    toast(\"成功\");\n  } else {\n    toast(\"失败\");\n  }\n}\n\nmain();",
  },
  {
    name: "splitScreen",
    platform: "android",
    category: "device",
    signature: "splitScreen()",
    description: "splitScreen 分割屏幕",
    example: "function main() {\n  var result = splitScreen();\n  if (result) {\n    toast(\"成功\");\n  } else {\n    toast(\"失败\");\n  }\n}\n\nmain();",
  },
  {
    name: "power",
    platform: "android",
    category: "device",
    signature: "power()",
    description: "power 模拟电源按键",
    example: "function main() {\n  var result = power();\n  if (result) {\n    toast(\"成功\");\n  } else {\n    toast(\"失败\");\n  }\n}\n\nmain();",
  },
  {
    name: "back",
    platform: "android",
    category: "device",
    signature: "back()",
    description: "back 返回键",
    example: "function main() {\n  var result = back();\n  if (result) {\n    toast(\"成功\");\n  } else {\n    toast(\"失败\");\n  }\n}\n\nmain();",
  },
  {
    name: "openNotification",
    platform: "android",
    category: "device",
    signature: "openNotification()",
    description: "openNotification 打开通知栏",
    example: "function main() {\n  var result = openNotification();\n  if (result) {\n    toast(\"成功\");\n  } else {\n    toast(\"失败\");\n  }\n}\n\nmain();",
  },
  {
    name: "openQuickSettings",
    platform: "android",
    category: "device",
    signature: "openQuickSettings()",
    description: "openQuickSettings 打开快速设置",
    example: "function main() {\n  var result = openQuickSettings();\n  if (result) {\n    toast(\"成功\");\n  } else {\n    toast(\"失败\");\n  }\n}\n\nmain();",
  },
  {
    name: "recentApps",
    platform: "android",
    category: "device",
    signature: "recentApps()",
    description: "recentApps 最近APP任务按键",
    example: "function main() {\n  var result = recentApps();\n  if (result) {\n    toast(\"成功\");\n  } else {\n    toast(\"失败\");\n  }\n}\n\nmain();",
  },
  {
    name: "getCurrentRunningPkg",
    platform: "android",
    category: "device",
    signature: "getCurrentRunningPkg()",
    description: "getCurrentRunningPkg 当前运行的App包名",
    example: "function main() {\n  var result = getCurrentRunningPkg();\n  logd(result)\n}\n\nmain();",
  },
  {
    name: "getRunningPkg",
    platform: "android",
    category: "device",
    signature: "getRunningPkg()",
    description: "getRunningPkg 当前运行的App包名",
    example: "function main() {\n  var result = getRunningPkg();\n}\n\nmain();",
  },
  {
    name: "getRunningActivity",
    platform: "android",
    category: "device",
    signature: "getRunningActivity()",
    description: "getRunningActivity 当前运行的Activity类名",
    example: "function main() {\n  var result = getRunningActivity();\n}\n\nmain();",
  },
  {
    name: "lockScreen",
    platform: "android",
    category: "device",
    signature: "lockScreen()",
    description: "lockScreen 锁屏",
    example: "function main() {\n  var result = lockScreen();\n  if (result) {\n    toast(\"成功\");\n  } else {\n    toast(\"失败\");\n  }\n}\n\nmain();",
  },
  {
    name: "performGlobalAction",
    platform: "android",
    category: "device",
    signature: "performGlobalAction()",
    description: "performGlobalAction 执行全局的action动作",
    example: "function main() {\n  var result = performGlobalAction(8);\n  if (result) {\n    toast(\"成功\");\n  } else {\n    toast(\"失败\");\n  }\n}\n\nmain();",
  },
  {
    name: "requestNotificationPermission",
    platform: "android",
    category: "device",
    signature: "requestNotificationPermission()",
    description: "requestNotificationPermission 请求监听状态栏的权限",
    example: "function main() {\n  var result = requestNotificationPermission(10);\n  toast(\"是否有权限:\" + result);\n}\n\nmain();",
  },
  {
    name: "hasNotificationPermission",
    platform: "android",
    category: "device",
    signature: "hasNotificationPermission()",
    description: "hasNotificationPermission 是否有状态栏监听权限",
    example: "function main() {\n  var result = hasNotificationPermission();\n  toast(\"是否有权限:\" + result);\n}\n\nmain();",
  },
  {
    name: "getLastNotification",
    platform: "android",
    category: "device",
    signature: "getLastNotification()",
    description: "getLastNotification 获取最近通知栏对象",
    example: "function main() {\n  var result = getLastNotification(\"com.x\", 100);\n  toast(\"结果:\" + result);\n}\n\nmain();",
  },
  {
    name: "shotNotification",
    platform: "android",
    category: "device",
    signature: "shotNotification()",
    description: "shotNotification 通知发射处理",
    example: "function main() {\n  var result = getLastNotification(\"com.x\", 1);\n  if (result && result.length > 0) {\n    var s = shotNotification(result[0].seqId);\n    toast(\"结果:\" + s);\n  }\n}\n\nmain();",
  },
  {
    name: "ignoreNotification",
    platform: "android",
    category: "device",
    signature: "ignoreNotification()",
    description: "ignoreNotification 忽略通知",
    example: "function main() {\n  var result = getLastNotification(\"com.x\", 1);\n  if (result != null && result.length > 0) {\n    var s = ignoreNotification(result[0].seqId);\n    toast(\"结果:\" + s);\n  }\n}\n\nmain();",
  },
  {
    name: "cancelNotification",
    platform: "android",
    category: "device",
    signature: "cancelNotification()",
    description: "cancelNotification 取消通知",
    example: "function main() {\n  var result = getLastNotification(\"com.x\", 1);\n  if (result != null && result.length > 0) {\n    var s = cancelNotification(result[0].seqId);\n    toast(\"结果:\" + s);\n  }\n}\n\nmain();",
  },
  {
    name: "getLastToast",
    platform: "android",
    category: "device",
    signature: "getLastToast()",
    description: "getLastToast 获取toast数据",
    example: "function main() {\n  let result = getLastToast(\"com.xx\", 100);\n  toast(\"结果:\" + JSON.stringify(result));\n  if (result && result.length > 0) {\n    for (let i = 0; i < result.length; i++) {\n      logd(result[i])\n    }\n  }\n}\n\nmain();",
  },
  {
    name: "requestFloatViewPermission",
    platform: "android",
    category: "device",
    signature: "requestFloatViewPermission()",
    description: "requestFloatViewPermission 请求浮窗权限",
    example: "function main() {\n  var result = requestFloatViewPermission(10);\n  toast(\"是否有权限:\" + result);\n}\n\nmain();",
  },
  {
    name: "hasFloatViewPermission",
    platform: "android",
    category: "device",
    signature: "hasFloatViewPermission()",
    description: "hasFloatViewPermission 检查浮窗权限",
    example: "function main() {\n  var result = hasFloatViewPermission();\n  toast(\"是否有权限:\" + result);\n}\n\nmain();",
  },
  {
    name: "showFloatView",
    platform: "android",
    category: "device",
    signature: "showFloatView()",
    description: "showFloatView 展示浮窗",
    example: "function main() {\n  var m = {\n    \"path\": \"main.html\",\n    \"tag\": \"tag\",\n    \"title\": \"sss\",\n    \"titleBg\": \"#888888\",\n    \"x\": 10,\n    \"y\": 10,\n    \"w\": 100,\n    \"h\": 200\n  };\n  var xd = showFloatView(m);\n  logd(\"showFloatView \" + xd);\n}\n\nmain();",
  },
  {
    name: "closeFloatView",
    platform: "android",
    category: "device",
    signature: "closeFloatView()",
    description: "closeFloatView 关闭浮窗",
    example: "function main() {\n  var m = {\n    \"path\": \"main.html\",\n    \"tag\": \"tag\",\n    \"title\": \"sss\",\n    \"titleBg\": \"#888888\",\n    \"x\": 10,\n    \"y\": 10,\n    \"w\": 100,\n    \"h\": 200\n  };\n  var xd = showFloatView(m);\n  logd(\"showFloatView \" + xd);\n  sleep(3000);\n  closeFloatView(\"tag\");\n}\n\nmain();",
  },
  {
    name: "closeAllFloatView",
    platform: "android",
    category: "device",
    signature: "closeAllFloatView()",
    description: "closeAllFloatView 关闭所有悬浮窗",
    example: "function main() {\n  var m = {\n    \"path\": \"main.html\",\n    \"tag\": \"tag\",\n    \"title\": \"sss\",\n    \"titleBg\": \"#888888\",\n    \"x\": 10,\n    \"y\": 10,\n    \"w\": 100,\n    \"h\": 200\n  };\n  var xd = showFloatView(m);\n  logd(\"showFloatView \" + xd);\n  sleep(3000);\n  closeAllFloatView();\n}\n\nmain();",
  },
  {
    name: "showScriptCtrlFloatView",
    platform: "android",
    category: "device",
    signature: "showScriptCtrlFloatView()",
    description: "showScriptCtrlFloatView 显示脚本暂停控制悬浮窗",
    example: "function main() {\n  showScriptCtrlFloatView();\n}\n\nmain();",
  },
  {
    name: "closeScriptCtrlFloatView",
    platform: "android",
    category: "device",
    signature: "closeScriptCtrlFloatView()",
    description: "closeScriptCtrlFloatView 关闭脚本暂停控制悬浮窗",
    example: "function main() {\n  closeScriptCtrlFloatView();\n}\n\nmain();",
  },
  {
    name: "showCtrlWindow",
    platform: "android",
    category: "device",
    signature: "showCtrlWindow()",
    description: "showCtrlWindow 展示启停浮窗",
    example: "function main() {\n  var result = showCtrlWindow();\n  toast(\"是否展示:\" + result);\n}\n\nmain();",
  },
  {
    name: "closeCtrlWindow",
    platform: "android",
    category: "device",
    signature: "closeCtrlWindow()",
    description: "closeCtrlWindow 关闭启停浮窗",
    example: "function main() {\n  var result = closeCtrlWindow();\n  toast(\"是否展示:\" + result);\n}\n\nmain();",
  },
  {
    name: "setCtrlViewSizeEx",
    platform: "android",
    category: "device",
    signature: "setCtrlViewSizeEx()",
    description: "setCtrlViewSizeEx 设置启停控制窗口",
    example: "{\n  \"x\": 100,\n  \"y\": 100,\n  \"backgroundColor\": \"#ffffff\"\n}\n//解释：\n//  x: 起始X位置\n//  y: 起始Y位置",
  },
  {
    name: "showLogWindow",
    platform: "android",
    category: "device",
    signature: "showLogWindow()",
    description: "showLogWindow 展示日志浮窗",
    example: "function main() {\n  var result = showLogWindow();\n  toast(\"是否展示:\" + result);\n}\n\nmain();",
  },
  {
    name: "closeLogWindow",
    platform: "android",
    category: "device",
    signature: "closeLogWindow()",
    description: "closeLogWindow 关闭日志浮窗",
    example: "function main() {\n  closeLogWindow();\n}\n\nmain();",
  },
  {
    name: "setLogViewSizeEx",
    platform: "android",
    category: "device",
    signature: "setLogViewSizeEx()",
    description: "setLogViewSizeEx 设置日志窗口",
    example: "{\n  \"x\": 100,\n  \"y\": 100,\n  \"w\": 100,\n  \"h\": 200,\n  \"textSize\": 12,\n  \"backgroundColor\": \"#ffffff\",\n  \"backgroundImg\": \"res/a.png\",\n  \"title\": \"我是日志\",\n  \"backgroundAlpha\": 255,\n  \"showTitle\": true,\n  \"canTouch\": false,\n  \"gravity\": \"center\",\n  \"showCloseBtn\": true\n}",
  },
  {
    name: "setLogFixedViewEx",
    platform: "android",
    category: "device",
    signature: "setLogFixedViewEx()",
    description: "setLogFixedViewEx 设置日志固定栏目属性",
    example: "function main() {\n  requestFloatViewPermission(1000);\n  var m = {\n    \"show\": true,\n    \"h\": 150,\n    \"textSize\": 12,\n    \"textColor\": \"#000000\",\n    \"backgroundColor\": \"#ffffff\"\n  }\n  showLogWindow();\n  sleep(1000)\n  setLogFixedViewEx(m);\n  setFixedViewText(\"TEST\\n都是对的\")\n\n  sleep(5000);\n\n}\n\nmain();",
  },
  {
    name: "setFixedViewText",
    platform: "android",
    category: "device",
    signature: "setFixedViewText()",
    description: "setFixedViewText 展示固定消息",
    example: "function main() {\n  requestFloatViewPermission(1000);\n  var m = {\n    \"show\": true,\n    \"h\": -1,\n    \"textSize\": 12,\n    \"backgroundColor\": \"#ffffff\"\n  }\n  showLogWindow();\n  sleep(1000)\n  setLogFixedViewEx(m);\n  sleep(1000);\n  setFixedViewText(\"TEST\")\n}\n\nmain();",
  },
  {
    name: "setLogText",
    platform: "android",
    category: "device",
    signature: "setLogText()",
    description: "setLogText 展示消息",
    example: "function main() {\n  var result = setLogText(\"开始运行...\", \"#ffffff\", 18);\n}\n\nmain();",
  },
  {
    name: "expandLogView",
    platform: "android",
    category: "device",
    signature: "expandLogView()",
    description: "expandLogView 展开日志悬浮窗",
    example: "function main() {\n  showLogWindow();\n  sleep(1000)\n  var result = expandLogView();\n}\n\nmain();",
  },
  {
    name: "collapseLogView",
    platform: "android",
    category: "device",
    signature: "collapseLogView()",
    description: "collapseLogView 折叠日志悬浮窗",
    example: "function main() {\n  showLogWindow();\n  sleep(1000)\n  var result = collapseLogView();\n}\n\nmain();",
  },
  {
    name: "startJob",
    platform: "android",
    category: "device",
    signature: "startJob()",
    description: "startJob 开启定时",
    example: "function main() {\n  var time = \"2020-04-17 09:00:00\";\n  //使用日期开启一个任务\n  var id = startJob(\"task1\", time, true);\n  logd(\"job id \" + id);\n  //使用秒数开启，60秒后执行一个任务\n  var id2 = startJob(\"task2\", \"60\", true);\n  logd(\"job id \" + id2);\n}\n\nmain();",
  },
  {
    name: "cancelAllJob",
    platform: "android",
    category: "device",
    signature: "cancelAllJob()",
    description: "cancelAllJob 取消所有定时",
    example: "function main() {\n  var result = cancelAllJob();\n  logd(result);\n}\n\nmain();",
  },
  {
    name: "cancelJob",
    platform: "android",
    category: "device",
    signature: "cancelJob()",
    description: "cancelJob 取消指定TAG定时",
    example: "function main() {\n\n  var result = cancelJob(\"task1\");\n  logd(result);\n}\n\nmain();",
  },
  {
    name: "getAllJobTag",
    platform: "android",
    category: "device",
    signature: "getAllJobTag()",
    description: "getAllJobTag 获取所有定时标签",
    example: "function main() {\n  var result = getAllJobTag();\n  logd(result);\n}\n\nmain();",
  },
  {
    name: "tryGetProjectionPermission",
    platform: "android",
    category: "device",
    signature: "tryGetProjectionPermission()",
    description: "tryGetProjectionPermission 获取截图自允许权限",
    example: "function main() {\n  let result = tryGetProjectionPermission()\n  logd(result);\n  // 接下来就可以进行申请截图权限了\n}\n\nmain();",
  },
  {
    name: "tryGetAccStartupPermission",
    platform: "android",
    category: "device",
    signature: "tryGetAccStartupPermission()",
    description: "tryGetAccStartupPermission 获取无障碍自允许权限",
    example: "function main() {\n  let result = tryGetAccStartupPermission()\n  logd(result);\n  // 接下来就可以进行\u0000无障碍自动启动\n  startEnv();\n}\n\nmain();",
  },
  {
    name: "requestRuntimePermission",
    platform: "android",
    category: "device",
    signature: "requestRuntimePermission()",
    description: "requestRuntimePermission 申请动态权限",
    example: "function main() {\n  let per = [\n    \"android.permission.READ_CALENDAR\",\n    \"android.permission.READ_SMS\"\n  ]\n  let result = requestRuntimePermission(per, 10000)\n  logd(result);\n}\n\nmain();",
  },
  {
    name: "random",
    platform: "android",
    category: "device",
    signature: "random()",
    description: "random 随机函数",
    example: "function main() {\n  var result = random(100, 1000);\n  sleep(result);\n}\n\nmain();",
  },
  {
    name: "isReleaseIec",
    platform: "android",
    category: "device",
    signature: "isReleaseIec()",
    description: "function main() {  var result = isReleaseIec();  logd(result)}main();上一页全局模块下一页选择器&节点说明代理节点设置setAgentSupportNode 设置代理模式下获取节点方式setShellDumpCompressed 代理模式dump压缩模式手势事件模式setGestureActionMode 设置手势模式事件点击函数clickPoint 坐标点击click 选择器点击clickEx 无指针点击longClickEx 无指针长点击clickRandom 选择器随机点击clickRandomEx 无指针随机点击...",
    example: "function main() {\n  var result = isReleaseIec();\n  logd(result)\n}\n\nmain();",
  },
  // ===================== image =====================
  {
    name: "useOpencvMat",
    platform: "android",
    category: "image",
    signature: "image.useOpencvMat()",
    description: "image.useOpencvMat 切换图片存储",
    example: "function main() {\n  let r = image.useOpencvMat(1);\n  logd(r)\n  //剩下的代码和之前一样 找色 找图等操作\n}\n\nmain();",
  },
  {
    name: "setInitParam",
    platform: "android",
    category: "image",
    signature: "image.setInitParam()",
    description: "image.setInitParam 初始化参数",
    example: "function main() {\n  //action_timeout 找图找色动作的最大时间，超时后会自动返回避免阻塞\n  // auto_click_request_dialog 是否自动点击截屏授权对话框，默认是true，自动点击\n  // image.setInitParam({\"action_timeout\":1000});\n  image.setInitParam(\n    {\n      \"action_timeout\": 20000,\n      \"auto_click_request_dialog\": false\n    }\n  );\n}\n\nmain();",
  },
  {
    name: "setFindColorImageMode",
    platform: "android",
    category: "image",
    signature: "image.setFindColorImageMode()",
    description: "image.setFindColorImageMode 设置找色找图的算法模式",
    example: "function main() {\n\n  image.setFindColorImageMode(2);\n\n}\n\nmain();",
  },
  {
    name: "requestScreenCapture",
    platform: "android",
    category: "image",
    signature: "image.requestScreenCapture()",
    description: "image.requestScreenCapture 申请截图权限",
    example: "function main() {\n  logd(\"isServiceOk \" + isServiceOk());\n  startEnv()\n  logd(\"isServiceOk \" + isServiceOk());\n  let request = image.requestScreenCapture(10000, 0);\n  if (!request) {\n    request = image.requestScreenCapture(10000, 0);\n  }\n  logd(\"申请截图结果... \" + request)\n  if (!request) {\n    loge(\"申请截图权限失败,检查是否开启后台弹出,悬浮框等权限\")\n    exit()\n  }\n  //申请完权限至少等1s(垃圾设备多加点)再截图,否则会截不到图\n  sleep(1000)\n}\n\nmain();",
  },
  {
    name: "releaseScreenCapture",
    platform: "android",
    category: "image",
    signature: "image.releaseScreenCapture()",
    description: "image.releaseScreenCapture 释放截屏请求",
    example: "function main() {\n  image.releaseScreenCapture();\n}\n\nmain();",
  },
  {
    name: "captureScreen",
    platform: "android",
    category: "image",
    signature: "image.captureScreen()",
    description: "image.captureScreen 截取屏幕Image对象",
    example: "function main() {\n  logd(\"isServiceOk \" + isServiceOk());\n  startEnv()\n  logd(\"isServiceOk \" + isServiceOk());\n\n  let request = image.requestScreenCapture(10000, 0);\n  if (!request) {\n    request = image.requestScreenCapture(10000, 0);\n  }\n  logd(\"申请截图结果... \" + request)\n  if (!request) {\n    loge(\"申请截图权限\u0000\u0000失败,检查是否开启后台弹出,悬浮框等权限\")\n    exit()\n  }\n  //申请完权限至少等1s(垃圾设备多加点)再截图,否则会截不到图\n  sleep(1000)\n  for (let i = 0; i < 10; i++) {\n    let cap = image.captureScreen(3, 0, 0, 300, 400)\n    logd(\"截图数据: \" + cap)\n    sleep(1000)\n    //图片要回收\n    image.recycle(cap)\n  }\n}\n\nmain();",
  },
  {
    name: "captureFullScreen",
    platform: "android",
    category: "image",
    signature: "image.captureFullScreen()",
    description: "image.captureFullScreen 截取全屏Image对象",
    example: "function main() {\n  logd(\"isServiceOk \" + isServiceOk());\n  startEnv()\n  logd(\"isServiceOk \" + isServiceOk());\n\n  let request = image.requestScreenCapture(10000, 0);\n\n  if (!request) {\n    request = image.requestScreenCapture(10000, 0);\n  }\n  logd(\"申请截图结果... \" + request)\n  if (!request) {\n    loge(\"申请截图权限失败,检查是否开启后台弹出,悬浮框等权限\")\n    exit()\n  }\n  //申请完权限至少等1s(垃圾设备多加点)再截图,否则会截不到图\n  sleep(1000)\n  for (let i = 0; i < 10; i++) {\n    let cap = image.captureFullScreen()\n    logd(\"截图数据: \" + cap)\n    sleep(1000)\n    //图片要回收\n    image.recycle(cap)\n  }\n}\n\nmain();",
  },
  {
    name: "captureFullScreenEx",
    platform: "android",
    category: "image",
    signature: "image.captureFullScreenEx()",
    description: "image.captureFullScreenEx 截取全屏屏Image对象扩展",
    example: "function main() {\n  logd(\"isServiceOk \" + isServiceOk());\n  startEnv()\n  logd(\"isServiceOk \" + isServiceOk());\n\n  let request = image.requestScreenCapture(10000, 0);\n\n  if (!request) {\n    request = image.requestScreenCapture(10000, 0);\n  }\n  logd(\"申请截图结果... \" + request)\n  if (!request) {\n    loge(\"申请截图权限失败,检查是否开启后台弹出,悬浮框等权限\")\n    exit()\n  }\n  //申请完权限至少等1s(垃圾设备多加点)再截图,否则会截不到图\n  sleep(1000)\n  for (let i = 0; i < 10; i++) {\n    let cap = image.captureFullScreenEx()\n    logd(\"截图数据: \" + cap)\n    sleep(1000)\n    //图片要回收\n    image.recycle(cap)\n  }\n}\n\nmain();",
  },
  {
    name: "captureScreenBitmap",
    platform: "android",
    category: "image",
    signature: "image.captureScreenBitmap()",
    description: "image.captureScreenBitmap 截取屏幕Bitmap对象",
    example: "function main() {\n  logd(\"isServiceOk \" + isServiceOk());\n  startEnv()\n  logd(\"isServiceOk \" + isServiceOk());\n\n  let request = image.requestScreenCapture(10000, 0);\n\n  if (!request) {\n    request = image.requestScreenCapture(10000, 0);\n  }\n  logd(\"申请截图结果... \" + request)\n  if (!request) {\n    loge(\"申请截图权限失败,检查是否开启后台弹出,悬浮框等权限\")\n    exit()\n  }\n  //申请完权限至少等1s(垃圾设备多加点)再截图,否则会截不到图\n  sleep(1000)\n  for (let i = 0; i < 10; i++) {\n    let cap = image.captureScreenBitmap(\"jpg\", 100, 100, 200, 300, 100);\n    logd(\"截图数据: \" + cap)\n    sleep(1000)\n    //图片要回收\n    image.recycle(cap)\n  }\n}\n\nmain();",
  },
  {
    name: "captureScreenBitmapEx",
    platform: "android",
    category: "image",
    signature: "image.captureScreenBitmapEx()",
    description: "image.captureScreenBitmapEx 截取Bitmap对象扩展",
    example: "function main() {\n  logd(\"isServiceOk \" + isServiceOk());\n  startEnv()\n  logd(\"isServiceOk \" + isServiceOk());\n\n  let request = image.requestScreenCapture(10000, 0);\n\n  if (!request) {\n    request = image.requestScreenCapture(10000, 0);\n  }\n  logd(\"申请截图结果... \" + request)\n  if (!request) {\n    loge(\"申请截图权限失败,检查是否开启后台弹出,悬浮框等权限\")\n    exit()\n  }\n  //申请完权限至少等1s(垃圾设备多加点)再截图,否则会截不到图\n  sleep(1000)\n  for (let i = 0; i < 10; i++) {\n    let cap = image.captureScreenBitmapEx();\n    logd(\"截图数据: \" + cap)\n    sleep(1000)\n    //图片要回收\n    image.recycle(cap)\n  }\n}\n\nmain();",
  },
  {
    name: "captureToFile",
    platform: "android",
    category: "image",
    signature: "image.captureToFile()",
    description: "image.captureToFile 截取屏幕到文件",
    example: "function main() {\n  logd(\"isServiceOk \" + isServiceOk());\n  startEnv()\n  logd(\"isServiceOk \" + isServiceOk());\n\n  let request = image.requestScreenCapture(10000, 0);\n\n  if (!request) {\n    request = image.requestScreenCapture(10000, 0);\n  }\n  logd(\"申请截图结果... \" + request)\n  if (!request) {\n    loge(\"申请截图权限失败,检查是否开启后台弹出,悬浮框等权限\")\n    exit()\n  }\n  home();\n  //申请完权限至少等1s(垃圾设备多加点)再截图,否则会截不到图\n  sleep(1000)\n  for (let i = 0; i < 10; i++) {\n    let cap = image.captureToFile(3, 0, 0, 300, 400, \"/sdcard/a\" + i + \".png\");\n    logd(\"是否成功: \" + cap)\n    sleep(1000)\n  }\n}\n\nmain();",
  },
  {
    name: "screencapImage",
    platform: "android",
    category: "image",
    signature: "image.screencapImage()",
    description: "image.screencapImage screencap命令截图",
    example: "function main() {\n  startEnv()\n  for (let i = 0; i < 10; i++) {\n    sleep(1000);\n    let d = image.screencapImage(false);\n    logd(d);\n    if (d) {\n      let r = image.saveTo(d, \"/sdcard/data/a\" + i + \".png\");\n      logd(r);\n      image.recycle(d)\n\n      logd(\"isRecycled \" + image.isRecycled(d));\n    }\n\n  }\n\n}\n\nmain()",
  },
  {
    name: "screencapBitmap",
    platform: "android",
    category: "image",
    signature: "image.screencapBitmap()",
    description: "image.screencapBitmap screencap命令截图Bitmap",
    example: "function main() {\n  startEnv()\n  for (let i = 0; i < 10; i++) {\n    sleep(1000);\n    let d = image.screencapBitmap(false);\n    logd(d);\n    if (d) {\n      let r = image.saveBitmap(d, \"png\", 100, \"/sdcard/data/b\" + i + \".png\");\n      logd(r);\n\n      image.recycle(d)\n\n      logd(\"isRecycled \" + image.isRecycled(d));\n\n    }\n  }\n}\n\nmain()",
  },
  {
    name: "cmpColor",
    platform: "android",
    category: "image",
    signature: "image.cmpColor()",
    description: "image.cmpColor 多点比色",
    example: "function main() {\n  let req = image.requestScreenCapture(10000, 0);\n  if (!req) {\n    req = image.requestScreenCapture(10000, 0);\n  }\n  if (!req) {\n    toast(\"申请权限失败\");\n    return;\n  }\n  //申请完权限至少等1s(垃圾设备多加点)再截图,否则会截不到图\n  sleep(1000)\n  let aimage = image.captureFullScreen();\n  if (aimage != null) {\n    let points3 = \"205|1130|0xff944b-0x101010,211|1158|0xff8e42,191|1175|0xfcfbf7\";\n    let points = image.cmpColor(aimage, points3, 0.9, 0, 0, 0, 0);\n    logd(\"points \" + points);\n    //图片要回收\n    image.recycle(aimage)\n  }\n\n}\n\nmain();",
  },
  {
    name: "cmpColorJ",
    platform: "android",
    category: "image",
    signature: "image.cmpColorJ()",
    description: "image.cmpColorJ 多点比色(JSON)",
    example: "function main() {\n  let req = image.requestScreenCapture(10000, 0);\n  if (!req) {\n    req = image.requestScreenCapture(10000, 0);\n  }\n  if (!req) {\n    toast(\"申请权限失败\");\n    return;\n  }\n  //申请完权限至少等1s(垃圾设备多加点)再截图,否则会截不到图\n  sleep(1000)\n  let aimage = image.captureFullScreen();\n  if (aimage != null) {\n    let points = image.cmpColorJ(aimage, \"a.json\");\n    logd(\"points \" + points);\n    //图片要回收\n    image.recycle(aimage)\n  }\n}\n\nmain();",
  },
  {
    name: "cmpColorEx",
    platform: "android",
    category: "image",
    signature: "image.cmpColorEx()",
    description: "image.cmpColorEx 多点比色扩展",
    example: "function main() {\n  let req = image.requestScreenCapture(10000, 0);\n  if (!req) {\n    req = image.requestScreenCapture(10000, 0);\n  }\n  if (!req) {\n    toast(\"申请权限失败\");\n    return;\n  }\n  //申请完权限至少等1s(垃圾设备多加点)再截图,否则会截不到图\n  sleep(1000)\n  let points3 = \"205|1130|0xff944b-0x101010,211|1158|0xff8e42,191|1175|0xfcfbf7\";\n  let points = image.cmpColorEx(points3, 0.9, 0, 0, 0, 0);\n  logd(\"points \" + points);\n\n}\n\nmain();",
  },
  {
    name: "cmpMultiColor",
    platform: "android",
    category: "image",
    signature: "image.cmpMultiColor()",
    description: "image.cmpMultiColor 多组比色",
    example: "function main() {\n  let req = image.requestScreenCapture(10000, 0);\n  if (!req) {\n    req = image.requestScreenCapture(10000, 0);\n  }\n  if (!req) {\n    toast(\"申请权限失败\");\n    return;\n  }\n  //申请完权限至少等1s(垃圾设备多加点)再截图,否则会截不到图\n  sleep(1000)\n  let aimage = image.captureFullScreen();\n  if (aimage != null) {\n    let points1 = \"205|112230|0xff944b,211|1158|0xff8e42,191|1175|0xfcfbf7\";\n    let points2 = \"205|113022|0xff944b,211|1158|0xff8e42,191|1175|0xfcfbf7\";\n    let points3 = \"205|1130|0xff944b,211|1158|0xff8e42,191|1175|0xfcfbf7\";\n    let points = image.cmpMultiColor(aimage, [points1, points2, points3], 0.9, 0, 0, 0, 0);\n    logd(\"points \" + points);\n    //图片要回收\n    image.recycle(aimage)\n  }\n}\n\nmain();",
  },
  {
    name: "cmpMultiColorJ",
    platform: "android",
    category: "image",
    signature: "image.cmpMultiColorJ()",
    description: "image.cmpMultiColorJ 多组比色(JSON)",
    example: "function main() {\n  let req = image.requestScreenCapture(10000, 0);\n  if (!req) {\n    req = image.requestScreenCapture(10000, 0);\n  }\n  if (!req) {\n    toast(\"申请权限失败\");\n    return;\n  }\n  //申请完权限至少等1s(垃圾设备多加点)再截图,否则会截不到图\n  sleep(1000)\n  let aimage = image.captureFullScreen();\n  if (aimage != null) {\n    let points = image.cmpMultiColorJ(aimage, \"a.json\");\n    logd(\"points \" + points);\n    //图片要回收\n    image.recycle(aimage)\n  }\n}\n\nmain();",
  },
  {
    name: "cmpMultiColorEx",
    platform: "android",
    category: "image",
    signature: "image.cmpMultiColorEx()",
    description: "image.cmpMultiColorEx 多组比色扩展",
    example: "function main() {\n  let req = image.requestScreenCapture(10000, 0);\n  if (!req) {\n    req = image.requestScreenCapture(10000, 0);\n  }\n  if (!req) {\n    toast(\"申请权限失败\");\n    return;\n  }\n  //申请完权限至少等1s(垃圾设备多加点)再截图,否则会截不到图\n  sleep(1000)\n  let points1 = \"205|112230|0xff944b,211|1158|0xff8e42,191|1175|0xfcfbf7\";\n  let points2 = \"205|113022|0xff944b,211|1158|0xff8e42,191|1175|0xfcfbf7\";\n  let points3 = \"205|1130|0xff944b,211|1158|0xff8e42,191|1175|0xfcfbf7\";\n  let points = image.cmpMultiColorEx([points1, points2, points3], 0.9, 0, 0, 0, 0);\n  logd(\"points \" + points);\n}\n\nmain();",
  },
  {
    name: "findColor",
    platform: "android",
    category: "image",
    signature: "image.findColor()",
    description: "image.findColor 单点找色",
    example: "function main() {\n  let req = image.requestScreenCapture(10000, 0);\n  if (!req) {\n    req = image.requestScreenCapture(10000, 0);\n  }\n  if (!req) {\n    toast(\"申请权限失败\");\n    return;\n  }\n  //申请完权限至少等1s(垃圾设备多加点)再截图,否则会截不到图\n  sleep(1000)\n  let aimage = image.captureFullScreen();\n  if (aimage != null) {\n    let points = image.findColor(aimage, \"0xCDD7E9-0x101010,0xCDD7E9-0x101010\", 0.9, 0, 0, 0, 0, 10, 1);\n    logd(\"points \" + JSON.stringify(points));\n    //这玩意是个数组\n    if (points) {\n      for (let i = 0; i < points.length; i++) {\n        logd(JSON.stringify(points[i]), points[i].x, points[i].y)\n        //点击坐标\n        clickPoint(points[i].x, points[i].y)\n      }\n    }\n    //图片要回收\n    image.recycle(aimage)\n  }\n\n}\n\nmain();",
  },
  {
    name: "findColorJ",
    platform: "android",
    category: "image",
    signature: "image.findColorJ()",
    description: "image.findColorJ 单点找色(JSON)",
    example: "function main() {\n  let req = image.requestScreenCapture(10000, 0);\n  if (!req) {\n    req = image.requestScreenCapture(10000, 0);\n  }\n  if (!req) {\n    toast(\"申请权限失败\");\n    return;\n  }\n  //申请完权限至少等1s(垃圾设备多加点)再截图,否则会截不到图\n  sleep(1000)\n  let aimage = image.captureFullScreen();\n  if (aimage != null) {\n    let points = image.findColorJ(aimage, \"金币.json\");\n    logd(\"points \" + JSON.stringify(points));\n    //这玩意是个数组\n    if (points && points.length > 0) {\n      for (let i = 0; i < points.length; i++) {\n        logd(JSON.stringify(points[i]), points[i].x, points[i].y)\n        //点击坐标\n        clickPoint(points[i].x, points[i].y)\n      }\n    }\n    //图片要回收\n    image.recycle(aimage)\n  }\n\n}\n\nmain();",
  },
  {
    name: "findColorEx",
    platform: "android",
    category: "image",
    signature: "image.findColorEx()",
    description: "image.findColorEx 自动截屏单点找色",
    example: "function main() {\n  let req = image.requestScreenCapture(10000, 0);\n  if (!req) {\n    req = image.requestScreenCapture(10000, 0);\n  }\n  if (!req) {\n    toast(\"申请权限失败\");\n    return;\n  }\n  //申请完权限至少等1s(垃圾设备多加点)再截图,否则会截不到图\n  sleep(1000)\n  let points = image.findColorEx(\"0xCDD7E9-0x101010,0xCDD7E9-0x101010\", 0.9, 0, 0, 0, 0, 10, 1);\n  logd(\"points \" + JSON.stringify(points));\n  //这玩意是个数组\n  if (points && points.length > 0) {\n    for (let i = 0; i < points.length; i++) {\n      logd(JSON.stringify(points[i]), points[i].x, points[i].y)\n      //点击坐标\n      clickPoint(points[i].x, points[i].y)\n    }\n  }\n\n}\n\nmain();",
  },
  {
    name: "findColorExJ",
    platform: "android",
    category: "image",
    signature: "image.findColorExJ()",
    description: "image.findColorExJ 自动截屏单点找色(JSON)",
    example: "function main() {\n  let req = image.requestScreenCapture(10000, 0);\n  if (!req) {\n    req = image.requestScreenCapture(10000, 0);\n  }\n  if (!req) {\n    toast(\"申请权限失败\");\n    return;\n  }\n  //申请完权限至少等1s(垃圾设备多加点)再截图,否则会截不到图\n  sleep(1000)\n  let points = image.findColorExJ(\"金币.json\");\n  logd(\"points \" + JSON.stringify(points));\n  //这玩意是个数组\n  if (points && points.length > 0) {\n    for (let i = 0; i < points.length; i++) {\n      logd(points[i], points[i].x, points[i].y)\n      //点击坐标\n      clickPoint(points[i].x, points[i].y)\n    }\n  }\n\n}\n\nmain();",
  },
  {
    name: "findMultiColor",
    platform: "android",
    category: "image",
    signature: "image.findMultiColor()",
    description: "image.findMultiColor 多点找色",
    example: "function main() {\n  let req = image.requestScreenCapture(10000, 0);\n  if (!req) {\n    req = image.requestScreenCapture(10000, 0);\n  }\n  if (!req) {\n    toast(\"申请权限失败\");\n    return;\n  }\n  //申请完权限至少等1s(垃圾设备多加点)再截图,否则会截不到图\n  sleep(1000)\n  let aimage = image.captureFullScreen();\n  if (aimage != null) {\n    let points = image.findMultiColor(aimage, \"0xDD7A5F-0x101010\", \"29|25|0xBB454B-0x101010,58|44|0xA6363A-0x101010\", 0.9, 0, 0, 0, 0, 10, 1);\n    logd(\"points \" + JSON.stringify(points));\n    //这玩意是个数组\n    if (points && points.length > 0) {\n      for (let i = 0; i < points.length; i++) {\n        logd(points[i], points[i].x, points[i].y)\n        //点击坐标\n        clickPoint(points[i].x, points[i].y)\n      }\n    }\n    //图片要回收\n    image.recycle(aimage)\n  }\n\n}\n\nmain();",
  },
  {
    name: "findMultiColorJ",
    platform: "android",
    category: "image",
    signature: "image.findMultiColorJ()",
    description: "image.findMultiColorJ 多点\u0000\u0000找色(JSON)",
    example: "function main() {\n  let req = image.requestScreenCapture(10000, 0);\n  if (!req) {\n    req = image.requestScreenCapture(10000, 0);\n  }\n  if (!req) {\n    toast(\"申请权限失败\");\n    return;\n  }\n  //申请完权限至少等1s(垃圾设备多加点)再截图,否则会截不到图\n  sleep(1000)\n  let aimage = image.captureFullScreen();\n  if (aimage != null) {\n    let points = image.findMultiColorJ(aimage, \"金币.json\");\n    logd(\"points \" + JSON.stringify(points));\n    //这玩意是个数组\n    if (points && points.length > 0) {\n      for (let i = 0; i < points.length; i++) {\n        logd(points[i], points[i].x, points[i].y)\n        //点击坐标\n        clickPoint(points[i].x, points[i].y)\n      }\n    }\n    //图片要回收\n    image.recycle(aimage)\n  }\n\n}\n\nmain();",
  },
  {
    name: "findMultiColorEx",
    platform: "android",
    category: "image",
    signature: "image.findMultiColorEx()",
    description: "image.findMultiColorEx 自动截屏多点找色",
    example: "function main() {\n  let req = image.requestScreenCapture(10000, 0);\n  if (!req) {\n    req = image.requestScreenCapture(10000, 0);\n  }\n  if (!req) {\n    toast(\"申请权限失败\");\n    return;\n  }\n\n  //申请完权限至少等1s(垃圾设备多加点)再截图,否则会截不到图\n  sleep(1000)\n  let points = image.findMultiColorEx(\"0xDD7A5F-0x101010\", \"29|25|0xBB454B-0x101010,58|44|0xA6363A-0x101010\", 0.9, 0, 0, 0, 0, 10, 1);\n  logd(\"points \" + JSON.stringify(points));\n  //这玩意是个数组\n  if (points && points.length > 0) {\n    for (let i = 0; i < points.length; i++) {\n      logd(points[i], points[i].x, points[i].y)\n      //点击坐标\n      clickPoint(points[i].x, points[i].y)\n    }\n  }\n\n}\n\nmain();",
  },
  {
    name: "findMultiColorExJ",
    platform: "android",
    category: "image",
    signature: "image.findMultiColorExJ()",
    description: "image.findMultiColorExJ 自动截屏多点找色(JSON)",
    example: "function main() {\n  let req = image.requestScreenCapture(10000, 0);\n  if (!req) {\n    req = image.requestScreenCapture(10000, 0);\n  }\n  if (!req) {\n    toast(\"申请权限失败\");\n    return;\n  }\n  //申请完权限至少等1s(垃圾设备多加点)再截图,否则会截不到图\n  sleep(1000)\n  let points = image.findMultiColorExJ(\"金币.json\");\n  logd(\"points \" + JSON.stringify(points));\n  //这玩意是个数组\n  if (points && points.length > 0) {\n    for (let i = 0; i < points.length; i++) {\n      logd(points[i], points[i].x, points[i].y)\n      //点击坐标\n      clickPoint(points[i].x, points[i].y)\n    }\n  }\n}\n\nmain();",
  },
  {
    name: "findNotColor",
    platform: "android",
    category: "image",
    signature: "image.findNotColor()",
    description: "image.findNotColor 找非色",
    example: "function main() {\n  let req = image.requestScreenCapture(10000, 0);\n  if (!req) {\n    req = image.requestScreenCapture(10000, 0);\n  }\n  if (!req) {\n    toast(\"申请权限失败\");\n    return;\n  }\n  //申请完权限至少等1s(垃圾设备多加点)再截图,否则会截不到图\n  sleep(1000)\n  let aimage = image.captureFullScreen();\n  if (aimage != null) {\n    let points = image.findNotColor(aimage, \"0xCDD7E9-0x101010,0xCDD7E9-0x101010\", 0.9, 0, 0, 0, 0, 10, 1);\n    logd(\"points \" + JSON.stringify(points));\n    //这玩意是个数组\n    if (points) {\n      for (let i = 0; i < points.length; i++) {\n        logd(JSON.stringify(points[i]), points[i].x, points[i].y)\n        //点击坐标\n        clickPoint(points[i].x, points[i].y)\n      }\n    }\n    //图片要回收\n    image.recycle(aimage)\n  }\n\n}\n\nmain();",
  },
  {
    name: "findNotColorJ",
    platform: "android",
    category: "image",
    signature: "image.findNotColorJ()",
    description: "image.findNotColorJ 找非色(JSON)",
    example: "function main() {\n  let req = image.requestScreenCapture(10000, 0);\n  if (!req) {\n    req = image.requestScreenCapture(10000, 0);\n  }\n  if (!req) {\n    toast(\"申请权限失败\");\n    return;\n  }\n  //申请完权限至少等1s(垃圾设备多加点)再截图,否则会截不到图\n  sleep(1000)\n  let aimage = image.captureFullScreen();\n  if (aimage != null) {\n    let points = image.findNotColorJ(aimage, \"a.json\");\n    logd(\"points \" + JSON.stringify(points));\n    //这玩意是个数组\n    if (points) {\n      for (let i = 0; i < points.length; i++) {\n        logd(JSON.stringify(points[i]), points[i].x, points[i].y)\n        //点击坐标\n        clickPoint(points[i].x, points[i].y)\n      }\n    }\n    //图片要回收\n    image.recycle(aimage)\n  }\n\n}\n\nmain();",
  },
  {
    name: "initOpenCV",
    platform: "android",
    category: "image",
    signature: "image.initOpenCV()",
    description: "image.initOpenCV 初始化OpenCV",
    example: "function main() {\n  let req = startEnv();\n  if (!req) {\n    toast(\"申请权限失败\");\n    return;\n  }\n  sleep(1000)\n  let d = image.initOpenCV();\n  logd(d)\n}\n\nmain();",
  },
  {
    name: "findImageByColor",
    platform: "android",
    category: "image",
    signature: "image.findImageByColor()",
    description: "image.findImageByColor 透明找图",
    example: "function main() {\n  let request = image.requestScreenCapture(10000, 0);\n  if (request) {\n    toast(\"申请成功\");\n  } else {\n    toast(\"申请失败\");\n    exit();\n  }\n\n  //申请完权限至少等1s(垃圾设备多加点)再截图,否则会截不到图\n  sleep(1000)\n  //从工程目录下res文件夹下读取sms.png文件\n  let sms = readResAutoImage(\"sms.png\");\n  //抓取屏幕\n  let aimage = image.captureFullScreen();\n  logd(\"aimage \" + aimage);\n  if (aimage != null) {\n    //在图片中查找\n    let points = image.findImageByColor(aimage, sms, 0, 0, 0, 0, 0.8, 5);\n    logd(\"points \" + JSON.stringify(points));\n    //这玩意是个数组\n    if (points && points.length > 0) {\n      for (let i = 0; i < points.length; i++) {\n        logd(points[i])\n        let x = points[i].x\n        let y = points[i].y\n        //点击坐标\n        clickPoint(x, y)\n      }\n    }\n    //图片要回收\n    image.recycle(aimage)\n  }\n//图片要回收\n  image.recycle(sms)\n}\n\nmain();",
  },
  {
    name: "findImageByColorJ",
    platform: "android",
    category: "image",
    signature: "image.findImageByColorJ()",
    description: "image.findImageByColorJ 透明找图(JSON)",
    example: "function main() {\n  let request = image.requestScreenCapture(10000, 0);\n  if (request) {\n    toast(\"申请成功\");\n  } else {\n    toast(\"申请失败\");\n    exit();\n  }\n  //申请完权限至少等1s(垃圾设备多加点)再截图,否则会截不到图\n  sleep(1000)\n  //抓取屏幕\n  let aimage = image.captureFullScreen();\n  logd(\"aimage \" + aimage);\n  if (aimage != null) {\n    //在图片中查找\n    let points = image.findImageByColorJ(aimage, \"a.json\");\n    logd(\"points \" + JSON.stringify(points));\n    //这玩意是个数组\n    if (points && points.length > 0) {\n      for (let i = 0; i < points.length; i++) {\n        logd(points[i])\n        let x = points[i].x\n        let y = points[i].y\n        //点击坐标\n        clickPoint(x, y)\n      }\n    }\n    image.recycle(aimage)\n  }\n}\n\nmain();",
  },
  {
    name: "findImageByColorEx",
    platform: "android",
    category: "image",
    signature: "image.findImageByColorEx()",
    description: "image.findImageByColorEx 透明找图扩展",
    example: "function main() {\n  let d = startEnv();\n  logd(\"启动服务--{}\", d)\n  let smallTmplate = readResAutoImage(\"tmp4.png\");\n\n  for (let i = 0; i < 100; i++) {\n    sleep(1000)\n    let img = image.captureFullScreen();\n    logd(\"img = {}\", img)\n    if (img == null) {\n      continue\n    }\n    console.time(1)\n    let extra = {\n      \"firstColorOffset\": \"#202020\",\n      \"otherColorOffset\": \"#000000\",\n      \"cmpColorSucThreshold\": 1,\n      \"firstColorThreshold\": \"1\",\n      \"otherColorThreshold\": \"1\",\n      \"startX\": 0,\n      \"startY\": 0\n    }\n    let points = image.findImageByColorEx(img, smallTmplate, 0, 0, 0, 0, 100, extra);\n    logd(\"time-{}\", console.timeEnd(1))\n    //这玩意是个数组\n    if (points) {\n      logd(\"points \" + JSON.stringify(points));\n    }\n\n    image.recycle(img)\n\n  }\n\n  image.recycle(smallTmplate)\n}\n\nmain()",
  },
  {
    name: "findImageByColorExJ",
    platform: "android",
    category: "image",
    signature: "image.findImageByColorExJ()",
    description: "image.findImageByColorExJ 透明找图扩展(JSON)",
    example: "function main() {\n  let request = image.requestScreenCapture(10000, 0);\n  if (request) {\n    toast(\"申请成功\");\n  } else {\n    toast(\"申请失败\");\n    exit();\n  }\n  //申请完权限至少等1s(垃圾设备多加点)再截图,否则会截不到图\n  sleep(1000)\n  //抓取屏幕\n  let aimage = image.captureFullScreen();\n  logd(\"aimage \" + aimage);\n  if (aimage != null) {\n    //在图片中查找\n    let points = image.findImageByColorExJ(aimage, \"a.json\");\n    logd(\"points \" + JSON.stringify(points));\n    //这玩意是个数组\n    if (points && points.length > 0) {\n      for (let i = 0; i < points.length; i++) {\n        logd(points[i])\n        let x = points[i].x\n        let y = points[i].y\n        //点击坐标\n        clickPoint(x, y)\n      }\n    }\n    image.recycle(aimage)\n  }\n}\n\nmain();",
  },
  {
    name: "findImage",
    platform: "android",
    category: "image",
    signature: "image.findImage()",
    description: "image.findImage 找图",
    example: "function main() {\n  let request = image.requestScreenCapture(10000, 0);\n  if (request) {\n    toast(\"申请成功\");\n  } else {\n    toast(\"申请失败\");\n    exit();\n  }\n  let d = image.initOpenCV();\n  logd(d)\n  //申请完权限至少等1s(垃圾设备多加点)再截图,否则会截不到图\n  sleep(1000)\n  //从工程目录下res文件夹下读取sms.png文件\n  let sms = readResAutoImage(\"sms.png\");\n  //抓取屏幕\n  let aimage = image.captureFullScreen();\n  logd(\"aimage \" + aimage);\n  if (aimage != null) {\n    //在图片中查找\n    let points = image.findImage(aimage, sms, 0, 0, 0, 0, 0.7, 0.9, 21, 5);\n    logd(\"points \" + JSON.stringify(points));\n    //这玩意是个数组\n    if (points && points.length > 0) {\n      for (let i = 0; i < points.length; i++) {\n        logd(points[i])\n        logd(\"相似度: \" + points[i]['similarity'])\n        let x = parseInt((points[i].left + points[i].right) / 2)\n        let y = parseInt((points[i].top + points[i].bottom) / 2)\n        //点击坐标\n        clickPoint(x, y)\n      }\n    }\n    //图片要回收\n    image.recycle(aimage)\n  }\n  //图片要回收\n  image.recycle(sms)\n}\n\nmain();",
  },
  {
    name: "findImageJ",
    platform: "android",
    category: "image",
    signature: "image.findImageJ()",
    description: "image.findImageJ 找图(JSON)",
    example: "function main() {\n  let request = image.requestScreenCapture(10000, 0);\n  if (request) {\n    toast(\"申请成功\");\n  } else {\n    toast(\"申请失败\");\n    exit();\n  }\n  let d = image.initOpenCV();\n  logd(d)\n  //申请完权限至少等1s(垃圾设备多加点)再截图,否则会截不到图\n  sleep(1000)\n  //抓取屏幕\n  let aimage = image.captureFullScreen();\n  logd(\"aimage \" + aimage);\n  if (aimage != null) {\n    //在图片中查找\n    let points = image.findImageJ(aimage, \"a.json\");\n    logd(\"points \" + JSON.stringify(points));\n    //这玩意是个数组\n    if (points && points.length > 0) {\n      for (let i = 0; i < points.length; i++) {\n        logd(points[i])\n        logd(\"相似度: \" + points[i]['similarity'])\n        let x = parseInt((points[i].left + points[i].right) / 2)\n        let y = parseInt((points[i].top + points[i].bottom) / 2)\n        //点击坐标\n        clickPoint(x, y)\n      }\n    }\n    //图片要回收\n    image.recycle(aimage)\n  }\n}\n\nmain();",
  },
  {
    name: "findImage2",
    platform: "android",
    category: "image",
    signature: "image.findImage2()",
    description: "image.findImage2 找图(含缩放)",
    example: "function main() {\n  let request = image.requestScreenCapture(10000, 0);\n  if (request) {\n    toast(\"申请成功\");\n  } else {\n    toast(\"申请失败\");\n    exit();\n  }\n  let d = image.initOpenCV();\n  logd(d)\n  //申请完权限至少等1s(垃圾设备多加点)再截图,否则会截不到图\n  sleep(1000)\n  //从工程目录下res文件夹下读取sms.png文件\n  let sms = readResAutoImage(\"sms.png\");\n  //抓取屏幕\n  let aimage = image.captureFullScreen();\n  logd(\"aimage \" + aimage);\n  if (aimage != null) {\n    //在图片中查找\n    let points = image.findImage2(aimage, sms, 0, 0, 0, 0, 0.7, 0.9, 21, 5);\n    logd(\"points \" + JSON.stringify(points));\n    //这玩意是个数组\n    if (points && points.length > 0) {\n      for (let i = 0; i < points.length; i++) {\n        logd(points[i])\n        logd(\"相似度: \" + points[i]['similarity'])\n        let x = parseInt((points[i].left + points[i].right) / 2)\n        let y = parseInt((points[i].top + points[i].bottom) / 2)\n        //点击坐标\n        clickPoint(x, y)\n      }\n    }\n    //图片要回收\n    image.recycle(aimage)\n  }\n  //图片要回收\n  image.recycle(sms)\n}\n\nmain();",
  },
  {
    name: "findImage2J",
    platform: "android",
    category: "image",
    signature: "image.findImage2J()",
    description: "image.findImage2J 找图(JSON)",
    example: "function main() {\n  let request = image.requestScreenCapture(10000, 0);\n  if (request) {\n    toast(\"申请成功\");\n  } else {\n    toast(\"申请失败\");\n    exit();\n  }\n  let d = image.initOpenCV();\n  logd(d)\n  //申请完权限至少等1s(垃圾设备多加点)再截图,否则会截不到图\n  sleep(1000)\n  //抓取屏幕\n  let aimage = image.captureFullScreen();\n  logd(\"aimage \" + aimage);\n  if (aimage != null) {\n    //在图片中查找\n    let points = image.findImage2J(aimage, \"a.json\");\n    logd(\"points \" + JSON.stringify(points));\n    //这玩意是个数组\n    if (points && points.length > 0) {\n      for (let i = 0; i < points.length; i++) {\n        logd(points[i])\n        logd(\"相似度: \" + points[i]['similarity'])\n        let x = parseInt((points[i].left + points[i].right) / 2)\n        let y = parseInt((points[i].top + points[i].bottom) / 2)\n        //点击坐标\n        clickPoint(x, y)\n      }\n    }\n    image.recycle(aimage)\n  }\n}\n\nmain();",
  },
  {
    name: "findImageEx",
    platform: "android",
    category: "image",
    signature: "image.findImageEx()",
    description: "image.findImageEx 自动截屏找图",
    example: "function main() {\n  let request = image.requestScreenCapture(10000, 0);\n  if (request) {\n    toast(\"申请成功\");\n  } else {\n    toast(\"申请失败\");\n    exit();\n  }\n  let d = image.initOpenCV();\n  logd(d)\n  //申请完权限至少等1s(垃圾设备多加点)再截图,否则会截不到图\n  sleep(1000)\n  //从工程目录下res文件夹下读取sms.png文件\n  let sms = readResAutoImage(\"sms.png\");\n  //在当前屏幕中查找，并且限制只查找一个\n  let points = image.findImageEx(sms, 0, 0, 0, 0, 0.7, 0.9, 21, 5);\n  logd(\"points \" + JSON.stringify(points));\n  //这玩意是个数组\n  if (points && points.length > 0) {\n    for (let i = 0; i < points.length; i++) {\n      logd(points[i])\n      let x = parseInt((points[i].left + points[i].right) / 2)\n      let y = parseInt((points[i].top + points[i].bottom) / 2)\n      //点击坐标\n      clickPoint(x, y)\n    }\n  }\n  //图片要回收\n  image.recycle(sms)\n}\n\nmain();",
  },
  {
    name: "matchTemplate",
    platform: "android",
    category: "image",
    signature: "image.matchTemplate()",
    description: "image.matchTemplate 图片模板匹配",
    example: "function main() {\n  let req = image.requestScreenCapture(10000, 0);\n  if (!req) {\n    req = image.requestScreenCapture(10000, 0);\n  }\n  if (!req) {\n    toast(\"申请权限失败\");\n    return;\n  }\n  let d = image.initOpenCV();\n  logd(d)\n  //申请完权限至少等1s(垃圾设备多加点)再截图,否则会截不到图\n  sleep(1000)\n  let aimage = image.captureFullScreen();\n  if (aimage != null) {\n    let temp = readResAutoImage(\"tmp.png\");\n    let rectp = new Rect();\n    rectp.left = 0;\n    rectp.top = 0;\n    rectp.right = device.getScreenWidth();\n    rectp.bottom = device.getScreenHeight();\n    let matchs = image.matchTemplate(aimage, temp, 0.7, 0.9, rectp, -1, 10, 5);\n    //这玩意是个数组\n    logd(JSON.stringify(matchs));\n    //这玩意是个数组\n    if (matchs && matchs.length > 0) {\n      for (let i = 0; i < matchs.length; i++) {\n        logd(JSON.stringify(matchs[i]));\n        clickPoint(matchs[i].point.x, matchs[i].point.y)\n      }\n    }\n    //图片要回收\n    image.recycle(aimage)\n    //图片要回收\n    image.recycle(temp)\n  }\n}\n\nmain();",
  },
  {
    name: "matchTemplateJ",
    platform: "android",
    category: "image",
    signature: "image.matchTemplateJ()",
    description: "image.matchTemplateJ 图片模板匹配(JSON)",
    example: "function main() {\n  let req = image.requestScreenCapture(10000, 0);\n  if (!req) {\n    req = image.requestScreenCapture(10000, 0);\n  }\n  if (!req) {\n    toast(\"申请权限失败\");\n    return;\n  }\n  let d = image.initOpenCV();\n  logd(d)\n  //申请完权限至少等1s(垃圾设备多加点)再截图,否则会截不到图\n  sleep(1000)\n  let aimage = image.captureFullScreen();\n  if (aimage != null) {\n    let matchs = image.matchTemplateJ(aimage, \"a.json\");\n    //这玩意是个数组\n    logd(JSON.stringify(matchs));\n    //这玩意是个数组\n    if (matchs && matchs.length > 0) {\n      for (let i = 0; i < matchs.length; i++) {\n        logd(JSON.stringify(matchs[i]));\n        clickPoint(matchs[i].point.x, matchs[i].point.y)\n      }\n    }\n    //图片要回收\n    image.recycle(aimage)\n  }\n}\n\nmain();",
  },
  {
    name: "matchTemplate2",
    platform: "android",
    category: "image",
    signature: "image.matchTemplate2()",
    description: "image.matchTemplate2 图片模板匹配(函数缩放)",
    example: "function main() {\n  let req = image.requestScreenCapture(10000, 0);\n  if (!req) {\n    req = image.requestScreenCapture(10000, 0);\n  }\n  if (!req) {\n    toast(\"申请权限失败\");\n    return;\n  }\n  let d = image.initOpenCV();\n  logd(d)\n  //申请完权限至少等1s(垃圾设备多加点)再截图,否则会截不到图\n  sleep(1000)\n  let aimage = image.captureFullScreen();\n  if (aimage != null) {\n    let temp = readResAutoImage(\"tmp.png\");\n    let rectp = new Rect();\n    rectp.left = 0;\n    rectp.top = 0;\n    rectp.right = device.getScreenWidth();\n    rectp.bottom = device.getScreenHeight();\n    let matchs = image.matchTemplate2(aimage, temp, 0.7, 0.9, rectp, -1, 10, 5);\n    //这玩意是个数组\n    logd(JSON.stringify(matchs));\n    //这玩意是个数组\n    if (matchs && matchs.length > 0) {\n      for (let i = 0; i < matchs.length; i++) {\n        logd(JSON.stringify(matchs[i]));\n        clickPoint(matchs[i].point.x, matchs[i].point.y)\n      }\n    }\n    //图片要回收\n    image.recycle(aimage)\n    //图片要回收\n    image.recycle(temp)\n  }\n}\n\nmain();",
  },
  {
    name: "matchTemplate2J",
    platform: "android",
    category: "image",
    signature: "image.matchTemplate2J()",
    description: "image.matchTemplate2J 图片模板匹配(JSON)",
    example: "function main() {\n  let req = image.requestScreenCapture(10000, 0);\n  if (!req) {\n    req = image.requestScreenCapture(10000, 0);\n  }\n  if (!req) {\n    toast(\"申请权限失败\");\n    return;\n  }\n  let d = image.initOpenCV();\n  logd(d)\n  //申请完权限至少等1s(垃圾设备多加点)再截图,否则会截不到图\n  sleep(1000)\n  let aimage = image.captureFullScreen();\n  if (aimage != null) {\n    let matchs = image.matchTemplate2J(aimage, \"a.json\");\n    //这玩意是个数组\n    logd(JSON.stringify(matchs));\n    //这玩意是个数组\n    if (matchs && matchs.length > 0) {\n      for (let i = 0; i < matchs.length; i++) {\n        logd(JSON.stringify(matchs[i]));\n        clickPoint(matchs[i].point.x, matchs[i].point.y)\n      }\n    }\n    //图片要回收\n    image.recycle(aimage)\n  }\n}\n\nmain();",
  },
  {
    name: "matchTemplateEx",
    platform: "android",
    category: "image",
    signature: "image.matchTemplateEx()",
    description: "image.matchTemplateEx 图片模板匹配",
    example: "function main() {\n  let req = image.requestScreenCapture(10000, 0);\n  if (!req) {\n    req = image.requestScreenCapture(10000, 0);\n  }\n  if (!req) {\n    toast(\"申请权限失败\");\n    return;\n  }\n  let d = image.initOpenCV();\n  logd(d)\n  //申请完权限至少等1s(垃圾设备多加点)再截图,否则会截不到图\n  sleep(1000)\n  let temp = readResAutoImage(\"tmp.png\");\n  let rectp = new Rect();\n  rectp.left = 0;\n  rectp.top = 0;\n  rectp.right = device.getScreenWidth();\n  rectp.bottom = device.getScreenHeight();\n  let matchs = image.matchTemplateEx(temp, 0.7, 0.9, rectp, -1, 1, 5);\n  logd(JSON.stringify(matchs));\n  //这玩意是个数组\n  if (matchs && matchs.length > 0) {\n    for (let i = 0; i < matchs.length; i++) {\n      logd(JSON.stringify(matchs[i]));\n      clickPoint(matchs[i].point.x, matchs[i].point.y)\n    }\n  }\n  //图片要回收\n  image.recycle(aimage)\n  //图片要回收\n  image.recycle(temp)\n}\n\nmain();",
  },
  {
    name: "gray",
    platform: "android",
    category: "image",
    signature: "image.gray()",
    description: "image.gray 灰度Image",
    example: "function main() {\n  let req = image.requestScreenCapture(10000, 0);\n  if (!req) {\n    req = image.requestScreenCapture(10000, 0);\n  }\n  if (!req) {\n    toast(\"申请权限失败\");\n    return;\n  }\n  let d = image.initOpenCV();\n  logd(d)\n  //申请完权限至少等1s(垃圾设备多加点)再截图,否则会截不到图\n  sleep(1000)\n  for (let i = 0; i < 1; i++) {\n    sleep(1000);\n    let d = image.captureFullScreenEx();\n    if (d) {\n      let s = new Date().getTime();\n      let bd = image.gray(d);\n      logd(\"time \" + (new Date().getTime() - s))\n      logd(bd.uuid);\n      if (bd) {\n        let saved = image.saveTo(bd, \"/sdcard/testb2.png\");\n        logd(\"saved \" + saved)\n        exit()\n      }\n      image.recycle(bd)\n      //图片要回收\n      image.recycle(d)\n    }\n  }\n\n}\n\nmain();",
  },
  {
    name: "grayBitmap",
    platform: "android",
    category: "image",
    signature: "image.grayBitmap()",
    description: "image.grayBitmap 灰度Bitmap",
    example: "function main() {\n  let req = image.requestScreenCapture(10000, 0);\n  if (!req) {\n    req = image.requestScreenCapture(10000, 0);\n  }\n  if (!req) {\n    toast(\"申请权限失败\");\n    return;\n  }\n  let d = image.initOpenCV();\n  logd(d)\n  //申请完权限至少等1s(垃圾设备多加点)再截图,否则会截不到图\n  sleep(1000)\n  for (let i = 0; i < 1; i++) {\n    sleep(1000);\n    let s = new Date().getTime();\n    let d = image.captureScreenBitmap(\"jpg\", 100, 100, 200, 300, 100);\n    if (d) {\n      let s = new Date().getTime();\n      let bd = image.grayBitmap(d);\n      logd(\"time \" + (new Date().getTime() - s))\n      logd(bd);\n      if (bd) {\n        exit()\n      }\n      //图片要回收\n      image.recycle(d)\n    }\n  }\n\n}\n\nmain();",
  },
  {
    name: "binaryzation",
    platform: "android",
    category: "image",
    signature: "image.binaryzation()",
    description: "image.binaryzation 二值化Image",
    example: "function main() {\n  let req = image.requestScreenCapture(10000, 0);\n  if (!req) {\n    req = image.requestScreenCapture(10000, 0);\n  }\n  if (!req) {\n    toast(\"申请权限失败\");\n    return;\n  }\n  let d = image.initOpenCV();\n  logd(d)\n  //申请完权限至少等1s(垃圾设备多加点)再截图,否则会截不到图\n  sleep(1000)\n  for (let i = 0; i < 1000; i++) {\n    sleep(1000);\n    let d = image.captureFullScreenEx();\n    if (d) {\n      let s = new Date().getTime();\n      let bd = image.binaryzation(d, 1, 200);\n      logd(\"time \" + (new Date().getTime() - s))\n      logd(bd.uuid);\n      if (bd) {\n        let saved = image.saveTo(bd, \"/sdcard/testb2.png\");\n        logd(\"saved \" + saved)\n        exit()\n      }\n      //图片要回收\n      image.recycle(d)\n    }\n  }\n\n}\n\nmain();",
  },
  {
    name: "binaryzationBitmap",
    platform: "android",
    category: "image",
    signature: "image.binaryzationBitmap()",
    description: "image.binaryzationBitmap 二值化Bitmap",
    example: "function main() {\n  let req = image.requestScreenCapture(10000, 0);\n  if (!req) {\n    req = image.requestScreenCapture(10000, 0);\n  }\n  if (!req) {\n    toast(\"申请权限失败\");\n    return;\n  }\n  let d = image.initOpenCV();\n  logd(d)\n  //申请完权限至少等1s(垃圾设备多加点)再截图,否则会截不到图\n  sleep(1000)\n  for (let i = 0; i < 1000; i++) {\n    sleep(1000);\n    let s = new Date().getTime();\n    let d = image.captureScreenBitmap(\"jpg\", 100, 100, 200, 300, 100);\n    if (d) {\n      let s = new Date().getTime();\n      let bd = image.binaryzationBitmap(d, 1, 200);\n      logd(\"time \" + (new Date().getTime() - s))\n      logd(bd);\n      if (bd) {\n        exit()\n      }\n      //图片要回收\n      image.recycle(d)\n    }\n  }\n\n}\n\nmain();",
  },
  {
    name: "binaryzationBitmapEx",
    platform: "android",
    category: "image",
    signature: "image.binaryzationBitmapEx()",
    description: "image.binaryzationBitmapEx 二值化 Bitmap",
    example: "function main() {\n let req = startEnv();\n if (!req) {\n   logd(\"申请权限失败\");\n   return;\n }\n //申请完权限至少等1s(垃圾设备多加点)再截图,否则会截不到图\n sleep(1000)\n for (let i = 0; i < 1000; i++) {\n   sleep(1000);\n   let d = image.captureScreenBitmapEx();\n   if (d) {\n     let s = new Date().getTime();\n     let bd = image.binaryzationBitmapEx(d,\n       {\n         \"diameter\": 20,\n         \"adaptiveMethod\": 1,\n         \"c\": 9, \"blockSize\": 51\n       });\n     logd(\"time \" + (new Date().getTime() - s))\n     logd(bd);\n     if (bd) {\n       exit()\n     }\n     //图片要回收\n     image.recycle(d)\n   }\n }\n}\n\nmain();",
  },
  {
    name: "binaryzationEx",
    platform: "android",
    category: "image",
    signature: "image.binaryzationEx()",
    description: "image.binaryzationEx 二值化",
    example: "function main() {\n  let req = startEnv();\n  if (!req) {\n    logd(\"申请权限失败\");\n    return;\n  }\n  //申请完权限至少等1s(垃圾设备多加点)再截图,否则会截不到图\n  sleep(1000)\n  for (let i = 0; i < 1000; i++) {\n    sleep(1000);\n    let d = image.captureFullScreenEx();\n    if (d) {\n      let s = new Date().getTime();\n      let bd = image.binaryzationEx(d, {\n        \"diameter\": 20,\n        \"adaptiveMethod\": 1,\n        \"c\": 9,\n        \"blockSize\": 51\n      });\n      logd(\"time \" + (new Date().getTime() - s))\n      logd(bd.uuid);\n      if (bd) {\n        let saved = image.saveTo(bd, \"/sdcard/testb2.png\");\n        logd(\"saved \" + saved)\n        exit()\n      }\n      //图片要回收\n      image.recycle(d)\n    }\n  }\n\n}\n\nmain();",
  },
  {
    name: "getWidth",
    platform: "android",
    category: "image",
    signature: "image.getWidth()",
    description: "image.getWidth 获取宽度",
    example: "function main() {\n  let img = image.captureFullScreen()\n  logd(\" img width \" + image.getWidth(img))\n  image.recycle(img)\n}\n\nmain();",
  },
  {
    name: "getHeight",
    platform: "android",
    category: "image",
    signature: "image.getHeight()",
    description: "image.getHeight 取得高度",
    example: "function main() {\n  let img = image.captureFullScreen()\n  logd(\" img getHeight \" + image.getHeight(img))\n  image.recycle(img)\n}\n\nmain();",
  },
  {
    name: "rotateImage",
    platform: "android",
    category: "image",
    signature: "image.rotateImage()",
    description: "image.rotateImage 旋转图片",
    example: "function main() {\n  let img = image.captureFullScreen()\n  logd(\" img width \" + image.getWidth(img))\n  let img2 = image.rotateImage(img, -90);\n  image.recycle(img)\n  logd(\" img2 width \" + image.getWidth(img2))\n  image.recycle(img2)\n}\n\nmain();",
  },
  {
    name: "rotateBitmap",
    platform: "android",
    category: "image",
    signature: "image.rotateBitmap()",
    description: "image.rotateBitmap 旋转Bitmap",
    example: "function main() {\n  let img = readResBitmap(\"a.jpg\");\n  logd(\" img width \" + image.getWidth(img))\n  let img2 = image.rotateBitmap(img, -90);\n  image.recycle(img)\n  logd(\" img2 width \" + image.getWidth(img2))\n  image.recycle(img2)\n}\n\nmain();",
  },
  {
    name: "readImage",
    platform: "android",
    category: "image",
    signature: "image.readImage()",
    description: "image.readImage 读取文件为Image",
    example: "function main() {\n  let autoimg = image.readImage(\"/sdcard/a.png\");\n  //图片要回收\n  image.recycle(autoimg)\n}\n\nmain();",
  },
  {
    name: "readBitmap",
    platform: "android",
    category: "image",
    signature: "image.readBitmap()",
    description: "image.readBitmap 读取文件为Bitmap",
    example: "function main() {\n  let autoimg = image.readBitmap(\"/sdcard/a.png\");\n  //图片要回收\n  image.recycle(autoimg)\n}\n\nmain();",
  },
  {
    name: "pixelInImage",
    platform: "android",
    category: "image",
    signature: "image.pixelInImage()",
    description: "image.pixelInImage 图像坐标点颜色",
    example: "function main() {\n  let request = image.requestScreenCapture(10000, 0);\n  if (!request) {\n    request = image.requestScreenCapture(10000, 0);\n  }\n  logd(\"申请截图结果... \" + request)\n  //申请完权限至少等1s(垃圾设备多加点)再截图,否则会截不到图\n  sleep(1000)\n  let imageX = image.captureFullScreen();\n  let color = image.pixelInImage(imageX, 100, 100);\n  //图片要回收\n  image.recycle(imageX)\n}\n\nmain();",
  },
  {
    name: "argb",
    platform: "android",
    category: "image",
    signature: "image.argb()",
    description: "image.argb 颜色转16进制字符串",
    example: "function main() {\n  let req = image.requestScreenCapture(10000, 0);\n  if (!req) {\n    req = image.requestScreenCapture(10000, 0);\n  }\n  if (!req) {\n    toast(\"申请权限失败\");\n  }\n  //申请完权限至少等1s(垃圾设备多加点)再截图,否则会截不到图\n  sleep(1000)\n  let aimage = image.captureFullScreen();\n  if (aimage != null) {\n    let points3 = \"765|22|0x1296DB\";\n    logd(\"==\" + image.argb(image.pixel(aimage, 765, 22)));\n    let points = image.cmpColor(aimage, points3, 0.5, 0, 0, 0, 0);\n    logd(\"points \" + points);\n    //图片要回收\n    image.recycle(aimage)\n  }\n}\n\nmain();",
  },
  {
    name: "argb",
    platform: "android",
    category: "image",
    signature: "image.argb()",
    description: "image.argb RGB字符串",
    example: "function main() {\n  let request = image.requestScreenCapture(10000, 0);\n  if (!request) {\n    request = image.requestScreenCapture(10000, 0);\n  }\n  logd(\"申请截图结果... \" + request)\n  //申请完权限至少等1s(垃圾设备多加点)再截图,否则会截不到图\n  sleep(1000)\n  let imageX = image.captureFullScreen();\n  let color = image.pixel(imageX, 100, 100);\n  logd(image.argb(color))\n  //图片要回收\n  image.recycle(imageX)\n}\n\nmain();",
  },
  {
    name: "getPixelBitmap",
    platform: "android",
    category: "image",
    signature: "image.getPixelBitmap()",
    description: "image.getPixelBitmap 取得Bitmap单点颜色",
    example: "function main() {\n  let request = image.requestScreenCapture(10000, 0);\n  if (!request) {\n    request = image.requestScreenCapture(10000, 0);\n  }\n  logd(\"申请截图结果... \" + request)\n  ///申请完权限至少等1s(垃圾设备多加点)再截图,否则会截不到图\n  sleep(1000)\n  let bitmap = image.captureScreenBitmap(\"jpg\", 800, 800, 100, 100, 100);\n  let color = image.getPixelBitmap(bitmap, 100, 100);\n  //图片要回收\n  image.recycle(bitmap)\n}\n\nmain();",
  },
  {
    name: "getPixelsBitmap",
    platform: "android",
    category: "image",
    signature: "image.getPixelsBitmap()",
    description: "image.getPixelsBitmap 取得Bitmap区域颜色",
    example: "function main() {\n  let request = image.requestScreenCapture(10000, 0);\n  if (!request) {\n    request = image.requestScreenCapture(10000, 0);\n  }\n  logd(\"申请截图结果... \" + request)\n  //申请完权限至少等1s(垃圾设备多加点)再截图,否则会截不到图\n  sleep(1000)\n  let bitmap = image.captureScreenBitmap(\"jpg\", 800, 800, 100, 100, 100);\n  let w = bitmap.getWidth();\n  let h = bitmap.getHeight();\n  let mPixels = image.getPixelsBitmap(bitmap, w * h, 0, w, 0, 0, w, h);\n  //图片要回收\n  image.recycle(bitmap)\n}\n\nmain();",
  },
  {
    name: "readImageNotAgent",
    platform: "android",
    category: "image",
    signature: "image.readImageNotAgent()",
    description: "image.readImageNotAgent 读取文件为Image",
    example: "function main() {\n  let autoimg = image.readImageNotAgent(\"/sdcard/a.png\");\n  //图片要回收\n  image.recycle(autoimg)\n}\n\nmain();",
  },
  {
    name: "bitmapToImageNotAgent",
    platform: "android",
    category: "image",
    signature: "image.bitmapToImageNotAgent()",
    description: "image.bitmapToImageNotAgent (Bitmap转AutoImage)",
    example: "function main() {\n  let request = image.requestScreenCapture(10000, 0);\n  if (!request) {\n    request = image.requestScreenCapture(10000, 0);\n  }\n\n  logd(\"申请截图结果... \" + request)\n  //申请完权限至少等1s(垃圾设备多加点)再截图,否则会截不到图\n  sleep(1000)\n  for (let i = 0; i < 100; i++) {\n    let d = image.captureFullScreenEx(\"jpg\", 0, 0, 0, 0, 100);\n    logd(d)\n    sleep(1000);\n    if (d) {\n      let ds = image.imageToBitmap(d);\n      logd(ds)\n      //再次转换为autoimage对象\n      let sy = image.bitmapToImageNotAgent(ds);\n      logd(sy)\n      ds.recycle();\n      image.recyle(d);\n    }\n\n  }\n\n}\n\nmain();",
  },
  {
    name: "readResAutoImageNotAgent",
    platform: "android",
    category: "image",
    signature: "image.readResAutoImageNotAgent()",
    description: "将res文件夹的文件转换为AutoImage",
    example: "function main() {\n  var b = image.readResAutoImageNotAgent(\"img/a.png\");\n  logd(\"b \" + b)\n}\n\nmain();",
  },
  {
    name: "saveTo",
    platform: "android",
    category: "image",
    signature: "image.saveTo()",
    description: "image.saveTo 保存到文件",
    example: "function main() {\n  let request = image.requestScreenCapture(10000, 0);\n  if (!request) {\n    request = image.requestScreenCapture(10000, 0);\n  }\n  logd(\"申请截图结果... \" + request)\n  //申请完权限至少等1s(垃圾设备多加点)再截图,否则会截不到图\n  sleep(1000)\n  let imageX = image.captureFullScreen();\n  let r = image.saveTo(imageX, \"/sdcard/a.png\");\n  toast(\"result \" + r);\n  //图片要回收\n  image.recycle(imageX)\n}\n\nmain();",
  },
  {
    name: "saveBitmap",
    platform: "android",
    category: "image",
    signature: "image.saveBitmap()",
    description: "image.saveBitmap 保存bitmap图像",
    example: "function main() {\n\n  //生成一个二维码bitmap 带logo的\n  let bot = utils.createQRCode(\"我是大佬的弟弟\", 1000, 1000, image.readBitmap(\"/sdcard/yyb2.png\"));\n  logd(\"bot \" + bot);\n  //保存的到文件\n  let saved = image.saveBitmap(bot, \"png\", 100, \"/sdcard/tmp.png\");\n  logd(\"saved \" + saved);\n  //回收掉防止内存暴涨\n  if (bot) {\n    bot.recycle()\n  }\n  //扫描二维码\n  let bitmap = image.readBitmap(\"/sdcard/tmp.png\")\n  let data = utils.decodeQRCode(bitmap);\n  logd(\"data \" + data);\n  //回收掉防止内存暴涨\n  if (bitmap) {\n    bitmap.recycle()\n  }\n}\n\nmain();",
  },
  {
    name: "toBase64Format",
    platform: "android",
    category: "image",
    signature: "image.toBase64Format()",
    description: "image.toBase64Format Image转base64",
    example: "function main() {\n  let request = image.requestScreenCapture(10000, 0);\n  if (!request) {\n    request = image.requestScreenCapture(10000, 0);\n  }\n  logd(\"申请截图结果... \" + request)\n  //申请完权限至少等1s(垃圾设备多加点)再截图,否则会截不到图\n  sleep(1000)\n  let imageX = image.captureFullScreen();\n  let r = image.toBase64Format(imageX, \"jpg\", 50);\n  toast(\"result \" + r);\n  //图片要回收\n  image.recycle(imageX)\n}\n\nmain();",
  },
  {
    name: "clip",
    platform: "android",
    category: "image",
    signature: "image.clip()",
    description: "image.clip 剪切图片",
    example: "function main() {\n  let request = image.requestScreenCapture(10000, 0);\n  if (!request) {\n    request = image.requestScreenCapture(10000, 0);\n  }\n  logd(\"申请截图结果... \" + request)\n  //申请完权限至少等1s(垃圾设备多加点)再截图,否则会截不到图\n  sleep(1000)\n  let imageX = image.captureFullScreen();\n  let r = image.clip(imageX, 100, 100, 300, 400);\n  toast(\"result \" + r);\n  //图片要回收\n  image.recycle(imageX)\n  image.recycle(r)\n}\n\nmain();",
  },
  {
    name: "scaleImage",
    platform: "android",
    category: "image",
    signature: "image.scaleImage()",
    description: "image.scaleImage 缩放图片",
    example: "function main() {\n  let request = image.requestScreenCapture(10000, 0);\n  if (!request) {\n    request = image.requestScreenCapture(10000, 0);\n  }\n  logd(\"申请截图结果... \" + request)\n  //申请完权限至少等1s(垃圾设备多加点)再截图,否则会截不到图\n  sleep(1000)\n  let imageX = image.captureFullScreen();\n  let r = image.scaleImage(imageX, 300, 400);\n  toast(\"result \" + r);\n  //图片要回收\n  image.recycle(imageX)\n  image.recycle(r)\n}\n\nmain();",
  },
  {
    name: "pixel",
    platform: "android",
    category: "image",
    signature: "image.pixel()",
    description: "image.pixel 图片某点颜色值",
    example: "function main() {\n  let request = image.requestScreenCapture(10000, 0);\n  if (!request) {\n    request = image.requestScreenCapture(10000, 0);\n  }\n  logd(\"申请截图结果... \" + request)\n  //申请完权限至少等1s(垃圾设备多加点)再截图,否则会截不到图\n  sleep(1000)\n  let imageX = image.captureFullScreen();\n  let r = image.pixel(imageX, 100, 100);\n  toast(\"result \" + r);\n  //图片要回收\n  image.recycle(imageX)\n}\n\nmain();",
  },
  {
    name: "isRecycled",
    platform: "android",
    category: "image",
    signature: "image.isRecycled()",
    description: "image.isRecycled 图片回收判断",
    example: "function main() {\n  let imageX = image.captureFullScreen();\n  let r = image.isRecycled(imageX);\n  toast(\"result \" + r);\n  //图片要回收\n  image.recycle(imageX)\n}\n\nmain();",
  },
  {
    name: "recycle",
    platform: "android",
    category: "image",
    signature: "image.recycle()",
    description: "image.recycle 回收图片",
    example: "function main() {\n  let imageX = image.captureFullScreen();\n  //图片要回收\n  image.recycle(imageX)\n\n}\n\nmain();",
  },
  {
    name: "recycleAllImage",
    platform: "android",
    category: "image",
    signature: "image.recycleAllImage()",
    description: "image.recycleAllImage 回收所有图片",
    example: "function main() {\n  let imageX = image.captureFullScreen();\n  //图片要回收\n  image.recycleAllImage()\n\n}\n\nmain();",
  },
  {
    name: "clipBitmap",
    platform: "android",
    category: "image",
    signature: "image.clipBitmap()",
    description: "image.clipBitmap (剪裁bitmap)",
    example: "function main() {\n  let request = image.requestScreenCapture(10000, 0);\n  if (!request) {\n    request = image.requestScreenCapture(10000, 0);\n  }\n  logd(\"申请截图结果... \" + request)\n  //申请完权限至少等1s(垃圾设备多加点)再截图,否则会截不到图\n  sleep(1000)\n  for (let i = 0; i < 100; i++) {\n    let d = image.captureScreenBitmap(\"jpg\", 0, 0, 0, 0, 100);\n    logd(d)\n    sleep(1000);\n    if (d) {\n      d = image.clipBitmap(d, 100, 100, 200, 200);\n      let ds = image.bitmapBase64(d, \"jpg\", 100);\n      logd(ds)\n      loge(image.base64Bitmap(ds, 0))\n      //图片要回收\n      image.recycle(d)\n    }\n  }\n\n}\n\nmain();",
  },
  {
    name: "scaleBitmap",
    platform: "android",
    category: "image",
    signature: "image.scaleBitmap()",
    description: "image.scaleBitmap (缩放bitmap)",
    example: "function main() {\n  let request = image.requestScreenCapture(10000, 0);\n  if (!request) {\n    request = image.requestScreenCapture(10000, 0);\n  }\n  logd(\"申请截图结果... \" + request)\n  //申请完权限至少等1s(垃圾设备多加点)再截图,否则会截不到图\n  sleep(1000)\n  for (let i = 0; i < 100; i++) {\n    let d = image.captureScreenBitmap(\"jpg\", 0, 0, 0, 0, 100);\n    logd(d)\n    sleep(1000);\n    if (d) {\n      d = image.scaleBitmap(d, 200, 200);\n      let ds = image.bitmapBase64(d, \"jpg\", 100);\n      logd(ds)\n      loge(image.base64Bitmap(ds, 0))\n      //图片要回收\n      image.recycle(d)\n    }\n  }\n\n}\n\nmain();",
  },
  {
    name: "base64Bitmap",
    platform: "android",
    category: "image",
    signature: "image.base64Bitmap()",
    description: "image.base64Bitmap (base64转bitmap)",
    example: "function main() {\n  let request = image.requestScreenCapture(10000, 0);\n  if (!request) {\n    request = image.requestScreenCapture(10000, 0);\n  }\n  logd(\"申请截图结果... \" + request)\n  //申请完权限至少等1s(垃圾设备多加点)再截图,否则会截不到图\n  sleep(1000)\n  for (let i = 0; i < 100; i++) {\n    let d = image.captureScreenBitmap(\"jpg\", 0, 0, 0, 0, 100);\n    logd(d)\n    sleep(1000);\n    if (d) {\n      d = image.clipBitmap(d, 100, 100, 200, 200);\n      let ds = image.bitmapBase64(d, \"jpg\", 100);\n      logd(ds)\n      loge(image.base64Bitmap(ds, 0))\n      //图片要回收\n      image.recycle(d)\n    }\n  }\n\n}\n\nmain();",
  },
  {
    name: "bitmapBase64",
    platform: "android",
    category: "image",
    signature: "image.bitmapBase64()",
    description: "image.bitmapBase64 (bitmap转base64)",
    example: "function main() {\n  let request = image.requestScreenCapture(10000, 0);\n  if (!request) {\n    request = image.requestScreenCapture(10000, 0);\n  }\n  logd(\"申请截图结果... \" + request)\n  //申请完权限至少等1s(垃圾设备多加点)再截图,否则会截不到图\n  sleep(1000)\n  for (let i = 0; i < 100; i++) {\n    let d = image.captureScreenBitmap(\"jpg\", 0, 0, 0, 0, 100);\n    logd(d)\n    sleep(1000);\n    if (d) {\n      d = image.clipBitmap(d, 100, 100, 200, 200);\n      let ds = image.bitmapBase64(d, \"jpg\", 100);\n      logd(ds)\n      loge(image.base64Bitmap(ds, 0))\n      //图片要回收\n      image.recycle(d)\n    }\n  }\n}\n\nmain();",
  },
  {
    name: "imageToBitmap",
    platform: "android",
    category: "image",
    signature: "image.imageToBitmap()",
    description: "image.imageToBitmap (AutoImage转Bitmap)",
    example: "function main() {\n  let request = image.requestScreenCapture(10000, 0);\n  if (!request) {\n    request = image.requestScreenCapture(10000, 0);\n  }\n  logd(\"申请截图结果... \" + request)\n  //申请完权限至少等1s(垃圾设备多加点)再截图,否则会截不到图\n  sleep(1000)\n  for (let i = 0; i < 100; i++) {\n    let d = image.captureFullScreenEx(\"jpg\", 0, 0, 0, 0, 100);\n    logd(d)\n    sleep(1000);\n    if (d) {\n      let ds = image.imageToBitmap(d);\n      logd(ds)\n      ds.recycle();\n      image.recycle(d);\n    }\n  }\n\n}\n\nmain();",
  },
  {
    name: "bitmapToImage",
    platform: "android",
    category: "image",
    signature: "image.bitmapToImage()",
    description: "image.bitmapToImage (Bitmap转AutoImage)",
    example: "function main() {\n  let request = image.requestScreenCapture(10000, 0);\n  if (!request) {\n    request = image.requestScreenCapture(10000, 0);\n  }\n\n  logd(\"申请截图结果... \" + request)\n  //申请完权限至少等1s(垃圾设备多加点)再截图,否则会截不到图\n  sleep(1000)\n  for (let i = 0; i < 100; i++) {\n    let d = image.captureFullScreen();\n    logd(d)\n    sleep(1000);\n    if (d) {\n      let ds = image.imageToBitmap(d);\n      logd(ds)\n      //再次转换为autoimage对象\n      let sy = image.bitmapToImage(ds);\n      logd(sy)\n      ds.recycle();\n      image.recyle(d);\n    }\n  }\n}\n\nmain();",
  },
  {
    name: "imageToMatFormat",
    platform: "android",
    category: "image",
    signature: "image.imageToMatFormat()",
    description: "image.imageToMatFormat ( Bitmap 转Mat AutoImage)",
    example: "function main() {\n  let req = startEnv();\n  if (!req) {\n    logd(\"申请权限失败\");\n    return;\n  }\n  logd(\"申请截图结果... \" + request)\n  //申请完权限至少等1s(垃圾设备多加点)再截图,否则会截不到图\n  sleep(1000)\n  image.useOpencvMat(0)\n  for (let i = 0; i < 100; i++) {\n    let d = image.captureFullScreen();\n    logd(d)\n    sleep(1000);\n    if (d) {\n      let ds = image.imageToMatFormat(d);\n      logd(ds)\n      image.recyle(d);\n    }\n\n  }\n}\n\nmain();",
  },
  {
    name: "matToImageFormat",
    platform: "android",
    category: "image",
    signature: "image.matToImageFormat()",
    description: "function main() {  let req = startEnv();  if (!req) {    logd(\"申请权限失败\");    return;  }  logd(\"申请截图结果... \" + request)  sleep(1000)  image.useOpencvMat(1)  for (let i = 0; i < 100; i++) {    let d = image.captureFullScreen();    logd(d)    sleep(1000);    if (d) {      let ds = image.matToImageForm...",
    example: "function main() {\n  let req = startEnv();\n  if (!req) {\n    logd(\"申请权限失败\");\n    return;\n  }\n  logd(\"申请截图结果... \" + request)\n  sleep(1000)\n  image.useOpencvMat(1)\n  for (let i = 0; i < 100; i++) {\n    let d = image.captureFullScreen();\n    logd(d)\n    sleep(1000);\n    if (d) {\n      let ds = image.matToImageFormat(d);\n      logd(ds)\n      image.recyle(d);\n    }\n\n  }\n}\n\nmain();",
  },
  // ===================== ocr =====================
  {
    name: "917\u7248\u672c-\u5355\u5b9e\u4f8b-paddleocronline",
    platform: "android",
    category: "ocr",
    signature: "ocr.917版本-单实例-paddleocronline()",
    description: "9.17+版本-单实例-paddleOcrOnline",
    example: "let paddleOcrOnline = null\n\n//脚本停止回调\nsetStopCallback(function () {\n    //释放所有资源,一般不需要调用,或者放到setStopCallback中\n    logi(\"释放paddleOcrOnline对象\")\n    paddleOcrOnline && paddleOcrOnline.releaseAll()\n})\n\n//初始化自动化环境\nfunction initEnv() {\n    if (!startEnv()) {\n        loge(\"自动化启动失败,结束脚本\")\n        exit()\n    }\n    if (!image.requestScreenCapture(10000, 0)) {\n        loge(\"申请截图权限失败,检查是否开启后台弹出,悬浮框等权限\")\n        exit()\n    }\n    //申请完权限至少等1s(垃圾设备多加点)再截图,否则会截不到图\n    sleep(1000)\n}\n\n//初始化所有ocr对象及环境\nfunction initAllOcr() {\n    // paddleOcrOnline,参数参考上方解释,不需要的参数可以不填\n    let paddleOcrOnlineMap = {\n        \"serverUrl\": \"10.0.0.112\", \n        \"type\": \"paddleOcrOnline\",\n        \"ocrType\": \"ONNX_PPOCR_V3\",\n    }\n    //创建ocr对象,仅脚本开头一次即可\n    paddleOcrOnline = ocr.newOcr()\n    //初始化ocr,仅脚本开头一次即可\n    if (!paddleOcrOnline.initOcr(paddleOcrOnlineMap)) {\n        loge(\"OCR初始化失败 : \" + paddleOcrOnline.getErrorMsg())\n        exit()\n    }\n}\n\n// ocr识别\nfunction ocrFunc() {\n    // 截图\n    let img = image.captureFullScreenEx()\n    if (!img) {\n        loge(\"截图失败\")\n        return\n    }\n    logi(\"===================paddleOcrOnline识别======================\")\n    // 用paddleOcrOnline进行识别\n    let result = paddleOcrOnline.ocrImage(img, 20 * 1000, {})\n    if (result) {\n        logd(\"ocr结果-> \" + JSON.stringify(result))\n        for (let i = 0; i < result.length; i++) {\n            let value = result[i]\n            logd(\"文字 : \" + value.label + \" x: \" + value.x + \" y: \" + value.y + \" width: \" + value.width + \" height: \" + value.height)\n        }\n    } else {\n        logw(\"未识别到结果\")\n    }\n\n    //回收图片\n    image.recycle(img)\n}\n\nfunction main() {\n    //初始化环境\n    initEnv()\n    //初始化所有ocr对象及环境\n    initAllOcr()\n    //多次识别\n    ocrFunc()\n    ocrFunc()\n    ocrFunc()\n}\n\nmain()",
  },
  {
    name: "1117\u7248\u672cpaddleliteocr\u4f8b\u5b50",
    platform: "android",
    category: "ocr",
    signature: "ocr.1117版本paddleliteocr例子()",
    description: "11.17+版本paddleLiteOcr例子",
    example: "let paddlelite = null\n//脚本停止回调\nsetStopCallback(function () {\n  //释放所有资源,一般不需要调用,或者放到setStopCallback中\n  logi(\"释放 paddleLite 对象\")\n  paddlelite && paddlelite.releaseAll()\n})\n\n//初始化自动化环境\nfunction initEnv() {\n  if (!startEnv()) {\n    loge(\"自动化启动失败,结束脚本\")\n    exit()\n  }\n  if (!image.requestScreenCapture(10000, 0)) {\n    loge(\"申请截图权限失败,检查是否开启后台弹出,悬浮框等权限\")\n    exit()\n  }\n  //申请完权限至少等1s(垃圾设备多加点)再截图,否则会截不到图\n  sleep(1000)\n}\n\n//初始化 paddlelite\nfunction initOcrpaddle() {\n  let paddleliteMap = {\"type\": \"paddleLiteOcr\"}\n  //创建ocr对象,仅脚本开头一次即可\n  paddlelite = ocr.newOcr()\n  //初始化ocr,仅脚本开头一次即可\n  if (!paddlelite.initOcr(paddleliteMap)) {\n    loge(\"OCR初始化失败 : \" + paddlelite.getErrorMsg())\n    exit()\n  }\n}\n\n// ocr识别\nfunction ocrFunc() {\n  // 截图\n  let img = image.captureFullScreenEx()\n  if (!img) {\n    loge(\"截图失败\")\n    return\n  }\n  // 对图片进行识别\n  let result = paddlelite.ocrImage(img, 20 * 1000, {})\n  if (result) {\n    logd(\"ocr结果-》 \" + JSON.stringify(result))\n    for (let i = 0; i < result.length; i++) {\n      let value = result[i]\n      logd(\"文字 : \" + value.label + \" x: \" + value.x + \" y: \" + value.y + \" width: \" + value.width + \" height: \" + value.height)\n    }\n  } else {\n    logw(\"未识别到结果\")\n  }\n  //回收图片\n  image.recycle(img)\n}\n\nfunction main() {\n  //初始化环境\n  initEnv()\n  //初始化ocr\n  initOcrpaddle()\n  //多次识别\n  ocrFunc()\n  ocrFunc()\n  ocrFunc()\n}\n\nmain()",
  },
  {
    name: "1126\u7248\u672c-paddleocronnxv4paddleocronnxv5-\u4f8b\u5b50",
    platform: "android",
    category: "ocr",
    signature: "ocr.1126版本-paddleocronnxv4paddleocronnxv5-例子()",
    description: "11.26+版本 paddleOcrOnnxV4,paddleOcrOnnxV5 例子",
    example: "let paddleOcrOnnx = null\n//脚本停止回调\nsetStopCallback(function () {\n  //释放所有资源,一般不需要调用,或者放到setStopCallback中\n  logi(\"释放 paddleOcrOnnx 对象\")\n  paddleOcrOnnx && paddleOcrOnnx.releaseAll()\n})\n\n//初始化自动化环境\nfunction initEnv() {\n  if (!startEnv()) {\n    loge(\"自动化启动失败,结束脚本\")\n    exit()\n  }\n  if (!image.requestScreenCapture(10000, 0)) {\n    loge(\"申请截图权限失败,检查是否开启后台弹出,悬浮框等权限\")\n    exit()\n  }\n  //申请完权限至少等1s(垃圾设备多加点)再截图,否则会截不到图\n  sleep(1000)\n}\n\n//初始化 paddlelite\nfunction initOcrpaddle() {\n  // type可以是paddleOcrOnnxV4或者paddleOcrOnnxV5\n  let paddleOnnxMap = {\"type\": \"paddleOcrOnnxV4\",\"modelsDir\":\"\",\"numThread\":2,\"padding\":60,\"maxSideLen\":960}\n  //创建ocr对象,仅脚本开头一次即可\n  paddleOcrOnnx = ocr.newOcr()\n  //初始化ocr,仅脚本开头一次即可\n  if (!paddleOcrOnnx.initOcr(paddleOnnxMap)) {\n    loge(\"OCR初始化失败 : \" + paddleOcrOnnx.getErrorMsg())\n    exit()\n  }\n}\n\n// ocr识别\nfunction ocrFunc() {\n  // 截图\n  let img = image.captureFullScreenEx()\n  if (!img) {\n    loge(\"截图失败\")\n    return\n  }\n  // 对图片进行识别\n  let result = paddleOcrOnnx.ocrImage(img, 20 * 1000, {})\n  if (result) {\n    logd(\"ocr结果-》 \" + JSON.stringify(result))\n    for (let i = 0; i < result.length; i++) {\n      let value = result[i]\n      logd(\"文字 : \" + value.label+\" confidence:\"+value.confidence + \" 范围: \" + value.x + \",\" + value.y + \",\" + (value.x+value.width) + \",\" + (value.y+value.height))\n    }\n  } else {\n    logw(\"未识别到结果\")\n  }\n  //回收图片\n  image.recycle(img)\n}\n\nfunction main() {\n  //初始化环境\n  initEnv()\n  //初始化ocr\n  initOcrpaddle()\n  //多次识别\n  ocrFunc()\n  ocrFunc()\n  ocrFunc()\n}\n\nmain()",
  },
  {
    name: "1128\u7248\u672c-paddleocrncnnv5-\u4f8b\u5b50",
    platform: "android",
    category: "ocr",
    signature: "ocr.1128版本-paddleocrncnnv5-例子()",
    description: "11.28+版本 paddleOcrNcnnV5 例子",
    example: "let ncnnOcr = null\n//脚本停止回调\nsetStopCallback(function () {\n  //释放所有资源,一般不需要调用,或者放到setStopCallback中\n  logi(\"释放 ncnnOcr 对象\")\n  ncnnOcr && ncnnOcr.releaseAll()\n})\n\n//初始化自动化环境\nfunction initEnv() {\n  if (!startEnv()) {\n    loge(\"自动化启动失败,结束脚本\")\n    exit()\n  }\n  if (!image.requestScreenCapture(10000, 0)) {\n    loge(\"申请截图权限失败,检查是否开启后台弹出,悬浮框等权限\")\n    exit()\n  }\n  //申请完权限至少等1s(垃圾设备多加点)再截图,否则会截不到图\n  sleep(1000)\n}\n\n//初始化 \nfunction initOcrpaddle() {\n  // type= paddleOcrNcnnV5\n  let config = {\"type\": \"paddleOcrNcnnV5\",\"modelsDir\":\"\",\"numThread\":0,\"padding\":32,\"maxSideLen\":640}\n  //创建ocr对象,仅脚本开头一次即可\n  ncnnOcr = ocr.newOcr()\n  //初始化ocr,仅脚本开头一次即可\n  if (!ncnnOcr.initOcr(config)) {\n    loge(\"OCR初始化失败 : \" + ncnnOcr.getErrorMsg())\n    exit()\n  }\n}\n\n// ocr识别\nfunction ocrFunc() {\n  // 截图\n  let img = image.captureFullScreenEx()\n  if (!img) {\n    loge(\"截图失败\")\n    return\n  }\n  // 对图片\u0000进行识别 也可以动态设置参数\n  let result = ncnnOcr.ocrImage(img, 20 * 1000, {\"padding\":32})\n  if (result) {\n    logd(\"ocr结果-》 \" + JSON.stringify(result))\n    for (let i = 0; i < result.length; i++) {\n      let value = result[i]\n      logd(\"文字 : \" + value.label+\" confidence:\"+value.confidence + \" 范围: \" + value.x + \",\" + value.y + \",\" + (value.x+value.width) + \",\" + (value.y+value.height))\n    }\n  } else {\n    logw(\"未识别到结果\")\n  }\n  //回收图片\n  image.recycle(img)\n}\n\nfunction main() {\n  //初始化环境\n  initEnv()\n  //初始化ocr\n  initOcrpaddle()\n  //多次识别\n  ocrFunc()\n  ocrFunc()\n  ocrFunc()\n}\n\nmain()",
  },
  {
    name: "917\u7248\u672c-\u5355\u5b9e\u4f8b-ocrlite",
    platform: "android",
    category: "ocr",
    signature: "ocr.917版本-单实例-ocrlite()",
    description: "9.17+版本-单实例-ocrLite",
    example: "let ocrLite = null\n//脚本停止回调\nsetStopCallback(function () {\n    //释放所有资源,一般不需要调用,或者放到setStopCallback中\n    logi(\"释放ocrLite对象\")\n    ocrLite && ocrLite.releaseAll()\n})\n\n//初始化自动化环境\nfunction initEnv() {\n    if (!startEnv()) {\n        loge(\"自动化启动失败,结束脚本\")\n        exit()\n    }\n    if (!image.requestScreenCapture(10000, 0)) {\n        loge(\"申请截图权限失败,检查是否开启后台弹出,悬浮框等权限\")\n        exit()\n    }\n    //申请完权限至少等1s(垃圾设备多加点)再截图,否则会截不到图\n    sleep(1000)\n}\n\n//初始化ocrlite\nfunction initOcrLite() {\n    let ocrLiteMap = {\"type\": \"ocrLite\", \"numThread\": 1, \"padding\": 10, \"maxSideLen\": 0}\n    //创建ocr对象,仅脚本开头一次即可\n    ocrLite = ocr.newOcr()\n    //初始化ocr,仅脚本开头一次即可\n    if (!ocrLite.initOcr(ocrLiteMap)) {\n        loge(\"OCR初始化失败 : \" + ocrLite.getErrorMsg())\n        exit()\n    }\n}\n\n// ocr识别\nfunction ocrFunc() {\n    // 截图\n    let img = image.captureFullScreenEx()\n    if (!img) {\n        loge(\"截图失败\")\n        return\n    }\n    // 对图片进行识别\n    let result = ocrLite.ocrImage(img, 20 * 1000, {})\n    if (result) {\n        logd(\"ocr结果-》 \" + JSON.stringify(result))\n        for (let i = 0; i < result.length; i++) {\n            let value = result[i]\n            logd(\"文字 : \" + value.label + \" x: \" + value.x + \" y: \" + value.y + \" width: \" + value.width + \" height: \" + value.height)\n        }\n    } else {\n        logw(\"未识别到结果\")\n    }\n    //回收图片\n    image.recycle(img)\n}\n\nfunction main() {\n    //初始化环境\n    initEnv()\n    //初始化ocr\n    initOcrLite()\n    //多次识别\n    ocrFunc()\n    ocrFunc()\n    ocrFunc()\n}\n\nmain()",
  },
  {
    name: "917\u7248\u672c-\u591a\u5b9e\u4f8b-ocrlite--mlkit",
    platform: "android",
    category: "ocr",
    signature: "ocr.917版本-多实例-ocrlite--mlkit()",
    description: "9.17+版本-多实例-ocrLite + mlkit",
    example: "let ocrLite = null\nlet mlkit = null\n//脚本停止回调\nsetStopCallback(function () {\n    //释放所有资源,一般不需要调用,或者放到setStopCallback中\n    logi(\"释放ocrLite对象\")\n    ocrLite && ocrLite.releaseAll()\n    logi(\"释放mlkit对象\")\n    mlkit && mlkit.releaseAll()\n})\n\n//初始化自动化环境\nfunction initEnv() {\n    if (!startEnv()) {\n        loge(\"自动化启动失败,结束脚本\")\n        exit()\n    }\n    if (!image.requestScreenCapture(10000, 0)) {\n        loge(\"申请截图权限失败,检查是否开启后台弹出,悬浮框等权限\")\n        exit()\n    }\n    //申请完权限至少等1s(垃圾设备多加点)再截图,否则会截不到图\n    sleep(1000)\n}\n\n//初始化所有ocr对象及环境\nfunction initAllOcr() {\n    let ocrLiteMap = {\"type\": \"ocrLite\", \"numThread\": 1, \"padding\": 10, \"maxSideLen\": 0}\n    //创建ocr对象,仅脚本开头一次即可\n    ocrLite = ocr.newOcr()\n    //初始化ocr,仅脚本开头一次即可\n    if (!ocrLite.initOcr(ocrLiteMap)) {\n        loge(\"OCR初始化失败 : \" + ocrLite.getErrorMsg())\n        exit()\n    }\n\n    let mlkitMap = {\n        \"type\": \"mlkit\",\n    }\n    //创建ocr对象,仅脚本开头一次即可\n    mlkit = ocr.newOcr()\n    //初始化ocr,仅脚本开头一次即可\n    if (!mlkit.initOcr(mlkitMap)) {\n        loge(\"OCR初始化失败 : \" + mlkit.getErrorMsg())\n        exit()\n    }\n}\n\n// ocr识别\nfunction ocrFunc() {\n    // 截图\n    let img = image.captureFullScreenEx()\n    if (!img) {\n        loge(\"截图失败\")\n        return\n    }\n    logi(\"===================ocrLite识别======================\")\n    // 用ocrLite进行识别\n    let result = ocrLite.ocrImage(img, 20 * 1000, {})\n    if (result) {\n        logd(\"ocr结果-》 \" + JSON.stringify(result))\n        for (let i = 0; i < result.length; i++) {\n            let value = result[i]\n            logd(\"文字 : \" + value.label + \" x: \" + value.x + \" y: \" + value.y + \" width: \" + value.width + \" height: \" + value.height)\n        }\n    } else {\n        logw(\"未识别到结果\")\n    }\n    // 用mlkit进行识别\n    logi(\"===================mlkit识别======================\")\n    // orz 参数代表是旋转角度，0 代表不旋转 90 代表向左旋转90度，还有180，270，360参数\n    result = mlkit.ocrImage(img, 20 * 1000,  {\"orz\": 0})\n    if (result) {\n        logd(\"ocr结果-》 \" + JSON.stringify(result))\n        for (let i = 0; i < result.length; i++) {\n            let value = result[i]\n            logd(\"文字 : \" + value.label + \" x: \" + value.x + \" y: \" + value.y + \" width: \" + value.width + \" height: \" + value.height)\n        }\n    } else {\n        logw(\"未识别到结果\")\n    }\n    //回收图片\n    image.recycle(img)\n}\n\nfunction main() {\n    //初始化环境\n    initEnv()\n    //初始化所有ocr对象及环境\n    initAllOcr()\n    //多次识别\n    ocrFunc()\n    ocrFunc()\n    ocrFunc()\n}\n\nmain()",
  },
  {
    name: "easyedge",
    platform: "android",
    category: "ocr",
    signature: "ocr.easyedge()",
    description: "提示此方法不推荐",
    example: "function main() {\n    logd(\"isServiceOk \" + isServiceOk());\n    startEnv()\n    logd(\"isServiceOk \" + isServiceOk());\n    let request = image.requestScreenCapture(10000, 0);\n    if (!request) {\n        request = image.requestScreenCapture(10000, 0);\n    }\n    logd(\"申请截图结果... \" + request)\n    if (!request) {\n        loge(\"申请截图权限失败,检查是否开启后台弹出,悬浮框等权限\")\n        exit()\n    }\n    //申请完权限至少等1s(垃圾设备多加点)再截图,否则会截不到图\n    sleep(1000)\n\n    let paddleocr = {\n        \"type\": \"paddleocr\"\n    }\n\n    let easyedge = {\n        \"type\": \"easyedge\",\n\n    }\n    let ocrlite = {\n        \"type\": \"ocrLite\",\n\n    }\n    let inited = ocr.initOcr(easyedge)\n    logd(\"初始化结果 -\" + inited);\n    if (!inited) {\n        loge(\"error : \" + ocr.getErrorMsg());\n        return;\n    }\n    // ocrLite 模式下 不需要初始化这个server 没用的\n    let initServer = ocr.initOcrServer(5 * 1000);\n    logd(\"initServer \" + initServer);\n    if (!initServer) {\n        loge(\"initServer error : \" + ocr.getErrorMsg());\n        return;\n    }\n    ocr.setDaemonServer(true, 500);\n    for (var ix = 0; ix < 20; ix++) {\n\n        //读取一个bitmap\n        let bitmap = image.readBitmap(\"/sdcard/Screenshot_20210127_152932_com.huawei.android.lau.jpg\");\n        if (!bitmap) {\n            loge(\"读取图片失败\");\n            continue;\n        }\n        console.time(\"1\")\n        logd(\"start---ocr\");\n        // 对图片进行识别\n        let result = ocr.ocrBitmap(bitmap, 20 * 1000, {});\n        logd(result)\n        if (result) {\n            logd(\"ocr结果-》 \" + JSON.stringify(result));\n            for (var i = 0; i < result.length; i++) {\n                var value = result[i];\n                logd(\"文字 : \" + value.label + \" x: \" + value.x + \" y: \" + value.y + \" width: \" + value.width + \" height: \" + value.height);\n            }\n        } else {\n            logw(\"未识别到结果\");\n        }\n        bitmap.recycle();\n        logd(\"耗时: \" + console.timeEnd(1) + \" ms\")\n        sleep(1000);\n        logd(\"ix = \" + ix)\n    }\n    //释放所有资源\n    ocr.releaseAll();\n}\n\nmain();",
  },
  {
    name: "Paddle",
    platform: "android",
    category: "ocr",
    signature: "ocr.Paddle()",
    description: "提示此方法不推荐",
    example: "function main() {\n    logd(\"isServiceOk \" + isServiceOk());\n    startEnv()\n    logd(\"isServiceOk \" + isServiceOk());\n    let request = image.requestScreenCapture(10000, 0);\n    if (!request) {\n        request = image.requestScreenCapture(10000, 0);\n    }\n    logd(\"申请截图结果... \" + request)\n    if (!request) {\n        loge(\"申请截图权限失败,检查是否开启后台弹出,悬浮框等权限\")\n        exit()\n    }\n    //申请完权限至少等1s(垃圾设备多加点)再截图,否则会截不到图\n    sleep(1000)\n\n    let paddleocr = {\n        \"type\": \"paddleocr\"\n    }\n\n    let easyedge = {\n        \"type\": \"easyedge\",\n    }\n\n    let inited = ocr.initOcr(paddleocr)\n    logd(\"初始化结果 -\" + inited);\n    if (!inited) {\n        loge(\"error : \" + ocr.getErrorMsg());\n        return;\n    }\n\n    let initServer = ocr.initOcrServer(5 * 1000);\n    logd(\"initServer \" + initServer);\n    if (!initServer) {\n        loge(\"initServer error : \" + ocr.getErrorMsg());\n        return;\n    }\n    ocr.setDaemonServer(true, 500);\n    for (var ix = 0; ix < 20; ix++) {\n\n        //读取一个bitmap\n        let bitmap = image.readBitmap(\"/sdcard/Screenshot_20210127_152932_com.huawei.android.lau.jpg\");\n        if (!bitmap) {\n            loge(\"读取图片失败\");\n            continue;\n        }\n        console.time(\"1\")\n        logd(\"start---ocr\");\n        // 对图片进行识别\n        let result = ocr.ocrBitmap(bitmap, 20 * 1000, {});\n        logd(result)\n        if (result) {\n            logd(\"ocr结果-》 \" + JSON.stringify(result));\n            for (var i = 0; i < result.length; i++) {\n                var value = result[i];\n                logd(\"文字 : \" + value.label + \" x: \" + value.x + \" y: \" + value.y + \" width: \" + value.width + \" height: \" + value.height);\n            }\n        } else {\n            logw(\"未识别到结果\");\n        }\n        bitmap.recycle();\n        logd(\"耗时: \" + console.timeEnd(1) + \" ms\")\n        sleep(1000);\n        logd(\"ix = \" + ix)\n    }\n    //释放所有资源\n    ocr.releaseAll();\n}\n\nmain();",
  },
  {
    name: "Tesseract",
    platform: "android",
    category: "ocr",
    signature: "ocr.Tesseract()",
    description: "Tesseract 例子 [低于9.17版本]",
    example: "function main() {\n    logd(\"isServiceOk \" + isServiceOk());\n    startEnv()\n    logd(\"isServiceOk \" + isServiceOk());\n    let request = image.requestScreenCapture(10000, 0);\n    if (!request) {\n        request = image.requestScreenCapture(10000, 0);\n    }\n    logd(\"申请截图结果... \" + request)\n    if (!request) {\n        loge(\"申请截图权限失败,检查是否开启后台弹出,悬浮框等权限\")\n        exit()\n    }\n    //申请完权限至少等1s(垃圾设备多加点)再截图,否则会截不到图\n    sleep(1000)\n\n    //Tesseract模块初始化参数\n    let tessInitMap = {\n        \"type\": \"tess\",\n        \"language\": \"chi_sim\",\n        \"debug\": true\n    }\n\n    let inited = ocr.initOcr(tessInitMap)\n    logd(\"初始化结果 -\" + inited);\n    if (!inited) {\n        loge(\"error : \" + ocr.getErrorMsg());\n        return;\n    }\n\n    //读取一个bitmap\n    let bitmap = image.readBitmap(\"/sdcard/a.png\");\n    if (!bitmap) {\n        loge(\"读取图片失败\");\n        return;\n    }\n    // 对图片进行识别\n    let result = ocr.ocrBitmap(bitmap, 20 * 1000, {});\n    if (result) {\n        logd(\"ocr结果-》 \" + JSON.stringify(result));\n        for (var i = 0; i < result.length; i++) {\n            var value = result[i];\n            logd(\"文字 : \" + value.label + \" x: \" + value.x + \" y: \" + value.y + \" width: \" + value.width + \" height: \" + value.height);\n        }\n    } else {\n        logw(\"未识别到结果\");\n    }\n\n    bitmap.recycle();\n    //释放所有资源\n    ocr.releaseAll();\n\n}\n\nmain();",
  },
  {
    name: "\u767e\u5ea6\u5728\u7ebfocr\u4f8b\u5b50-\u4f4e\u4e8e917\u7248\u672c",
    platform: "android",
    category: "ocr",
    signature: "ocr.百度在线ocr例子-低于917版本()",
    description: "百度在线OCR例子 [低于9.17版本]",
    example: "function main() {\n    logd(\"isServiceOk \" + isServiceOk());\n    startEnv()\n    logd(\"isServiceOk \" + isServiceOk());\n    let request = image.requestScreenCapture(10000, 0);\n    if (!request) {\n        request = image.requestScreenCapture(10000, 0);\n    }\n    logd(\"申请截图结果... \" + request)\n    if (!request) {\n        loge(\"申请截图权限失败,检查是否开启后台弹出,悬浮框等权限\")\n        exit()\n    }\n    //申请完权限至少等1s(垃圾设备多加点)再截图,否则会截不到图\n    sleep(1000)\n\n    let baiduOnlineInitMap = {\n        \"type\": \"baiduOnline\",\n        \"ak\": \"xx\",\n        \"sk\": \"xx\"\n    }\n\n    let inited = ocr.initOcr(baiduOnlineInitMap)\n    logd(\"初始化结果 -\" + inited);\n    if (!inited) {\n        loge(\"error : \" + ocr.getErrorMsg());\n        return;\n    }\n\n    //读取一个bitmap\n    let bitmap = image.readBitmap(\"/sdcard/a.png\");\n    if (!bitmap) {\n        loge(\"读取图片失败\");\n        return;\n    }\n    // URL 参数参见 ： https://ai.baidu.com/ai-doc/OCR/tk3h7y2aq\n    // 对图片进行识别\n    let result = ocr.ocrBitmap(bitmap, 20 * 1000, {\"url\": \"https://aip.baidubce.com/rest/2.0/ocr/v1/accurate\"});\n    if (result) {\n        logd(\"ocr结果-》 \" + JSON.stringify(result));\n        for (var i = 0; i < result.length; i++) {\n            var value = result[i];\n            logd(\"文字 : \" + value.label + \" x: \" + value.x + \" y: \" + value.y + \" width: \" + value.width + \" height: \" + value.height);\n        }\n    } else {\n        logw(\"未识别到结果 \" + ocr.getErrorMsg());\n    }\n\n    bitmap.recycle();\n    //释放所有资源\n    ocr.releaseAll();\n\n}\n\nmain();",
  },
  {
    name: "Mlkit",
    platform: "android",
    category: "ocr",
    signature: "ocr.Mlkit()",
    description: "Mlkit OCR例子 [低于9.17版本]",
    example: "function main() {\n    logd(\"isServiceOk \" + isServiceOk());\n    startEnv()\n    logd(\"isServiceOk \" + isServiceOk());\n    let request = image.requestScreenCapture(10000, 0);\n    if (!request) {\n        request = image.requestScreenCapture(10000, 0);\n    }\n    logd(\"申请截图结果... \" + request)\n    if (!request) {\n        loge(\"申请截图权限失败,检查是否开启后台弹出,悬浮框等权限\")\n        exit()\n    }\n    //申请完权限至少等1s(垃圾设备多加点)再截图,否则会截不到图\n    sleep(1000)\n\n    let mlkit = {\n        \"type\": \"mlkit\"\n    }\n\n    let inited = ocr.initOcr(mlkit)\n    logd(\"初始化结果 -\" + inited);\n    if (!inited) {\n        loge(\"error : \" + ocr.getErrorMsg());\n        return;\n    }\n    for (var ix = 0; ix < 20; ix++) {\n        let tmpImage = image.captureFullScreen();\n        // orz 参数代表是旋转角度，0 代表不旋转 90 代表向左旋转90度，还有180，270，360参数\n        let result = ocr.ocrImage(tmpImage, 20000, {\"orz\": 0});\n        logd(\"耗时 {}\", console.timeEnd(1))\n        if (result) {\n            for (let i = 0; i < result.length; i++) {\n                logd(JSON.stringify(result[i]))\n            }\n        }\n        image.recycle(tmpImage)\n    }\n    //释放所有资源\n    ocr.releaseAll();\n}\n\nmain();",
  },
  {
    name: "ocrLite",
    platform: "android",
    category: "ocr",
    signature: "ocr.ocrLite()",
    description: "ocrLite OCR例子 [低于9.17版本]",
    example: "function main() {\n    //let start = startEnv()\n    //logd(\"start {}\", start)\n\n    let s = image.requestScreenCapture(10000, 0);\n    logd(\"s {}\", s)\n\n    logd(\"初始化ocrLite\")\n\n    let m = {\n        \"type\": \"ocrLite\"\n    }\n    m = {\"type\": \"ocrLite\", \"numThread\": 1, \"padding\": 10, \"maxSideLen\": 0};\n    let iniit = ocr.initOcr(m);\n    logd(\"初始化o \" + iniit)\n    image.initOpenCV()\n    sleep(1000)\n    let id = thread.execAsync(function () {\n        while (true) {\n            sleep(1000)\n\n            let tmpImage = image.captureFullScreen();\n            logd(\"截图 tmpImage {}\", tmpImage)\n            let tt = image.binaryzation(tmpImage, 1, 100)\n            console.time(1)\n            let result = ocr.ocrImage(tt, 10000, {\"maxSideLen\": 1024});\n            if (result) {\n                for (let i = 0; i < result.length; i++) {\n                    logd(JSON.stringify(result[i]))\n                }\n            }\n            logd(\"耗时 {}\", console.timeEnd(1))\n            image.recycle(tt)\n            image.recycle(tmpImage)\n        }\n    })\n\n    logd(\"线程 thread id = {}\", id)\n\n    sleep(115 * 1000)\n    thread.cancelThread(id)\n    sleep(1000)\n    //restartScript(\"/sdcard/release.iec\", true, 3)\n}\n\nmain();",
  },
  // ===================== file =====================
  {
    name: "readFile",
    platform: "android",
    category: "file",
    signature: "file.readFile()",
    description: "file.readFile 读取为字符串",
    example: "function main() {\n    var data = file.readFile(\"/sdcard/test.txt\");\n    toast(data);\n}\n\nmain();",
  },
  {
    name: "deleteLine",
    platform: "android",
    category: "file",
    signature: "file.deleteLine()",
    description: "file.deleteLine 删除文件某一行",
    example: "function main() {\n    //删除包含 时间 字符串的行\n    let r = file.deleteLine(\"/sdcard/a.txt\", -1, \"时间\");\n    logd(\"r \" + r);\n    //删除第3行\n    r = file.deleteLine(\"/sdcard/a.txt\", 3, null);\n    logd(\"r \" + r);\n}\n\nmain();",
  },
  {
    name: "listDir",
    platform: "android",
    category: "file",
    signature: "file.listDir()",
    description: "file.listDir 列出所有文件",
    example: "function main() {\n    var data = file.listDir(\"/sdcard/\");\n    for (var i = 0; i < data.length; i++) {\n        logd(\"path \" + data[i]);\n    }\n\n}\n\nmain();",
  },
  {
    name: "writeFile",
    platform: "android",
    category: "file",
    signature: "file.writeFile()",
    description: "file.writeFile 写入文件",
    example: "function main() {\n    var data = \"Test\";\n    file.writeFile(data, \"/sdcard/test.txt\");\n}\n\nmain();",
  },
  {
    name: "create",
    platform: "android",
    category: "file",
    signature: "file.create()",
    description: "file.create 创建",
    example: "function main() {\n    var create = file.create(\"/sdcard/test.txt\");\n    toast(create);\n}\n\nmain();",
  },
  {
    name: "mkdirs",
    platform: "android",
    category: "file",
    signature: "file.mkdirs()",
    description: "file.mkdirs 创建文件夹",
    example: "function main() {\n    var t = file.mkdirs(\"/sdcard/testdir/\");\n    toast(t);\n}\n\nmain();",
  },
  {
    name: "readAssets",
    platform: "android",
    category: "file",
    signature: "file.readAssets()",
    description: "file.readAssets 读取assets",
    example: "function main() {\n    var data = file.readAssets(\"data/test.txt\");\n    toast(data);\n}\n\nmain();",
  },
  {
    name: "deleteAllFile",
    platform: "android",
    category: "file",
    signature: "file.deleteAllFile()",
    description: "file.deleteAllFile 删除",
    example: "function main() {\n    // 删除文件\n    file.deleteAllFile(\"/sdcard/test.txt\");\n    // 删除文件夹\n    file.deleteAllFile(\"/sdcard/testdir/\");\n}\n\nmain();",
  },
  {
    name: "appendLine",
    platform: "android",
    category: "file",
    signature: "file.appendLine()",
    description: "file.appendLine 追加字符串",
    example: "function main() {\n    var data = \"sss\";\n    var t = file.appendLine(data, \"/sdcard/test.txt\");\n    toast(t);\n}\n\nmain();",
  },
  {
    name: "readLine",
    platform: "android",
    category: "file",
    signature: "file.readLine()",
    description: "file.readLine 读取一行",
    example: "function main() {\n    var t = file.readLine(\"/sdcard/test.txt\", 1);\n    toast(t);\n}\n\nmain();",
  },
  {
    name: "readAllLines",
    platform: "android",
    category: "file",
    signature: "file.readAllLines()",
    description: "file.readAllLines 读取所有行",
    example: "function main() {\n    var t = file.readAllLines(\"/sdcard/test.txt\");\n    toast(t);\n}\n\nmain();",
  },
  {
    name: "exists",
    platform: "android",
    category: "file",
    signature: "file.exists()",
    description: "file.exists 是否存在",
    example: "function main() {\n    var t = file.exists(\"/sdcard/testdir/\");\n    toast(t);\n}\n\nmain();",
  },
  {
    name: "copy",
    platform: "android",
    category: "file",
    signature: "file.copy()",
    description: "function main() {    var t = file.copy(\"/sdcard/a.png\", \"/sdcard/b.png\");    toast(t);}main();上一页悬浮窗函数下一页Storage存储函数说明file.readFile 读取为字符串file.deleteLine 删除文件某一行file.listDir 列出所有文件file.writeFile 写入文件file.create 创建file.mkdirs 创建文件夹file.readAssets 读取assetsfile.deleteAllFile 删除file.appendLine 追加字符串f...",
    example: "function main() {\n    var t = file.copy(\"/sdcard/a.png\", \"/sdcard/b.png\");\n    toast(t);\n}\n\nmain();",
  },
  // ===================== shell =====================
  {
    name: "installApp",
    platform: "android",
    category: "shell",
    signature: "shell.installApp()",
    description: "shell.installApp 安装APP",
    example: "function main() {\n    var result = shell.installApp(\"/sdcard/app.apk\");\n}\n\nmain();",
  },
  {
    name: "uninstallApp",
    platform: "android",
    category: "shell",
    signature: "shell.uninstallApp()",
    description: "shell.uninstallApp 卸载APP",
    example: "function main() {\n    var result = shell.uninstallApp(\"com.xx\");\n}\n\nmain();",
  },
  {
    name: "stopApp",
    platform: "android",
    category: "shell",
    signature: "shell.stopApp()",
    description: "shell.stopApp 停止APP",
    example: "function main() {\n    var result = shell.stopApp(\"com.xx\");\n}\n\nmain();",
  },
  {
    name: "execCommand",
    platform: "android",
    category: "shell",
    signature: "shell.execCommand()",
    description: "shell.execCommand 执行Shell命令",
    example: "function main() {\n    var result = shell.execCommand(\"pm install /sdcard/app.apk\");\n}\n\nmain();",
  },
  {
    name: "sudo",
    platform: "android",
    category: "shell",
    signature: "shell.sudo()",
    description: "shell.sudo 执行ROOT下命令",
    example: "function main() {\n    var result = shell.sudo(\"pm install /sdcard/app.apk\");\n}\n\nmain();",
  },
  {
    name: "su",
    platform: "android",
    category: "shell",
    signature: "shell.su()",
    description: "shell.su 请求授予root",
    example: "function main() {\n    var result = shell.su();\n}\n\nmain();",
  },
  {
    name: "execAgentCommand",
    platform: "android",
    category: "shell",
    signature: "shell.execAgentCommand()",
    description: "shell.execAgentCommand 代理模式下执行shell",
    example: "function main() {\n    var result = shell.execAgentCommand(\"pm install /sdcard/app.apk\");\n}\n\nmain();",
  },
  {
    name: "execAgentCommandEx",
    platform: "android",
    category: "shell",
    signature: "shell.execAgentCommandEx()",
    description: "shell.execAgentCommandEx 代理模式下执行shell",
    example: "function main() {\n    startEnv()\n\n    let cmd = \"ls /sdcard/\"\n    let d = agentEvent.execShellCommandEx(cmd)\n    if (d) {\n        for (var i = 0; i < d.length; i++) {\n            var value = d[i];\n            logd(value);\n\n        }\n    }\n}\n\nmain();",
  },
  {
    name: "addSuBin",
    platform: "android",
    category: "shell",
    signature: "shell.addSuBin()",
    description: "shell.addSuBin 新增root命\u0000\u0000令",
    example: "function main() {\n    shell.addSuBin(\"222ddd\")\n    //增加完成后 使用su请求一下root授权\n    var result = shell.su();\n}\n\nmain();",
  },
  {
    name: "execShizukuCommand",
    platform: "android",
    category: "shell",
    signature: "shell.execShizukuCommand()",
    description: "shell.execShizukuCommand 执行shell Shizuku命令",
    example: "function main() {\n    let cmd = \"ls /sdcard/\"\n    let d = shell.execShizukuCommand(cmd)\n    logd(d);\n}\n\nmain();",
  },
  {
    name: "isShizukuOk",
    platform: "android",
    category: "shell",
    signature: "shell.isShizukuOk()",
    description: "function main() {    let d = shell.isShizukuOk()    logd(d);}main();上一页工具函数下一页Sqlite命令函数说明shell.installApp 安装APPshell.uninstallApp 卸载APPshell.stopApp 停止APPshell.execCommand 执行Shell命令shell.sudo 执行ROOT下命令shell.su 请求授予rootshell.execAgentCommand 代理模式下执行shellshell.execAgentCommandEx 代理模式下执行shellshell....",
    example: "function main() {\n\n    let d = shell.isShizukuOk()\n    logd(d);\n\n}\n\nmain();",
  },
  // ===================== sqlite =====================
  {
    name: "connectOrCreateDb",
    platform: "android",
    category: "sqlite",
    signature: "sqlite.connectOrCreateDb()",
    description: "sqlite.connectOrCreateDb 连接数据库",
    example: "function main() {\n    var create = sqlite.connectOrCreateDb(\"/sdcard/test.db\");\n    logd(\"create db result：\" + create);\n}\n\nmain();",
  },
  {
    name: "createTable",
    platform: "android",
    category: "sqlite",
    signature: "sqlite.createTable()",
    description: "sqlite.createTable 创建表",
    example: "function main() {\n    var tableName = \"tbl_user\";\n    var columns = [\"name\", \"pwd\"];\n    var createTable = sqlite.createTable(tableName, columns);\n    logd(\"createTable result：\" + createTable);\n}\n\nmain();",
  },
  {
    name: "insert",
    platform: "android",
    category: "sqlite",
    signature: "sqlite.insert()",
    description: "sqlite.insert 插入数据",
    example: "function main() {\n    var tableName = \"tbl_user\";\n    var map = {\n        \"name\": \"我是名称\",\n        \"pwd\": \"我是密码\"\n    };\n    var insert = sqlite.insert(tableName, map);\n    logd(\"insert result：\" + insert);\n}\n\nmain();",
  },
  {
    name: "update",
    platform: "android",
    category: "sqlite",
    signature: "sqlite.update()",
    description: "sqlite.update 更新数据",
    example: "function main() {\n    var tableName = \"tbl_user\";\n    var map = {\n        \"name\": \"我是名称22\"\n    };\n    var where = \"id>3\";\n    var update = sqlite.update(tableName, map, where);\n    logd(\"update result：\" + update);\n}\n\nmain();",
  },
  {
    name: "query",
    platform: "android",
    category: "sqlite",
    signature: "sqlite.query()",
    description: "sqlite.query 查询数据",
    example: "function main() {\n    var tableName = \"tbl_user\";\n    var sql = \"select * from \" + tableName;\n    var data = sqlite.query(sql);\n    logd(\"data result：\" + JSON.stringify(data));\n}\n\nmain();",
  },
  {
    name: "delete",
    platform: "android",
    category: "sqlite",
    signature: "sqlite.delete()",
    description: "sqlite.delete 删除数据",
    example: "function main() {\n    var tableName = \"tbl_user\";\n    var sql = \"delete from \" + tableName + \" where id>3;\";\n    var result = sqlite.delete(sql);\n    logd(\"delete result：\" + result);\n}\n\nmain();",
  },
  {
    name: "execSql",
    platform: "android",
    category: "sqlite",
    signature: "sqlite.execSql()",
    description: "sqlite.execSql 执行sql",
    example: "function main() {\n    var tableName = \"tbl_user\";\n    var sql = \"delete from \" + tableName + \" where id>3;\";\n    var result = sqlite.execSql(sql);\n    logd(\"execSql result：\" + result);\n}\n\nmain();",
  },
  {
    name: "dropDatabase",
    platform: "android",
    category: "sqlite",
    signature: "sqlite.dropDatabase()",
    description: "sqlite.dropDatabase 删除数据库",
    example: "function main() {\n    var result = sqlite.dropDatabase();\n    logd(\"dropDatabase result：\" + result);\n}\n\nmain();",
  },
  {
    name: "dropTable",
    platform: "android",
    category: "sqlite",
    signature: "sqlite.dropTable()",
    description: "sqlite.dropTable 删除表",
    example: "function main() {\n    var tableName = \"tbl_user\";\n    var result = sqlite.dropTable(tableName);\n    logd(\"dropTable result：\" + result);\n}\n\nmain();",
  },
  {
    name: "close",
    platform: "android",
    category: "sqlite",
    signature: "sqlite.close()",
    description: "sqlite.close 关闭数据库链接，释放资源",
    example: "function main() {\n    var result = sqlite.close();\n    logd(\"close result：\" + result);\n}\n\nmain();",
  },
  {
    name: "getErrorMsg",
    platform: "android",
    category: "sqlite",
    signature: "sqlite.getErrorMsg()",
    description: "function main() {    var tableName = \"tbl_user\";    var result = sqlite.dropTable(tableName);    logd(\"dropTable result：\" + result);    var result = sqlite.getErrorMsg();    logd(\"getErrorMsg result：\" + result);}main();上一页Shell命令函数下一页JDBC MySQL函数说明sqlite.connectOrCreateDb 连接数据库sqlite.createTable ...",
    example: "function main() {\n    var tableName = \"tbl_user\";\n    var result = sqlite.dropTable(tableName);\n    logd(\"dropTable result：\" + result);\n    var result = sqlite.getErrorMsg();\n    logd(\"getErrorMsg result：\" + result);\n}\n\nmain();",
  },
  // ===================== util =====================
  {
    name: "openApp",
    platform: "android",
    category: "util",
    signature: "utils.openApp()",
    description: "utils.openApp 打开APP",
    example: "function main() {\n    utils.openApp(\"com.xx\");\n}\n\nmain();",
  },
  {
    name: "getStartAppCmd",
    platform: "android",
    category: "util",
    signature: "utils.getStartAppCmd()",
    description: "utils.getStartAppCmd 命令打开app",
    example: "function main() {\n    startEnv()\n    let pkg = \"com.youdao.translator\";\n    //获取命令\n    let d = utils.getStartAppCmd(pkg);\n\n    logd(d);\n\n    //激活命令执行\n    //let result = shell.execCommand(d);\n    //logd(\"result =\"+result);\n    //root方式执行\n    //var result = shell.sudo(d);\n    //logd(\"result =\"+result);\n    //开启代理服务，使用代理服务执行命令\n    var result = shell.execAgentCommand(d);\n    logd(\"result =\" + result);\n}\n\nmain();",
  },
  {
    name: "openAppByName",
    platform: "android",
    category: "util",
    signature: "utils.openAppByName()",
    description: "utils.openAppByName 打开APP(应用名称)",
    example: "function main() {\n    utils.openAppByName(\"xx\");\n}\n\nmain();",
  },
  {
    name: "openActivity",
    platform: "android",
    category: "util",
    signature: "utils.openActivity()",
    description: "utils.openActivity 打开activity",
    example: "function main() {\n    //打开安装包界面\n    var m = {\n        \"action\": \"android.intent.action.VIEW\",\n        \"uri\": \"file:///sdcard/a.apk\",\n        \"type\": \"application/vnd.android.package-archive\"\n    };\n    var x = utils.openActivity(m);\n    logd(\"x \" + x);\n\n    var map = {\n        \"uri\": \"xx://xx/live/6701887916223941379\",\n    };\n    utils.openActivity(map);\n}\n\nmain();",
  },
  {
    name: "getStartActivityCmd",
    platform: "android",
    category: "util",
    signature: "utils.getStartActivityCmd()",
    description: "utils.getStartActivityCmd命令 打开activity",
    example: "function main() {\n    startEnv()\n    //let pkg = \"com.youdao.translator\";\n\n    let map1 = {\n        \"action\": \"android.intent.action.VIEW\",\n        \"pkg\": \"com.youdao.translator\"\n    }\n    //uri演示打电话 这里的uri可以是其他\n    let map2 = {\n        \"action\": \"android.intent.action.CALL\",\n        \"uri\": \"tel:1000\"\n    }\n    //获取命令\n    let d = utils.getStartActivityCmd(map1);\n\n    logd(d);\n\n    //激活命令执行\n    //let result = shell.execCommand(d);\n    //logd(\"result =\"+result);\n    //root方式执行\n    //var result = shell.sudo(d);\n    //logd(\"result =\"+result);\n    //开启代理服务，使用代理服务执行命令\n    var result = shell.execAgentCommand(d);\n    logd(\"result =\" + result);\n\n}\n\nmain()",
  },
  {
    name: "openIntentAction",
    platform: "android",
    category: "util",
    signature: "utils.openIntentAction()",
    description: "utils.openIntentAction 通过Action打开某个界面",
    example: "function main() {\n    utils.openIntentAction(\"android.settings.ACCESSIBILITY_SETTINGS\");\n}\n\nmain();",
  },
  {
    name: "insertImageToAlbum",
    platform: "android",
    category: "util",
    signature: "utils.insertImageToAlbum()",
    description: "utils.insertImageToAlbum 图片插入相册",
    example: "function main() {\n    utils.insertImageToAlbum(\"/sdcard/DCIM/a.png\");\n}\n\nmain();",
  },
  {
    name: "insertVideoToAlbum",
    platform: "android",
    category: "util",
    signature: "utils.insertVideoToAlbum()",
    description: "utils.insertVideoToAlbum 视频插入相册",
    example: "function main() {\n    utils.insertVideoToAlbum(\"/sdcard/DCIM/a.mp4\");\n}\n\nmain();",
  },
  {
    name: "zip",
    platform: "android",
    category: "util",
    signature: "utils.zip()",
    description: "utils.zip 压缩",
    example: "function main() {\n    zipFile = \"/sdcard/a.zip\"\n    //压缩文件\n    let passwd = \"123\";\n    let files = [\"/sdcard/test.json\", \"/sdcard/gifshow\"]\n    let re = utils.zip(zipFile, passwd, files);\n    logd(\"压缩结果: \" + re);\n\n    let ure = utils.unzip(zipFile, passwd, \"/sdcard/test111/\");\n    logd(\"解压结果: \" + ure);\n\n    let data = utils.readFileInZip(\"/sdcard/a.zip\", passwd, \"test.json\")\n    logd(\"读取数据结果: \" + data);\n}\n\nmain()",
  },
  {
    name: "unzip",
    platform: "android",
    category: "util",
    signature: "utils.unzip()",
    description: "utils.unzip 解压",
    example: "function main() {\n\n    zipFile = \"/sdcard/a.zip\"\n    //压缩文件\n    let passwd = \"123\";\n    let files = [\"/sdcard/test.json\", \"/sdcard/gifshow\"]\n    let re = utils.zip(zipFile, passwd, files);\n    logd(\"压缩结果: \" + re);\n\n    let ure = utils.unzip(zipFile, passwd, \"/sdcard/test111/\");\n    logd(\"解压结果: \" + ure);\n\n    let data = utils.readFileInZip(\"/sdcard/a.zip\", passwd, \"test.json\")\n    logd(\"读取数据结果: \" + data);\n}\n\nmain()",
  },
  {
    name: "unzipWithEncode",
    platform: "android",
    category: "util",
    signature: "utils.unzipWithEncode()",
    description: "utils.unzipWithEncode 解压带编码",
    example: "function main() {\n\n    let zipFile = \"/sdcard/a.zip\"\n    let ure = utils.unzipWithEncode(zipFile, \"\", \"/sdcard/test111/\", \"GBK\");\n    logd(\"解压结果: \" + ure);\n}\n\nmain()",
  },
  {
    name: "readFileInZip",
    platform: "android",
    category: "util",
    signature: "utils.readFileInZip()",
    description: "utils.readFileInZip ZIP中读取",
    example: "function main() {\n\n    zipFile = \"/sdcard/a.zip\"\n    //压缩文件\n    let passwd = \"123\";\n    let files = [\"/sdcard/test.json\", \"/sdcard/gifshow\"]\n    let re = utils.zip(zipFile, passwd, files);\n    logd(\"压缩结果: \" + re);\n\n    let ure = utils.unzip(zipFile, passwd, \"/sdcard/test111/\");\n    logd(\"解压结果: \" + ure);\n\n    let data = utils.readFileInZip(\"/sdcard/a.zip\", passwd, \"test.json\")\n    logd(\"读取数据结果: \" + data);\n}\n\nmain()",
  },
  {
    name: "encodeDecoder.aesEncrypt",
    platform: "android",
    category: "util",
    signature: "utils.encodeDecoder.aesEncrypt()",
    description: "encodeDecoder.aesEncrypt AES加密",
    example: "function main() {\n    let dat = \"我是数据\"\n    let pwd = \"12356783\";\n    let d = encodeDecoder.aesEncrypt(dat, pwd)\n    logd(\"加密后的数据-\" + d);\n    let dd = encodeDecoder.aesDecrypt(d, pwd)\n    logd(\"解密后的数据-\" + dd);\n}\n\nmain();",
  },
  {
    name: "encodeDecoder.aesDecrypt",
    platform: "android",
    category: "util",
    signature: "utils.encodeDecoder.aesDecrypt()",
    description: "encodeDecoder.aesDecrypt AES解密",
    example: "function main() {\n    let dat = \"我是数据\"\n    let pwd = \"12356783\";\n    let d = encodeDecoder.aesEncrypt(dat, pwd)\n    logd(\"加密后的数据-\" + d);\n    let dd = encodeDecoder.aesDecrypt(d, pwd)\n    logd(\"解密后的数据-\" + dd);\n}\n\nmain();",
  },
  {
    name: "encodeDecoder.desEncrypt",
    platform: "android",
    category: "util",
    signature: "utils.encodeDecoder.desEncrypt()",
    description: "encodeDecoder.desEncrypt DES加密",
    example: "function main() {\n    let dat = \"我是数据\"\n    let pwd = \"12356783\";\n    let d = encodeDecoder.desEncrypt(dat, pwd)\n    logd(\"加密后的数据-\" + d);\n    let dd = encodeDecoder.desDecrypt(d, pwd)\n    logd(\"解密后的数据-\" + dd);\n}\n\nmain();",
  },
  {
    name: "encodeDecoder.desDecrypt",
    platform: "android",
    category: "util",
    signature: "utils.encodeDecoder.desDecrypt()",
    description: "encodeDecoder.desDecrypt DES解密",
    example: "function main() {\n    let dat = \"我是数据\"\n    let pwd = \"12356783\";\n    let d = encodeDecoder.desEncrypt(dat, pwd)\n    logd(\"加密后的数据-\" + d);\n    let dd = encodeDecoder.desDecrypt(d, pwd)\n    logd(\"解密后的数据-\" + dd);\n}\n\nmain();",
  },
  {
    name: "encodeDecoder.des3Encrypt",
    platform: "android",
    category: "util",
    signature: "utils.encodeDecoder.des3Encrypt()",
    description: "encodeDecoder.des3Encrypt 3DES加密",
    example: "function main() {\n    let dat = \"我是数据\"\n    let pwd = \"12356783\";\n    let d = encodeDecoder.des3Encrypt(dat, pwd)\n    logd(\"加密后的数据-\" + d);\n    let dd = encodeDecoder.des3Decrypt(d, pwd)\n    logd(\"解密后的数据-\" + dd);\n}\n\nmain();",
  },
  {
    name: "encodeDecoder.des3Decrypt",
    platform: "android",
    category: "util",
    signature: "utils.encodeDecoder.des3Decrypt()",
    description: "encodeDecoder.des3Decrypt 3DES解密",
    example: "function main() {\n    let dat = \"我是数据\"\n    let pwd = \"12356783\";\n    let d = encodeDecoder.des3Encrypt(dat, pwd)\n    logd(\"加密后的数据-\" + d);\n    let dd = encodeDecoder.des3Decrypt(d, pwd)\n    logd(\"解密后的数据-\" + dd);\n}\n\nmain();",
  },
  {
    name: "encodeDecoder.getErrorMsg",
    platform: "android",
    category: "util",
    signature: "utils.encodeDecoder.getErrorMsg()",
    description: "encodeDecoder.getErrorMsg 获取加解密错误信息",
    example: "function main() {\n    let dat = \"我是数据\"\n    let pwd = \"12356783\";\n    let d = encodeDecoder.des3Encrypt(dat, pwd)\n    logd(\"加密后的数据-\" + d);\n    let dd = encodeDecoder.des3Decrypt(d, pwd)\n    logd(\"解密后的数据-\" + dd);\n    logd(\"getErrorMsg -\" + encodeDecoder.getErrorMsg());\n}\n\nmain();",
  },
  {
    name: "createQRCode",
    platform: "android",
    category: "util",
    signature: "utils.createQRCode()",
    description: "utils.createQRCode 生成一个二维码",
    example: "function main() {\n\n    //生成一个二维码bitmap 带logo的\n    let bot = utils.createQRCode(\"我是大佬的弟弟\", 1000, 1000, image.readBitmap(\"/sdcard/yyb2.png\"));\n    logd(\"bot \" + bot);\n    //保存的到文件\n    let saved = image.saveBitmap(bot, \"png\", 100, \"/sdcard/tmp.png\");\n    logd(\"saved \" + saved);\n    //回收掉防止内存暴涨\n    if (bot) {\n        bot.recycle()\n    }\n    //扫描二维码\n    let bitmap = image.readBitmap(\"/sdcard/tmp.png\")\n    let data = utils.decodeQRCode(bitmap);\n    logd(\"data \" + data);\n    //回收掉防止内存暴涨\n    if (bitmap) {\n        bitmap.recycle()\n    }\n}\n\nmain();",
  },
  {
    name: "decodeQRCode",
    platform: "android",
    category: "util",
    signature: "utils.decodeQRCode()",
    description: "utils.decodeQRCode 解析一个二维码",
    example: "function main() {\n\n    //生成一个二维码bitmap 带logo的\n    let bot = utils.createQRCode(\"我是大佬的弟弟\", 1000, 1000, image.readBitmap(\"/sdcard/yyb2.png\"));\n    logd(\"bot \" + bot);\n    //保存的到文件\n    let saved = image.saveBitmap(bot, \"png\", 100, \"/sdcard/tmp.png\");\n    logd(\"saved \" + saved);\n    //回收掉防止内存暴涨\n    if (bot) {\n        bot.recycle()\n    }\n    //扫描二维码\n    let bitmap = image.readBitmap(\"/sdcard/tmp.png\")\n    let data = utils.decodeQRCode(bitmap);\n    logd(\"data \" + data);\n    //回收掉防止内存暴涨\n    if (bitmap) {\n        bitmap.recycle()\n    }\n}\n\nmain();",
  },
  {
    name: "readConfigInt",
    platform: "android",
    category: "util",
    signature: "utils.readConfigInt()",
    description: "utils.readConfigInt 读取整型配置",
    example: "function main() {\n    var testData = utils.readConfigInt(jsonObject, \"test_key\");\n}\n\nmain();",
  },
  {
    name: "readJSONString",
    platform: "android",
    category: "util",
    signature: "utils.readJSONString()",
    description: "utils.readJSONString 读取JSON中字符串",
    example: "function main() {\n    var testData = utils.readConfigString(jsonObject, \"test_key\");\n}\n\nmain();",
  },
  {
    name: "isObjectNull",
    platform: "android",
    category: "util",
    signature: "utils.isObjectNull()",
    description: "utils.isObjectNull 判断一个对象为空",
    example: "function main() {\n    var isNull = utils.isObjectNull(\"test_key\");\n}\n\nmain();",
  },
  {
    name: "isObjectNotNull",
    platform: "android",
    category: "util",
    signature: "utils.isObjectNotNull()",
    description: "utils.isObjectNotNull 判断一个对象不为空",
    example: "function main() {\n    var isNull = utils.isObjectNotNull(\"test_key\");\n}\n\nmain();",
  },
  {
    name: "getRatio",
    platform: "android",
    category: "util",
    signature: "utils.getRatio()",
    description: "utils.getRatio 取得比例",
    example: "function main() {\n    var ratio = utils.getRatio(20)\n    toast(ratio);\n}\n\nmain();",
  },
  {
    name: "getRangeInt",
    platform: "android",
    category: "util",
    signature: "utils.getRangeInt()",
    description: "utils.getRangeInt 取得某个范围的随机值",
    example: "function main() {\n    var value = utils.getRangeInt(1, 100);\n    toast(value);\n}\n\nmain();",
  },
  {
    name: "isTrue",
    platform: "android",
    category: "util",
    signature: "utils.isTrue()",
    description: "utils.isTrue 判断布尔型对象是否为真",
    example: "function main() {\n    var value = utils.isTrue(true);\n}\n\nmain();",
  },
  {
    name: "fileMd5",
    platform: "android",
    category: "util",
    signature: "utils.fileMd5()",
    description: "utils.fileMd5 文件的MD5",
    example: "function main() {\n    var md5 = utils.fileMd5(\"/sdcard/a.txt\");\n}\n\nmain();",
  },
  {
    name: "dataMd5",
    platform: "android",
    category: "util",
    signature: "utils.dataMd5()",
    description: "utils.dataMd5 数据计算出来的MD5",
    example: "function main() {\n    var md5 = utils.dataMd5(\"data\");\n}\n\nmain();",
  },
  {
    name: "randomInt",
    platform: "android",
    category: "util",
    signature: "utils.randomInt()",
    description: "utils.randomInt 随机整型数据",
    example: "function main() {\n    var r = utils.randomInt(2);\n}\n\nmain();",
  },
  {
    name: "randomCharNumber",
    platform: "android",
    category: "util",
    signature: "utils.randomCharNumber()",
    description: "utils.randomCharNumber 取得随机的数字和字母",
    example: "function main() {\n    var r = utils.randomCharNumber(2);\n}\n\nmain();",
  },
  {
    name: "getApkPkgName",
    platform: "android",
    category: "util",
    signature: "utils.getApkPkgName()",
    description: "utils.getApkPkgName 取得App文件的包名",
    example: "function main() {\n    var pkgName = utils.getApkPkgName(\"/sdcard/app.apk\");\n}\n\nmain();",
  },
  {
    name: "isAppExist",
    platform: "android",
    category: "util",
    signature: "utils.isAppExist()",
    description: "utils.isAppExist App是否已经安装",
    example: "function main() {\n    var result = utils.isAppExist(\"com.xx\");\n}\n\nmain();",
  },
  {
    name: "getAppVersionCode",
    platform: "android",
    category: "util",
    signature: "utils.getAppVersionCode()",
    description: "utils.getAppVersionCode 程序的版本标示",
    example: "function main() {\n    var versionCode = utils.getAppVersionCode(\"com.xx\");\n}\n\nmain();",
  },
  {
    name: "getAppVersionName",
    platform: "android",
    category: "util",
    signature: "utils.getAppVersionName()",
    description: "utils.getAppVersionName 版本字符串标示",
    example: "function main() {\n    var r = utils.getAppVersionName(\"com.xx\");\n}\n\nmain();",
  },
  {
    name: "setClipboardText",
    platform: "android",
    category: "util",
    signature: "utils.setClipboardText()",
    description: "utils.setClipboardText 设置剪贴板文本",
    example: "function main() {\n    var r = utils.setClipboardText(\"com.xx\");\n    toast(\"设置结果:\" + r);\n}\n\nmain();",
  },
  {
    name: "getClipboardText",
    platform: "android",
    category: "util",
    signature: "utils.getClipboardText()",
    description: "utils.getClipboardText 读取剪贴板文本",
    example: "function main() {\n    var r = utils.getClipboardText();\n    toast(\"读取结果:\" + r);\n}\n\nmain();",
  },
  {
    name: "playMp3",
    platform: "android",
    category: "util",
    signature: "utils.playMp3()",
    description: "utils.playMp3 播放MP3音乐",
    example: "function main() {\n    //如果想把MP3内置到脚本中,将MP3文件放到res目录下\n    saveResToFile(\"a.mp3\", \"/sdcard/a.mp3\")\n    let d = utils.playMp3(\"/sdcard/a.mp3\", true)\n    logd(\"dd \" + d)\n    //给点延迟,等音乐开始真正播放\n    sleep(30 * 1000)\n    utils.stopMp3()\n    logd(\"stop play \")\n}\n\nmain();",
  },
  {
    name: "playMp3WaitEnd",
    platform: "android",
    category: "util",
    signature: "utils.playMp3WaitEnd()",
    description: "utils.playMp3WaitEnd 同步播放MP3音乐",
    example: "function main() {\n    //如果想把MP3内置到脚本中,将MP3文件放到res目录下\n    saveResToFile(\"a.mp3\", \"/sdcard/a.mp3\")\n    let d = utils.playMp3WaitEnd(\"/sdcard/a.mp3\", false)\n    logd(\"dd \" + d)\n}\n\nmain();",
  },
  {
    name: "stopMp3",
    platform: "android",
    category: "util",
    signature: "utils.stopMp3()",
    description: "function main() {    //如果想把MP3内置到脚本中,将MP3文件放到res目录下    saveResToFile(\"a.mp3\", \"/sdcard/a.mp3\")    let d = utils.playMp3(\"/sdcard/a.mp3\", true)    logd(\"dd \" + d)    //给点延迟,等音乐开始真正播放    sleep(30 * 1000)    utils.stopMp3()    logd(\"stop play \")}main();上一页线程函数下一页Shell命令函数说明打开App相关utils.openApp 打开APP...",
    example: "function main() {\n    //如果想把MP3内置到脚本中,将MP3文件放到res目录下\n    saveResToFile(\"a.mp3\", \"/sdcard/a.mp3\")\n    let d = utils.playMp3(\"/sdcard/a.mp3\", true)\n    logd(\"dd \" + d)\n    //给点延迟,等音乐开始真正播放\n    sleep(30 * 1000)\n    utils.stopMp3()\n    logd(\"stop play \")\n}\n\nmain();",
  },
  // ===================== storage =====================
  {
    name: "storages.create",
    platform: "android",
    category: "storage",
    signature: "storage.storages.create()",
    description: "storages.create 创建存储",
    example: "function main() {\n    let storage = storages.create(\"123\");\n    logd(storage);\n}\n\nmain();",
  },
  {
    name: "keys",
    platform: "android",
    category: "storage",
    signature: "storage.keys()",
    description: "storage.keys 所有key获取",
    example: "function main() {\n    let storage = storages.create(\"123\");\n    logd(storage);\n    //存储数据\n    let r = storage.putString(\"key\", \"sdfasfd\");\n    logd(r);\n    logd(storage.keys());\n}\n\nmain();",
  },
  {
    name: "all",
    platform: "android",
    category: "storage",
    signature: "storage.all()",
    description: "storage.all 获取所有的key和值",
    example: "function main() {\n    let storage = storages.create(\"123\");\n    logd(storage);\n    //存储数据\n    let r = storage.putString(\"key\", \"sdfasfd\");\n    logd(r);\n    logd(storage.all());\n}\n\nmain();",
  },
  {
    name: "putString",
    platform: "android",
    category: "storage",
    signature: "storage.putString()",
    description: "storage.putString 存储字符串",
    example: "function main() {\n    let storage = storages.create(\"123\");\n    logd(storage);\n    //存储数据\n    let r = storage.putString(\"key\", \"sdfasfd\");\n    logd(r);\n    //获取数据,暂时有bug,需 +\"\"\n    logd(storage.getString(\"key\", \"\") + \"\");\n}\n\nmain();",
  },
  {
    name: "putInt",
    platform: "android",
    category: "storage",
    signature: "storage.putInt()",
    description: "storage.putInt 存储整型数据",
    example: "function main() {\n    let storage = storages.create(\"123\");\n    logd(storage);\n    //存储数据\n    let r = storage.putInt(\"key\", 1);\n    logd(r);\n    //获取数据\n    logd(storage.getInt(\"key\", 0));\n}\n\nmain();",
  },
  {
    name: "putBoolean",
    platform: "android",
    category: "storage",
    signature: "storage.putBoolean()",
    description: "storage.putBoolean 存储布尔型数据",
    example: "function main() {\n    let storage = storages.create(\"123\");\n    logd(storage);\n    //存储数据\n    let r = storage.putBoolean(\"key\", true);\n    logd(r);\n    //获取数据\n    logd(storage.getBoolean(\"key\", false));\n}\n\nmain();",
  },
  {
    name: "putFloat",
    platform: "android",
    category: "storage",
    signature: "storage.putFloat()",
    description: "storage.putFloat 存储浮点型数据",
    example: "function main() {\n    let storage = storages.create(\"123\");\n    logd(storage);\n    //存储数据\n    let r = storage.putFloat(\"key\", 1.0);\n    logd(r);\n    //获取数据\n    logd(storage.getFloat(\"key\", 0));\n}\n\nmain();",
  },
  {
    name: "putEncrypt",
    platform: "android",
    category: "storage",
    signature: "storage.putEncrypt()",
    description: "storage.putEncrypt 存储并加密字符串",
    example: "function main() {\n    let storage = storages.create(\"123\");\n    logd(storage);\n    //存储数据\n    let r = storage.putEncrypt(\"key\", \"3232\");\n    logd(r);\n    //获取数据\n    logd(storage.getDecryptString(\"key\") + \"\");\n}\n\nmain();",
  },
  {
    name: "getString",
    platform: "android",
    category: "storage",
    signature: "storage.getString()",
    description: "storage.getString 获取字符串数据",
    example: "function main() {\n    let storage = storages.create(\"123\");\n    logd(storage);\n    //存储数据\n    let r = storage.putString(\"key\", \"sdfasfd\");\n    logd(r);\n    //获取数据,暂时有bug,需 +\"\"\n    logd(storage.getString(\"key\", \"\") + \"\");\n}\n\nmain();",
  },
  {
    name: "getInt",
    platform: "android",
    category: "storage",
    signature: "storage.getInt()",
    description: "storage.getInt 获取整型数据",
    example: "function main() {\n    let storage = storages.create(\"123\");\n    logd(storage);\n    //存储数据\n    let r = storage.putInt(\"key\", 1);\n    logd(r);\n    //获取数据\n    logd(storage.getInt(\"key\", 0));\n}\n\nmain();",
  },
  {
    name: "getBoolean",
    platform: "android",
    category: "storage",
    signature: "storage.getBoolean()",
    description: "storage.getBoolean 获取布尔型数据",
    example: "function main() {\n    let storage = storages.create(\"123\");\n    logd(storage);\n    //存储数据\n    let r = storage.putBoolean(\"key\", true);\n    logd(r);\n    //获取数据\n    logd(storage.getBoolean(\"key\", false));\n}\n\nmain();",
  },
  {
    name: "getFloat",
    platform: "android",
    category: "storage",
    signature: "storage.getFloat()",
    description: "storage.getFloat 获取浮点型数据",
    example: "function main() {\n    let storage = storages.create(\"123\");\n    logd(storage);\n    //存储数据\n    let r = storage.putFloat(\"key\", 1.0);\n    logd(r);\n    //获取数据\n    logd(storage.getFloat(\"key\", 0));\n}\n\nmain();",
  },
  {
    name: "getDecryptString",
    platform: "android",
    category: "storage",
    signature: "storage.getDecryptString()",
    description: "storage.getDecryptString 获取解密字符串",
    example: "function main() {\n    let storage = storages.create(\"123\");\n    logd(storage);\n    //存储数据\n    let r = storage.putEncrypt(\"key\", \"3232\");\n    logd(r);\n    //获取数据\n    logd(storage.getDecryptString(\"key\") + \"\");\n}\n\nmain();",
  },
  {
    name: "clear",
    platform: "android",
    category: "storage",
    signature: "storage.clear()",
    description: "storage.clear 清空存储",
    example: "function main() {\n    let storage = storages.create(\"123\");\n    logd(storage);\n    //存储数据\n    let r = storage.putEncrypt(\"key\", \"3232\");\n    logd(r);\n    //获取数据\n    logd(storage.getDecryptString(\"key\") + \"\");\n    logd(storage.clear());\n    logd(storage.getDecryptString(\"key\") + \"\");\n}\n\nmain();",
  },
  {
    name: "remove",
    platform: "android",
    category: "storage",
    signature: "storage.remove()",
    description: "storage.remove 移出key对应的值",
    example: "function main() {\n    let storage = storages.create(\"123\");\n    logd(storage);\n    //存储数据\n    let r = storage.putEncrypt(\"key\", \"3232\");\n    logd(r);\n    //获取数据\n    logd(storage.getDecryptString(\"key\")) + \"\";\n    logd(storage.remove(\"key\"));\n    logd(storage.getDecryptString(\"key\") + \"\");\n}\n\nmain();",
  },
  {
    name: "contains",
    platform: "android",
    category: "storage",
    signature: "storage.contains()",
    description: "function main() {    let storage = storages.create(\"123\");    logd(storage);    //存储数据    let r = storage.putEncrypt(\"key\", \"3232\");    logd(r);    //获取数据    logd(storage.getDecryptString(\"key\") + \"\");    logd(storage.contains(\"key\"));    logd(storage.getDecryptString(\"key\") + \"\");}main();上一页文件函数...",
    example: "function main() {\n    let storage = storages.create(\"123\");\n    logd(storage);\n    //存储数据\n    let r = storage.putEncrypt(\"key\", \"3232\");\n    logd(r);\n    //获取数据\n    logd(storage.getDecryptString(\"key\") + \"\");\n    logd(storage.contains(\"key\"));\n    logd(storage.getDecryptString(\"key\") + \"\");\n}\n\nmain();",
  },
  // ===================== thread =====================
  {
    name: "setTimeout",
    platform: "android",
    category: "thread",
    signature: "thread.setTimeout()",
    description: "setTimeout 延迟执行",
    example: "function main() {\n    var t = setTimeout(function () {\n        toast(\"我是一秒后执行的代码\");\n    }, 1000);\n    //模拟脚本运行中\n    while (true) {\n        sleep(1000)\n    }\n}\n\nmain();",
  },
  {
    name: "cancelTimeout",
    platform: "android",
    category: "thread",
    signature: "thread.cancelTimeout()",
    description: "cancelTimeout 取消延迟执行",
    example: "function main() {\n    var t = setTimeout(function () {\n        toast(\"我是一秒后执行的代码\");\n    }, 1000);\n    //取消就不会执行了\n    cancelTimeout(t);\n}\n\nmain();",
  },
  {
    name: "setInterval",
    platform: "android",
    category: "thread",
    signature: "thread.setInterval()",
    description: "setInterval 周期执行",
    example: "function main() {\n    var t = setInterval(function () {\n        toast(\"我是每隔一秒执行的代码\");\n    }, 1000);\n    //模拟脚本运行中\n    while (true) {\n        sleep(1000)\n    }\n}\n\nmain();",
  },
  {
    name: "cancelInterval",
    platform: "android",
    category: "thread",
    signature: "thread.cancelInterval()",
    description: "cancelInterval 取消周期执行",
    example: "function main() {\n    var t = setInterval(function () {\n        toast(\"我是每隔一秒执行的代码\");\n    }, 1000);\n    cancelInterval(t);\n}\n\nmain();",
  },
  {
    name: "execSync",
    platform: "android",
    category: "thread",
    signature: "thread.execSync()",
    description: "execSync 同步执行",
    example: "function main() {\n    execSync(function () {\n        logd(\"我是同步执行的代码\");\n    }, 1000);\n}\n\nmain();",
  },
  {
    name: "stopAll",
    platform: "android",
    category: "thread",
    signature: "thread.stopAll()",
    description: "thread.stopAll 停止",
    example: "function main() {\n    execSync(function () {\n        logd(\"我是同步执行的代码\");\n    }, 1000);\n    thread.stopAll();\n}\n\nmain();",
  },
  {
    name: "execAsync",
    platform: "android",
    category: "thread",
    signature: "thread.execAsync()",
    description: "thread.execAsync 异步执行",
    example: "function main() {\n    var tid = thread.execAsync(function () {\n        while (true) {\n            logd(\"我是异步执行的代码\");\n            sleep(1000);\n            if (thread.isCancelled(tid)) {\n                break;\n            }\n        }\n    });\n    logd(\"tid \" + tid);\n    //5s后取消线程\n    sleep(5000);\n    logd(\"取消线程 \" + tid);\n    thread.cancelThread(tid);\n    sleep(5000);\n    logd(\"结束 \");\n}\n\nmain();",
  },
  {
    name: "execSync",
    platform: "android",
    category: "thread",
    signature: "thread.execSync()",
    description: "thread.execSync 同步执行",
    example: "function main() {\n    thread.execSync(function () {\n        logd(\"我是同步执行的代码\");\n    }, 1000);\n}\n\nmain();",
  },
  {
    name: "cancelThread",
    platform: "android",
    category: "thread",
    signature: "thread.cancelThread()",
    description: "thread.cancelThread 取消线程",
    example: "function main() {\n    var tid = thread.execAsync(function () {\n        while (true) {\n            logd(\"我是异步执行的代码\");\n            sleep(1000);\n            if (thread.isCancelled(tid)) {\n                break;\n            }\n        }\n    });\n    logd(\"tid \" + tid);\n    //5s后取消线程\n    sleep(5000);\n    logd(\"取消线程 \" + tid);\n    thread.cancelThread(tid);\n    sleep(5000);\n    logd(\"结束 \");\n}\n\nmain();",
  },
  {
    name: "isCancelled",
    platform: "android",
    category: "thread",
    signature: "thread.isCancelled()",
    description: "function main() {    var tid = thread.execAsync(function () {        while (true) {            logd(\"我是异步执行的代码\");            sleep(1000);            if (thread.isCancelled(tid)) {                break;            }        }    });    logd(\"tid \" + tid);    //5s后取消线程    sleep(5000);    logd(\"取消线程 ...",
    example: "function main() {\n    var tid = thread.execAsync(function () {\n        while (true) {\n            logd(\"我是异步执行的代码\");\n            sleep(1000);\n            if (thread.isCancelled(tid)) {\n                break;\n            }\n        }\n    });\n    logd(\"tid \" + tid);\n    //5s后取消线程\n    sleep(5000);\n    logd(\"取消线程 \" + tid);\n    thread.cancelThread(tid);\n    sleep(5000);\n    logd(\"结束 \");\n}\n\nmain();",
  },
  // ===================== event =====================
  {
    name: "setAgentCallParam",
    platform: "android",
    category: "event",
    signature: "event.setAgentCallParam()",
    description: "setAgentCallParam 代理全局通信设置超时",
    example: "function main() {\n    var result = agentEvent.setAgentCallParam({\"remoteCallTimeout\": 10000});\n    if (result) {\n        toast(\"是\");\n    } else {\n        toast(\"否\");\n    }\n}\n\nmain();",
  },
  {
    name: "setCurrentIme",
    platform: "android",
    category: "event",
    signature: "event.setCurrentIme()",
    description: "setCurrentIme 设置当前的输入法",
    example: "function main() {\n    var result = agentEvent.setCurrentIme();\n    if (result) {\n        toast(\"是\");\n    } else {\n        toast(\"否\");\n    }\n}\n\nmain();",
  },
  {
    name: "restoreIme",
    platform: "android",
    category: "event",
    signature: "event.restoreIme()",
    description: "restoreIme 恢复到之前的输入法",
    example: "function main() {\n    var result = agentEvent.restoreIme();\n    if (result) {\n        toast(\"是\");\n    } else {\n        toast(\"否\");\n    }\n}\n\nmain();",
  },
  {
    name: "fastScreenshot",
    platform: "android",
    category: "event",
    signature: "event.fastScreenshot()",
    description: "fastScreenshot 快速截屏幕的截图",
    example: "function main() {\n    var result = agentEvent.fastScreenshot(\"/sdcard/a.jpg\");\n    toast(\"result:\" + result);\n}\n\nmain();",
  },
  {
    name: "inputEvent",
    platform: "android",
    category: "event",
    signature: "event.inputEvent()",
    description: "inputEvent 执行输入事件",
    example: "function main() {\n    var result = agentEvent.inputEvent(1, 10, 10, 0);\n    if (result) {\n        toast(\"成功\");\n    } else {\n        toast(\"失败\");\n    }\n}\n\nmain();",
  },
  {
    name: "touchDown",
    platform: "android",
    category: "event",
    signature: "event.touchDown()",
    description: "touchDown 执行按下",
    example: "function main() {\n    //整个动作需要配合使用,单独无效\n    //按下\n    agentEvent.touchDown(100, 100)\n    sleep(50)\n    //移动\n    agentEvent.touchMove(100, 150)\n    sleep(50)\n    //移动\n    agentEvent.touchMove(100, 200)\n    sleep(50)\n    //抬起\n    agentEvent.touchUp(100, 200)\n    sleep(200)\n}\n\nmain();",
  },
  {
    name: "touchMove",
    platform: "android",
    category: "event",
    signature: "event.touchMove()",
    description: "touchMove 执行移动",
    example: "function main() {\n    //整个动作需要配合使用,单独无效\n    //按下\n    agentEvent.touchDown(100, 100)\n    sleep(50)\n    //移动\n    agentEvent.touchMove(100, 150)\n    sleep(50)\n    //移动\n    agentEvent.touchMove(100, 200)\n    sleep(50)\n    //抬起\n    agentEvent.touchUp(100, 200)\n    sleep(200)\n}\n\nmain();",
  },
  {
    name: "touchUp",
    platform: "android",
    category: "event",
    signature: "event.touchUp()",
    description: "touchUp 执行弹起输入",
    example: "function main() {\n    //整个动作需要配合使用,单独无效\n    //按下\n    agentEvent.touchDown(100, 100)\n    sleep(50)\n    //移动\n    agentEvent.touchMove(100, 150)\n    sleep(50)\n    //移动\n    agentEvent.touchMove(100, 200)\n    sleep(50)\n    //抬起\n    agentEvent.touchUp(100, 200)\n    sleep(200)\n}\n\nmain();",
  },
  {
    name: "pressKey",
    platform: "android",
    category: "event",
    signature: "event.pressKey()",
    description: "pressKey 模拟按键",
    example: "function main() {\n    var result = agentEvent.pressKey(\"home\");\n    if (result) {\n        toast(\"成功\");\n    } else {\n        toast(\"失败\");\n    }\n}\n\nmain();",
  },
  {
    name: "pressKeyCode",
    platform: "android",
    category: "event",
    signature: "event.pressKeyCode()",
    description: "pressKeyCode 模拟键盘输入",
    example: "function main() {\n    var result = agentEvent.pressKeyCode(65);\n    if (result) {\n        toast(\"成功\");\n    } else {\n        toast(\"失败\");\n    }\n}\n\nmain();",
  },
  {
    name: "pressKeyCodeWithMetaState",
    platform: "android",
    category: "event",
    signature: "event.pressKeyCodeWithMetaState()",
    description: "pressKeyCodeWithMetaState 模拟键盘输入",
    example: "function main() {\n    var result = agentEvent.pressKeyCodeWithMetaState(65, 1);\n    if (result) {\n        toast(\"成功\");\n    } else {\n        toast(\"失败\");\n    }\n}\n\nmain();",
  },
  {
    name: "menu",
    platform: "android",
    category: "event",
    signature: "event.menu()",
    description: "menu 打开菜单",
    example: "function main() {\n    var result = agentEvent.menu();\n    if (result) {\n        toast(\"成功\");\n    } else {\n        toast(\"失败\");\n    }\n}\n\nmain();",
  },
  {
    name: "enter",
    platform: "android",
    category: "event",
    signature: "event.enter()",
    description: "enter  Enter键",
    example: "function main() {\n    var result = agentEvent.enter();\n    if (result) {\n        toast(\"成功\");\n    } else {\n        toast(\"失败\");\n    }\n}\n\nmain();",
  },
  {
    name: "delete",
    platform: "android",
    category: "event",
    signature: "event.delete()",
    description: "delete 删除键",
    example: "function main() {\n    var result = agentEvent.delete();\n    if (result) {\n        toast(\"成功\");\n    } else {\n        toast(\"失败\");\n    }\n}\n\nmain();",
  },
  {
    name: "search",
    platform: "android",
    category: "event",
    signature: "event.search()",
    description: "search 搜索",
    example: "function main() {\n    var result = agentEvent.search();\n    if (result) {\n        toast(\"成功\");\n    } else {\n        toast(\"失败\");\n    }\n}\n\nmain();",
  },
  {
    name: "closeScreen",
    platform: "android",
    category: "event",
    signature: "event.closeScreen()",
    description: "closeScreen 关闭屏幕",
    example: "function main() {\n    // 如果这个函数没用处，请到论坛下载下载另外一个插件\n    // https://bbs.ieasyclick.com/thread-617-1-1.html\n    var x = agentEvent.closeScreen();\n}\n\nmain();",
  },
  {
    name: "lightScreen",
    platform: "android",
    category: "event",
    signature: "event.lightScreen()",
    description: "function main() {    var x = agentEvent.lightScreen();    // 如果这个函数没效果    // 使用这个命令强制电量屏幕    // agentEvent.execShellCommandEx(\"input keyevent 26 && input keyevent 82 && input keyevent 82\")}main();上一页无障碍事件下一页HID事件说明设置setAgentCallParam 代理全局通信设置超时输入数据setCurrentIme 设置当前的输入法restoreIme 恢复到之前的输入法截图fastS...",
    example: "function main() {\n    var x = agentEvent.lightScreen();\n    // 如果这个函数没效果\n    // 使用这个命令强制电量屏幕\n    // agentEvent.execShellCommandEx(\"input keyevent 26 && input keyevent 82 && input keyevent 82\")\n}\n\nmain();",
  },
];

/**
 * 按平台和分类筛选API
 */
export function queryApi(options: {
  platform?: string;
  category?: string;
  keyword?: string;
}): ApiEntry[] {
  let results = EC_API_DATABASE;

  if (options.platform && options.platform !== "all") {
    results = results.filter(
      (api) =>
        api.platform === options.platform || api.platform === "common"
    );
  }

  if (options.category) {
    results = results.filter((api) => api.category === options.category);
  }

  if (options.keyword) {
    const kw = options.keyword.toLowerCase();
    results = results.filter(
      (api) =>
        api.name.toLowerCase().includes(kw) ||
        api.description.toLowerCase().includes(kw) ||
        api.signature.toLowerCase().includes(kw) ||
        api.category.toLowerCase().includes(kw)
    );
  }

  return results;
}

/**
 * 获取所有API分类
 */
export function getCategories(): string[] {
  const cats = new Set(EC_API_DATABASE.map((api) => api.category));
  return Array.from(cats).sort();
}

/**
 * 获取API按分类的分组统计
 */
export function getApiStats(): Record<string, number> {
  const stats: Record<string, number> = {};
  for (const api of EC_API_DATABASE) {
    stats[api.category] = (stats[api.category] || 0) + 1;
  }
  return stats;
}
